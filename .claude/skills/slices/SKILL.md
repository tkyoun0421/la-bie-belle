---
name: slices
description: "[Epic]을 수직 슬라이스 [Slice] sub-issue로 쪼갠다. 문서는 만들지 않는다. `/doc` 라우터가 넘긴다. Dev 전용."
---

# Slices

Epic 번호를 받아 그것이 링크한 스펙을 읽고, 수직 슬라이스마다 `[Slice]` Issue를 하나씩 발행한다. 파일은 쓰지 않고 PR도 열지 않는다.

`/doc`을 지나지 않고 여기로 들어왔으면 `/doc`으로 다시 시작해라. 종류를 가리는 것은 라우터가 갖는다.

## 원천을 읽는다

```
gh issue view <epic> --comments
```

`docs/specs/<feature>.md` 링크를 따라가 파일을 읽어라. Issue 본문은 요약이다 — 인수 조건은 파일에 살고, 슬라이스가 나누는 것이 그 조건들이다.

라우터가 넘긴 인용에서 구현을 묶는 결정을 확인해라. 모양을 정하기 전이다.

## 슬라이스를 자른다

슬라이스는 모든 층을 관통하는 좁지만 완결된 경로 하나를 자른다 — 스키마, API, 화면, 테스트다. 그 자체로 시연 가능하고, 새 컨텍스트 창 하나에 들어갈 크기다. 층을 가로로 자른 것은 슬라이스가 아니다. "데이터베이스 테이블 추가"는 사람이 볼 수 있는 것을 아무것도 남기지 않는다.

각 슬라이스는 스펙의 번호 매긴 인수 조건 중 일부를 맡는다. 모든 조건은 정확히 한 슬라이스에 앉는다. 아무도 안 맡은 조건이 있으면 Epic이 다 쪼개지지 않은 것이고, 두 번 맡은 조건이 있으면 두 슬라이스가 같은 코드를 두고 다툰다.

이 대응은 쪼개는 자리에서 확인하고 Issue 본문에 옮겨 적지 않는다. 조건의 정본은 스펙 파일이며 `[Slice]` 폼에는 그걸 받는 칸이 없다 — "확인 방법" 칸은 직접 눌러 보는 순서를 받는 자리이지 조건을 복사하는 자리가 아니다. 슬라이스가 무엇을 맡았는지는 "이 슬라이스가 끝나면"이 사람 말로 말한다. Epic을 다 쪼갠 뒤 조건 목록을 한 번 훑어 빠진 것이 없는지 봐라. 있으면 슬라이스를 더 잘라라.

먼저 끝나야 하는 슬라이스를 지목해라. 선행이 없는 슬라이스는 곧바로 시작할 수 있다.

슬라이스마다 어떻게 만드는지도 개괄해라 — 화면·서버·데이터가 각각 무엇을 필요로 하는지, 그리고 두 사용자가 부딪히거나 호출이 실패할 때 무엇을 조심해야 하는지다. `[Slice]` 폼이 둘 다 묻고, 답할 수 있는 건 Dev뿐이다. 개괄에 머물러라 — 정확한 endpoint 시그니처, 컬럼 타입, 응답 형태는 Issue가 아니라 설계 문서의 몫이다.

## 계약을 못 박는다

슬라이스를 던지기 전에 정한다. 나중에 정하면 두 슬라이스가 서로 다른 답을 들고 만난다.

- 슬라이스마다 **쓸 수 있는 경로의 정확한 목록**. 두 슬라이스가 같은 경로를 쥐면 하나를 앞선 의존으로 돌리거나 통합 슬라이스로 뗀다
- 슬라이스 사이를 지나는 **인터페이스와 데이터 형태**
- **에러와 호환 규약** — 실패를 어떻게 돌려주고 옛 형태를 언제까지 받아 주나

이 결정들은 `[Slice]` Issue 본문의 만드는 방법 칸에 개괄로 앉는다. 정확한 시그니처와 컬럼 타입은 여전히 설계 문서의 몫이다.

## 통합을 누가 보나

슬라이스 다섯이 각각 초록으로 닫혀도 합쳐서 돈다는 뜻은 아니다. 지금 그것을 보는 자리가 없다.

슬라이스를 다 발행한 뒤 Epic에 **통합 gate를 코멘트로 남겨라**. 종단 경로 하나, 슬라이스 사이 인터페이스, 그리고 기존 동작이 안 깨졌는지다. 형식은 `docs/rules/matchers/gates.md`의 원장 그대로다.

```
- [ ] N1: 예약을 만들고 취소까지 한 번에 지난다
  CHECK: pnpm test:e2e reservation
  EXPECT: 4 passed
  EVIDENCE: pending
```

Epic 본문은 고치지 마라. 코멘트로 남긴다. 슬라이스가 전부 닫히면 **PM이** 최신 `origin/main`에서 이 gate를 돌리고 결과를 같은 Epic에 코멘트한다. 초록이면 닫고 빨간불이면 닫지 않고 그 결과를 근거로 Dev에게 `[Request]`를 연다. 닫는 사람이 닫는 근거를 스스로 만들고 gate를 쓴 손(Dev)과 통과를 판정하는 손이 갈린다. 명령 실행은 쓰기가 아니라 소유 가드에 걸리지 않는다.

형식만 빌린 것이고 `scripts/gate-check.py`는 이 gate를 읽지 않는다. 검사는 `.gates/` 아래만 훑는다. 통합 gate를 기계에 물리는 일은 #140이 받는다.

넓은 기계적 리팩터링은 수직 슬라이싱의 예외다. 폭발 반경이 코드베이스 전체를 가로지르는 변경 하나는 예광탄처럼 초록으로 착지할 수 없다. 대신 순서를 매겨라 — 옛 형태 옆에 새 형태를 두고, 호출 지점을 CI가 초록으로 남을 만큼 작은 묶음으로 옮기고, 남은 호출자가 없을 때 옛 형태를 지운다. 각 단계가 자기 슬라이스이고 앞 단계에 막혀 있다.

## 발행

`gh-issue-generator`를 붙이고 슬라이스 목록을 의존 순서로 넘겨라. 그래야 각 Issue가 실제 번호를 참조할 수 있다. 그 에이전트가 Issue를 만들고, Epic의 sub-issue로 붙이고, 라벨을 달고, 카드를 보드에 올린다.

새 슬라이스 카드는 **Backlog**로 간다. 선행이 없는 것만 **Todo**로 간다 — Dev가 곧바로 집을 수 있는 것들이다.

Epic은 고치지도 닫지도 말고 카드도 옮기지 마라. 슬라이스가 전부 닫히면 PM이 닫는다.

이 스킬은 파일을 쓰지 않고 PR도 열지 않으니 딸 브랜치도, 옮길 자기 카드도 없다. Issue가 생기고 보드에 올라가면 슬라이싱은 끝난 것이다.
