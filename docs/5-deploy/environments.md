# 배포 환경

환경별 구성과 제약이 산다. 첫 출시 준비 task가 채운다.

## 환경별 구성

| 환경 | 용도 | 앱·DB·외부 서비스 | 관리 위치·담당 역할 |
| --- | --- | --- | --- |

행은 [Q-01](#q-01)이 정해진 뒤 채운다.

## 설정

| 설정 이름 | 필요한 환경 | 설정 위치 | 읽히는 시점 | 누락 확인 방법 |
| --- | --- | --- | --- | --- |

행은 [Q-01](#q-01)과 [Q-03](#q-03)이 정해진 뒤 채운다. 이름과 보관 위치만 적고 실제 값은 적지 않는다.

## 데이터와 외부 의존성

pg_cron·Database Webhook·Edge Function 셋 다 Free에서 된다. 7일 무활동이면 프로젝트가 멈추고 셋이 같이 멈춘다 — 운영 중엔 매일 접속하니 안 걸리고, 출시 전 스테이징이 걸린다. Edge Function은 월 50만 회다.

- 서체는 앱 번들에 든다([서체 연결](../2-design/design-system/tokens.md#서체-연결)). 바깥 서비스에 안 매인다.
- **한 앱이 여러 환경을 못 본다.** 웹은 주소로 스테이징과 운영이 갈렸는데 설치한 앱은 빌드 하나가 한 곳만 본다. 스테이징을 따로 보려면 그 설정으로 빌드를 하나 더 만들어 내부 배포로 깔아야 한다.

## 미정

### Q-01

- 질문: EAS 빌드 프로필을 몇 개 두고 각각 어느 Supabase를 보게 하나
- 영향: 환경별 구성과 설정 표, [배포 절차](procedure.md)의 실행 순서와 되돌리기가 전부 막힌다
- 결정 담당과 시점: 첫 출시 준비 task가 정한다. 어디로 올리는지는 [ADR-011](../2-design/adr/ADR-011-expo-native-app.md)이 이미 정했다
- 필요한 근거와 대안: 내부 배포로 스테이징 빌드를 돌릴지, 개발용 빌드가 로컬 Supabase를 봐도 되는지, 버전과 빌드 번호를 올리는 규칙

### Q-02

- 질문: 원격 Supabase로 언제 어떻게 전환하나
- 영향: 환경별 구성의 DB 칸과 마이그레이션 적용 지점이 막힌다
- 결정 담당과 시점: 첫 출시 준비 task가 Q-01과 같이 정한다
- 필요한 근거와 대안: 로컬과 원격의 마이그레이션 적용 순서, Free 플랜의 정지 조건

### Q-03

- 질문: 환경 변수를 어디에 두고 어떻게 주입하나
- 영향: 설정 표의 설정 위치·읽히는 시점·누락 확인 방법이 막힌다
- 결정 담당과 시점: 첫 출시 준비 task가 Q-01을 정한 뒤 이어 정한다
- 필요한 근거와 대안: EAS가 시크릿을 보관하는 방식, 빌드 시점에 번들에 박히는 값과 실행 시점에 읽는 값의 구분. 앱은 설치된 뒤 값을 못 바꾸므로 잘못 박힌 값은 새 빌드로만 고쳐진다

### Q-04

- 질문: 릴리스 단위를 무엇으로 끊고 기록을 어디에 남기나
- 영향: `releases/`의 첫 파일과 [배포 절차](procedure.md)의 진입 조건이 막힌다
- 결정 담당과 시점: 첫 출시 준비 task가 정한다
- 필요한 근거와 대안: 로드맵의 릴리스 묶음, 한 번에 올릴 마이그레이션 범위
