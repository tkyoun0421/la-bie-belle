# 근무자 대시보드와 출근 인증

근무자가 앱을 열면 보는 화면과 거기서 출근을 찍으러 가는 화면이다. 값은 [tokens.md](../tokens.md)에만 있고 여기는 토큰 이름으로만 말한다.

움직이는 모습은 옆의 `dashboard.sian.html`을 브라우저로 열어 본다. 구현할 때 이 문서와 그 파일과 [tokens.md](../tokens.md) 셋을 같이 연다. 시안과 이 문서가 어긋나면 이 문서가 이긴다.

화면 셋을 한 파일에 둔 것은 셋이 한 흐름이기 때문이다. 대시보드의 주요 액션이 인증 화면으로 가고, 인증이 끝나면 대시보드로 돌아와 그 결과가 띠 아래 한 줄로 남는다. 못 찍은 채로 창이 닫히면 같은 자리에서 사유 시트가 열린다. 뒤의 둘이 무엇을 그리는지는 대시보드가 어떤 상태였는지가 정한다.

[prd.md](../../prd.md#화면)가 대시보드에 담을 넷을 정했다. 위에서부터 안 본 알림, 출근 인증, 이번 주 근무, 내 급여다. 그 순서를 세로 순서로 읽었고 무게는 따로 정했다 — 아래 「짜임」에 근거가 있다.

---

## 대시보드

### 짜임

세로로 쌓는다. 가운데 정렬은 없다.

1. 앱바 — 오늘 날짜와 브랜드 마크
2. 안 본 알림
3. 가는 선
4. 오늘 한 줄
5. 하루 띠
6. 띠 아래 한 줄
7. 출근 인증 버튼
8. 가는 선
9. 이번 주 근무
10. 이번 주 예상 급여

**카드를 쓰지 않는다.** 넷을 각각 둥근 상자에 담으면 넷이 같은 무게가 된다. 그런데 이 화면에서 지금 해야 할 일은 하나뿐이고 나머지 셋은 확인하는 것이다. 상자로 나누면 무엇이 급한지를 화면이 말하지 못한다.

**대신 하루 띠가 무게를 가져간다.** 5가 화면에서 가장 넓고 6과 7이 거기 붙어 한 덩이로 읽힌다. 9와 10은 가는 선 아래에서 이름표와 값 두 줄로 조용히 선다. [prd.md](../../prd.md#화면)의 순서를 세로로 지키면서 무게는 띠 쪽에 실었다.

**7은 조건부다.** [domain/attendance.md](../../domain/attendance.md#언제-누를-수-있나)가 인증 창을 근무 시작 1시간 전부터 그날 18:00까지로 정했다. 그 밖에서는 이 자리가 이렇게 갈린다.

| 언제 | 7 자리 |
| --- | --- |
| 창이 열리기 전 | 눌리지 않는 버튼. 라벨이 남은 시간을 센다 |
| 창이 열렸을 때 | 「출근 인증하기」 |
| 찍은 뒤 | 버튼이 없다. 6에 인증 시각이 들어간다 |
| 창이 닫힌 뒤에도 안 찍혔을 때 | 경고 블록과 「사유 넣기」 |
| 근무가 없는 날 | 버튼이 없다 |

**찍은 뒤에 버튼이 남지 않는다.** [domain/attendance.md](../../domain/attendance.md)가 한 번 찍으면 되돌릴 수 없다고 정했으니 두 번 누를 일이 없다. 누를 수 없는 버튼을 회색으로 남기면 자리만 먹고 「내가 뭘 잘못했나」를 묻게 한다. 결과는 6이 말한다.

**2도 조건부다.** 안 본 알림이 0건이면 2와 3이 통째로 없다. 빈 목록에 「알림이 없어요」를 적지 않는다 — 없다는 것을 알리려고 자리를 만들면 없는 것이 있는 것보다 눈에 띈다.

### 하루 띠

시간 축 하나에 그날의 근무를 얹는다. 이 화면에서 가장 큰 조각이고 [components.md](../components.md)에 아직 없는 새 조각이다.

**축은 06:00부터 21:00까지 15시간이다.** 홀 근무가 이 범위 안에서 끝난다. 축의 양끝을 눈금으로 그리지 않는다 — 몇 시부터 몇 시까지인지를 읽는 띠가 아니라 「지금 어디쯤인지」를 보는 띠라서 그렇다.

세 겹이다.

| 겹 | 무엇 |
| --- | --- |
| 트랙 | 하루 전체. 옅은 면 하나 |
| 막대 | 근무 시간. 트랙 위 그 시각 자리에 놓인다 |
| 지금 선 | 현재 시각. 트랙 위아래로 조금 튀어나온다 |

**막대 안에 시작과 끝 시각을 넣는다.** 왼쪽 끝에 시작, 오른쪽 끝에 끝이다. 막대 밖에 적으면 화살표나 선으로 어느 막대의 시각인지를 다시 알려야 한다.

**지금 선 위에 현재 시각을 붙인다.** 선만 있으면 그게 지금인지 다른 표시인지 모른다.

**근무가 없는 날은 막대가 없다.** 트랙과 지금 선만 남는다. 빈 상태를 위해 삽화나 다른 배치를 따로 만들지 않는다.

### 안 본 알림

**개수와 가장 최근 한 건만 세운다.** 목록을 펼치지 않는다. [domain/notification.md](../../domain/notification.md#읽음)가 안 본 알림은 눌러야 사라진다고 정했는데, 목록을 통째로 세우면 화면 위쪽이 알림에 다 먹히고 오늘 근무가 아래로 밀린다. 한 줄만 서면 몇 건이 밀려 있든 이 자리는 한 줄이다.

한 건이 차지하는 자리는 두 단이다.

```
안 본 알림 2                                   ✕
● 제목 한 줄
  받은 시각                        [CTA 버튼]
```

**CTA는 오른쪽 끝에 붙고 글자만큼만 잡는다.** 남은 폭을 다 채우지 않는다. 이 자리에서 주인공은 알림 제목이고 CTA는 거기 딸린 것이라, 버튼이 커지면 무게가 뒤집힌다. 오른쪽 끝만 ✕와 맞춰두면 눈이 찾는 자리는 이미 정해진다.

높이는 30px다. 출근 인증 버튼이 44px인데 알림 CTA가 그만 하면 화면에 주요 액션이 둘로 보인다.

**받은 시각과 CTA는 세로 가운데가 맞는다.** 줄 높이를 버튼 높이인 30px로 잡고 둘 다 그 가운데에 세운다. 시각의 줄 간격을 1로 눌러 글자 상자가 글자만큼만 되게 한 다음 가운데를 맞춘다 — 그러지 않으면 줄 간격의 위아래 여백 때문에 눈에는 시각이 조금 올라간 것으로 보인다.

**✕는 「안 본 알림」 머리 줄에 있다.** 항목 안이 아니다. CTA는 그 아래 시각과 한 줄이라, 오른쪽 끝이 위에서 아래로 정렬된다.

✕를 항목 안에 두면 제목이 오른쪽 끝까지 못 간다. 알림 제목은 한 줄로 끝나야 하는데 「박서연 님이 9월 13일 근무를 바꾸고 싶어 해요」 정도가 375픽셀에서 딱 한 줄이라 32픽셀을 뺏기면 두 줄이 된다. 머리 줄은 「안 본 알림 2」뿐이라 오른쪽이 비어 있다.

✕는 그냥 지우고 CTA는 갈 곳으로 데려간다. 한 줄만 서니 ✕가 가리키는 대상이 헷갈릴 일도 없다.

**CTA는 여섯 중 셋에만 붙는다.** [domain/notification.md](../../domain/notification.md)의 종류를 이렇게 나눴다.

| 알림 | CTA |
| --- | --- |
| 근무표가 확정되거나 변경됐을 때 | 근무표 보기 |
| 교대의 각 단계 — 요청이 왔을 때 | 수락 · 거절 |
| 가입 승인 | 없다. 이 화면이 안 보이는 동안 오는 알림이다 |
| 미리 알림 | 없다 |
| 출근 직전 | 없다 |
| 관리자 공지 | 없다 |

**교대 요청은 화면을 안 옮긴다.** 그 줄에서 수락이나 거절을 받는다. 가장 자주 오는 알림이고 답이 둘뿐이라 상세 화면을 거칠 이유가 얇다.

**답해도 줄이 사라지지 않는다. 문구가 결과로 바뀐다.** [domain/swap.md](../../domain/swap.md#절차)가 요청과 수락 다음에 관리자 승인이 또 온다고 정했다. 수락하고 줄이 사라지면 끝난 것으로 읽힌다. 그래서 줄을 남기고 남은 단계를 그 자리에 적는다.

| 무엇을 눌렀나 | 제목 | 아래 줄 |
| --- | --- | --- |
| 수락 | 9월 13일 근무를 바꾸기로 했어요 | 관리자가 승인하면 알려드릴게요 |
| 거절 | 9월 13일 교대를 거절했어요 | 박서연 님에게 알려드렸어요 |

바뀐 줄에서는 안 읽음 점이 표시로 바뀌고 CTA가 빠진다. ✕만 남는다. 이 줄은 안 본 알림 수에서 빠진다.

### 색

| 자리 | 토큰 |
| --- | --- |
| 화면 바탕 | `bg.neutral` |
| 브랜드 마크 면 | `bg.brand-solid` |
| 브랜드 마크 글자 | `fg.brand-contrast` |
| 오늘 날짜 | `fg.neutral-subtle` |
| 안 본 알림 머리 | `fg.neutral-subtle` |
| 안 본 알림 개수 | `fg.neutral-muted` |
| 안 읽음 점 | `bg.brand-solid` |
| 알림 제목 | `fg.neutral` |
| 알림 받은 시각 | `fg.neutral-subtle` |
| 알림 닫기 | `fg.neutral-subtle` |
| 알림 CTA | [components.md](../components.md#button)의 Button secondary |
| 교대 수락 버튼 | [components.md](../components.md#button)의 Button primary |
| 교대 거절 버튼 | [components.md](../components.md#button)의 Button secondary |
| 수락한 알림의 표시 | `fg.positive` |
| 거절한 알림의 표시 | `fg.neutral-subtle` |
| 가는 선 | `stroke.neutral` |
| 오늘 한 줄 — 근무가 있을 때 | `fg.neutral` |
| 오늘 한 줄 — 근무가 없을 때 | `fg.neutral-muted` |
| 띠 트랙 | `bg.neutral-weak` |
| 띠 트랙 — 확정 전 | 면 없이 `stroke.neutral-muted` 점선 |
| 띠 막대 | `bg.brand-band` |
| 띠 막대 위 시각 | `fg.neutral` |
| 지금 선 | `bg.brand-solid` |
| 지금 시각 라벨 | `fg.brand` |
| 띠 아래 한 줄 | `fg.neutral-subtle` |
| 띠 아래 한 줄 — 인증 시각 부분 | `fg.neutral-muted` |
| 인증 체크 | `fg.positive` |
| 출근 인증 버튼 | [components.md](../components.md#button)의 Button primary |
| 눌리지 않는 인증 버튼 | `bg.neutral-disabled` `fg.neutral-disabled` `stroke.neutral-disabled` |
| 버튼 아래 안내 문구 | `fg.neutral-subtle` |
| 못 찍음 블록 면 | `bg.warning-weak` |
| 못 찍음 블록 아이콘 | 팔레트 `warning-500` |
| 블록 제목 | `fg.neutral` |
| 블록 아래 줄 | `fg.neutral-muted` |
| 사유 넣기 버튼 | [components.md](../components.md#button)의 Button secondary |
| 보내는 중 스피너 | 버튼 글자색을 따른다 |
| 사유를 보낸 버튼 면 | `bg.positive-weak` |
| 사유를 보낸 버튼 글자와 체크 | `fg.positive` |
| 이름표 | `fg.neutral-muted` |
| 값 | `fg.neutral` |
| 값 없음 | `fg.neutral-subtle` |

`bg.brand-band`는 아직 없는 토큰이다. 아래 「규칙과 부딪힌 자리」에 근거가 있다.

**띠 막대는 옅은 면이고 인증 버튼은 꽉 찬 면이다.** [pages/login.md](login.md#규칙과-부딪힌-자리)가 알약과 버튼을 두고 내린 판정을 그대로 잇는다. 무게가 갈리면 어디를 누르는지 헷갈리지 않는다. 막대는 애초에 누르는 것이 아니다.

**초록은 둘에서 같은 뜻으로 쓴다.** 띠 아래 인증 체크와 사유를 보낸 버튼이다. 둘 다 이미 끝나서 화면에 남아 있는 상태다. 인증 화면의 축하는 순간이라 여기 안 든다. [color.md](../foundation/color.md)가 `positive`를 「잘 끝난 일. 출근 인증 완료」로 이미 적어뒀다.

**막대 색은 측정하고 골랐다.** [color.md](../foundation/color.md#대비-검증)가 눈으로 판정하지 말라고 했다. `brand-300` 위의 `fg.neutral`이 라이트 10.83, 다크 10.56이다. 후보였던 `brand-400`은 8.08과 7.70이고 `brand-200`은 13.16과 12.69다. `brand-300`을 고른 것은 대비 때문이 아니라 트랙과의 거리 때문이다 — 트랙과의 명도 차가 `brand-200`은 0.060, `brand-300`은 0.120, `brand-400`은 0.205다. 0.060은 막대가 트랙에 묻히고 0.205는 브랜드 색이 화면에서 두 번째 신호가 된다.

### 글자

| 자리 | 유틸 |
| --- | --- |
| 브랜드 마크 글자 | `text-xs font-bold` |
| 오늘 날짜 | `text-sm` |
| 안 본 알림 머리 | `text-xs` |
| 안 본 알림 개수 | `text-xs font-semibold` |
| 알림 제목 | `text-sm` |
| 알림 받은 시각 | `text-xs` |
| 알림 CTA 라벨 | `text-xs font-medium` |
| 오늘 한 줄 | `text-xl font-bold` |
| 띠 막대 위 시각 | `text-xs font-medium` |
| 지금 시각 라벨 | `text-xs font-semibold` |
| 띠 아래 한 줄 | `text-xs` |
| 띠 아래 한 줄 — 인증 시각 부분 | `text-xs font-medium` |
| 인증 버튼 라벨 | `text-sm font-medium` |
| 버튼 아래 안내 문구 | `text-xs` |
| 블록 제목 | `text-sm font-semibold` |
| 블록 아래 줄 | `text-xs` |
| 이름표 | `text-sm` |
| 값 | `text-lg font-semibold` |

[typography.md](../foundation/typography.md#숫자-정렬)대로 `tabular-nums`를 건다. 이 화면에서 거는 자리는 넷이다 — 띠 막대 위 시각, 지금 시각 라벨, 인증 시각, 이번 주 근무와 급여의 값이다. 지금 시각 라벨은 1초마다 바뀌고 카운트다운은 매초 자릿수가 흔들리니 이 둘이 특히 그렇다.

### 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 화면 좌우 여백 | `px-6` |
| 앱바 높이 | `min-h-10` |
| 브랜드 마크 | `size-7` `rounded-sm` |
| 안 본 알림 한 건 | 아래 `pb-3` |
| 안 읽음 점 | `size-1.5` `rounded-full` |
| 알림 제목과 점 사이 | `gap-2.5` |
| 알림 닫기 버튼 | `size-8` `rounded-full`. 머리 줄 오른쪽 끝에 붙는다 |
| 안 본 알림 머리 줄 | `min-h-[30px]` — 닫기 버튼이 들어갈 높이 |
| 알림 CTA 버튼 | `h-[30px]` `px-3` `rounded-full`. 오른쪽 끝에 붙는다 |
| 받은 시각과 CTA가 선 줄 | `min-h-[30px]`. 둘이 세로 가운데로 맞춰진다 |
| 받은 시각과 CTA 사이 | 최소 `gap-3`. 남는 폭은 사이가 가져간다 |
| 수락과 거절 사이 | `gap-1.5` |
| 오늘 한 줄과 띠 사이 | `mb-5` |
| 띠 트랙 | `h-8` `rounded-sm` |
| 띠 막대 | 트랙 안쪽으로 위아래 `inset-y-1` `rounded-xs` |
| 띠 막대 좌우 안쪽 여백 | `px-2` |
| 지금 선 | `w-0.5` `rounded-full`. 트랙 위아래로 각각 7px 튀어나온다 |
| 띠와 아래 한 줄 사이 | `mt-2.5` |
| 띠 덩이와 인증 버튼 사이 | `mt-4` |
| 인증 버튼 | `h-11` `rounded-full` |
| 버튼과 안내 문구 사이 | `mt-2` |
| 블록 | `rounded-lg` `p-4` |
| 가는 선 위아래 | `my-5` |
| 이름표와 값 한 줄 | `py-3` |

**띠 막대의 모서리가 트랙보다 작다.** 트랙이 `rounded-sm`이고 막대가 `rounded-xs`다. 막대가 트랙 안에 들어 있는 것으로 읽히려면 안쪽 모서리가 더 작아야 한다.

**알림 닫기 버튼이 32px다.** [spacing-shape.md](../foundation/spacing-shape.md)에 최소 터치 크기 규칙이 아직 없다. 아래 「규칙과 부딪힌 자리」에 적었다.

**사유 버튼은 두 상태가 같은 크기다.** `h-11`과 `rounded-full`을 그대로 두고 안쪽 내용만 바뀐다. 보내는 중은 이 버튼에 없다 — 시트가 그 시간을 맡는다.

### 문안

| 자리 | 문장 |
| --- | --- |
| 브랜드 마크 | 라 |
| 오늘 날짜 | 9월 12일(토) |
| 안 본 알림 머리 | 안 본 알림 2 |
| 오늘 한 줄 — 근무가 있을 때 | 오늘 근무가 있어요 |
| 오늘 한 줄 — 근무가 없을 때 | 오늘은 근무가 없어요 |
| 오늘 한 줄 — 확정 전 | 9월 근무표는 아직 확정 전이에요 |
| 오늘 한 줄 — 이번 주 배정이 없을 때 | 이번 주는 근무가 없어요 |
| 띠 아래 한 줄 — 찍기 전 | 안내 · 9시간 |
| 띠 아래 한 줄 — 찍은 뒤 | 9:52에 출근 인증했어요 · 안내 · 9시간 |
| 띠 아래 한 줄 — 근무가 없을 때 | 다음 근무는 9월 12일(토) 10:00이에요 |
| 띠 아래 한 줄 — 확정 전 | 관리자가 확정하면 알려드릴게요 |
| 인증 버튼 — 창이 열리기 전 | 1시간 29분 뒤 출근 인증 |
| 인증 버튼 — 창이 열렸을 때 | 출근 인증하기 |
| 버튼 아래 안내 | 홀에서 누르면 위치로 확인해요 |
| 못 찍음 블록 제목 | 출근 인증을 못 찍었어요 |
| 못 찍음 블록 아래 줄 — 보내기 전 | 왜 못 찍었는지 남기면 관리자가 확인하고 근무 시간을 살려요 |
| 못 찍음 블록 아래 줄 — 보낸 뒤 | 관리자가 확인하면 알려드릴게요 |
| 사유 버튼 — 누르기 전 | 사유 넣기 |
| 사유 버튼 — 보낸 뒤 | 사유를 보냈어요 |
| 이름표 | 이번 주 근무 · 이번 주 예상 급여 |

**카운트다운은 남은 시간이 앞이고 할 일이 뒤다.** 「1시간 29분 뒤 출근 인증」이다. 「출근 인증은 1시간 29분 뒤에 열려요」로 쓰면 매초 바뀌는 숫자가 문장 가운데 있어 눈이 그걸 찾아야 한다. 숫자를 앞에 두면 시선이 한 자리에 머문다.

용어는 [writing.md](../writing.md#도메인-용어를-그대로-쓴다)가 못 박은 「출근 인증」을 그대로 쓴다. 「출근 확인」이나 「체크인」으로 줄이지 않는다.

**「결근」이라고 안 쓴다.** [domain/attendance.md](../../domain/attendance.md#용어)가 결근을 「인증 없이 그날이 지난 것」으로 정했으니 창이 닫힌 시점에 이미 결근이 맞다. 그런데 사유를 넣고 관리자가 승인하면 근무 시간이 살아난다. 승인이 남아 있는 동안 결근이라고 부르면 확정된 것으로 읽힌다.

**사유를 보내도 새 블록이 안 뜬다.** 결과는 시트 안에서 나오고, 시트가 닫히면 경고 블록의 아래 줄이 다음 단계로 바뀌어 있고 버튼이 체크로 남아 있다. 성공 블록을 하나 더 띄우면 화면이 그만큼 길어지고 방금 본 결과를 두 번 말하게 된다.

**날짜를 약속하지 않는다.** 사유를 보낸 뒤에 「관리자가 확인하면 알려드릴게요」로 끝낸다. 관리자가 언제 확인하는지가 [domain/](../../domain/)에 없다. 승인 대기 화면이 내린 것과 같은 판정이다.

**시각은 24시간제다.** [writing.md](../writing.md#숫자와-단위)를 따른다. 한 자리 시각의 앞자리 0은 안 붙인다 — `9:52`지 `09:52`가 아니다. 그 문서가 한 자리 시각의 예를 안 들어서 여기서 정했다.

### 모션

**등장 모션이 없다.** [motion.md](../foundation/motion.md#자주-일어나는-것은-움직이지-않는다)가 자주 일어나는 인터랙션에는 모션을 더하지 말라고 했다. 대시보드는 근무자가 앱을 열 때마다 보는 화면이고 하루에 몇 번씩 다시 온다. 로그인 화면이 한 번 보고 마는 것과 다르다.

**카운트다운은 1초마다 숫자만 바뀐다.** 자리가 움직이거나 밝기가 흔들리지 않는다. 매초 움직이는 것은 여기 있는 유일한 것이고 그마저 자릿수 하나다.

**지금 선은 안 움직인다.** 1분마다 자리를 다시 잡을 뿐 옮겨 가는 것을 보여주지 않는다. 화면을 보고 있는 동안 선이 기어가면 시선이 계속 그리로 끌린다.

**알림 지우기.** 나가는 줄이 오른쪽으로 밀리며 밝기가 빠진다. 시간은 `--duration-base`고 이동은 `--duration-slow`다. 그 뒤 다음 줄이 아래에서 올라오며 밝아진다. 마지막 하나가 나가면 영역이 통째로 접힌다.

**교대에 답할 때.** 바뀌는 것들의 밝기가 먼저 빠지고(`--duration-base`) 새 문구가 아래에서 올라오며 밝아진다(`--duration-slow`). 줄 자체는 자리를 안 옮긴다. 자리가 그대로여야 「이 줄이 바뀐 것」으로 읽힌다.

**사유 넣기.** 버튼을 누르면 시트가 아래에서 올라온다. 이 화면에서는 그것뿐이고, 보내는 동안과 결과는 [사유 시트](#사유-시트)가 맡는다.

**시트가 닫힌 뒤 버튼이 바뀐다.** 시트가 내려가고 220ms 뒤에 버튼이 「사유를 보냈어요」가 되고 블록의 아래 줄이 바뀐다. 시트가 화면을 덮고 있는 동안 뒤에서 바뀌면 사람이 그 변화를 못 본다.

**버튼 눌림.** `--duration-fast`로 살짝 줄어든다.

**움직임 줄이기.** 켠 사람에게는 이동이 빠지고 밝기만 바뀐다. 이동 계열 클래스에만 `motion-safe:`를 붙인다. 도는 원은 멈추고 체크는 커지는 것 없이 나타난다.

---

## 출근 인증

「출근 인증하기」를 누르면 넘어가는 화면이다.

### 짜임

지도가 화면을 끝까지 채우고 그 위에 둘이 얹힌다.

1. 지도 — 화면 전체
2. 지도 위 버튼 둘 — 왼쪽 위에 닫기, 오른쪽 위에 내 위치로
3. 아래 시트 — 제목, 아래 줄, 버튼

**지도가 배경이 아니라 내용이다.** 상자 안에 지도를 넣고 그 아래에 설명을 쌓으면 지도가 장식이 된다. 이 화면에서 판단의 근거는 내가 어디 있느냐 하나뿐이라 그것이 화면 전체여야 한다.

지도 위에 셋을 그린다.

| 무엇 | 어떻게 |
| --- | --- |
| 내 위치 | 점 하나. 그 둘레에 옅은 원판이 정확도를 그린다 |
| 홀 | 이름표가 달린 앵커 |
| 허용 반경 | 홀을 중심으로 한 점선 원 |

**점선 원이 100미터다.** [domain/attendance.md](../../domain/attendance.md#두-길)가 정한 값이다. 내 점이 그 안에 있는지 밖에 있는지가 곧 답이라, 되는지 안 되는지를 글로 읽기 전에 눈으로 본다.

**QR은 항상 아래에 있다.** 위치가 잡히는 동안에도 열려 있다 — 실내에서 위치가 안 잡히는 일이 흔한데 그때 길을 찾아 헤매게 하지 않는다. 다만 버튼은 아니다. 무게는 아래 「세 모습」에 적었다.

### 세 모습

한 화면이 상태에 따라 셋으로 갈린다.

| 모습 | 언제 | 지도 | 시트 |
| --- | --- | --- | --- |
| 확인 중 | 들어왔을 때의 기본 | 내 점에서 원이 퍼진다 | 눌리지 않는 「출근 인증하기」, 아래에 QR 글 |
| 인증됐다 | 인증이 기록됐을 때 | 브랜드 색이 터지고 흰 원에 체크가 그어진다 | 인증 시각과 거리, 「대시보드로 돌아가기」 |
| 멀거나 안 잡힘 | 홀에서 100미터 밖일 때 | 내 점이 점선 원 밖에 있다 | 「위치 다시 확인하기」, 아래에 QR 글 |

**QR은 세 모습 어디서도 버튼이 아니다.** 밑줄 친 글로 시트 맨 아래에 놓는다.

[domain/attendance.md](../../domain/attendance.md#두-길)가 위치와 QR 둘을 정했지만 둘의 무게가 같지는 않다. 위치는 앱이 알아서 하는 것이고 QR은 사람이 코드를 찾아가 비추는 것이라, QR을 버튼으로 세우면 매번 둘 중 하나를 고르는 화면이 된다. 대부분은 고를 일이 없다.

**홀에서 멀 때도 주요 액션은 「위치 다시 확인하기」다.** 320미터는 걸어서 좁혀지는 거리고 그게 먼저 할 일이다. 여기서 QR을 primary로 올리면 「멀면 QR로 찍으면 된다」로 읽혀서, 홀 밖에서 코드 사진만 있으면 찍히는 것처럼 보인다. [writing.md](../writing.md#능동형과-긍정형을-먼저-쓴다)가 되는 것을 먼저 말하라고 한 것은 되는 길을 내밀라는 뜻이지 보조 수단을 승격하라는 뜻이 아니다.

**위치 권한을 거부한 경우는 여기 없다.** 「멀다」와 「못 본다」는 다른 상태고 후자는 지도 자체가 안 뜬다. 아래 「아직 안 정한 것」에 있다.

### 색

| 자리 | 토큰 |
| --- | --- |
| 지도 바닥 | 팔레트 `neutral-100` |
| 지도 건물 | 팔레트 `neutral-200` |
| 지도 도로 | 팔레트 `neutral-00` |
| 지도 공원 | `bg.positive-weak` |
| 지도 글자 | 팔레트 `neutral-600` |
| 지도 위 버튼 면 | `bg.neutral` |
| 지도 위 버튼 아이콘 | `fg.neutral` |
| 내 위치 점 | `bg.brand-solid`. 테두리는 `bg.neutral` |
| 정확도 원판 | `bg.brand-solid`을 옅게 |
| 퍼지는 원 | `stroke.brand-solid` |
| 축하의 섬광·원판·링·방사 | `bg.brand-solid` |
| 정확도 원판 — 인증된 뒤 | `bg.brand-solid`을 옅게 |

| 홀 이름표 면 | `bg.neutral-solid` |
| 홀 이름표 글자 | `fg.neutral-contrast` |
| 허용 반경 원 | `stroke.neutral-muted` 점선 |
| 시트 면 | `bg.neutral` |
| 시트 손잡이 | `stroke.neutral-muted` |
| 시트 제목 | `fg.neutral` |
| 시트 아래 줄 | `fg.neutral-subtle` |
| 시트 주요 버튼 | [components.md](../components.md#button)의 Button primary |
| QR 글 | `fg.neutral-muted`. 밑줄은 `stroke.neutral-muted` |
| 인증된 뒤 원 | `bg.neutral`. 테두리는 `bg.brand-solid` |
| 인증된 뒤 체크 | `bg.brand-solid` |

**지도 색이 팔레트를 직접 쓴다.** 지도는 우리가 그리는 면이 아니라 지도 API가 그리는 타일이라, 역할 토큰으로 부를 자리가 아니다. 그쪽 스타일 설정에 넣을 값이라 팔레트 단계로 적었다.

**지도도 다크에서 뒤집힌다.** [tokens.md](../tokens.md#적응형-팔레트)의 명도가 통째로 뒤집힌다는 규칙을 지도 타일도 따른다.

### 글자

| 자리 | 유틸 |
| --- | --- |
| 홀 이름표 | `text-xs font-semibold` |
| 지도 글자 | 지도 API가 정한다 |
| 시트 제목 | `text-lg font-bold` |
| 시트 아래 줄 | `text-sm` |
| 버튼 라벨 | `text-sm font-medium` |
| 버튼 아래 안내 문구 | `text-xs` |

인증 시각과 거리에 `tabular-nums`를 건다.

### 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 지도 위 버튼 | `size-9.5` `rounded-full` |
| 지도 위 버튼 여백 | 위 `top-3.5`, 좌우 `inset-x-4` |
| 내 위치 점 | `size-5` `rounded-full`. 테두리 3px |
| 시트 | 위쪽만 `rounded-xl` |
| 시트 안쪽 여백 | `px-6` `pb-6` |
| 시트 손잡이 | `w-8` `h-1` `rounded-full` |
| 시트 제목과 아래 줄 사이 | `mt-1` |
| 시트 아래 줄과 버튼 사이 | `mt-4` |
| 버튼과 QR 글 사이 | `mt-3.5` |
| QR 글 | `py-1.5`. 밑줄은 글에서 3px 떨어진다 |

시트 아래 여백에 `env(safe-area-inset-bottom)`을 더한다.

### 문안

| 자리 | 문장 |
| --- | --- |
| 홀 이름표 | 라비에벨 |
| 제목 — 확인 중 | 위치를 확인하고 있어요 |
| 아래 줄 — 확인 중 | 홀 근처인지 보는 중이에요 |
| 제목 — 인증됐다 | 출근 인증했어요 |
| 아래 줄 — 인증됐다 | 9:52에 찍혔어요 · 홀에서 18m |
| 제목 — 멀 때 | 홀에서 조금 떨어져 있어요 |
| 아래 줄 — 멀 때 | 홀에서 320m 거리예요 · 100m 안에서 찍을 수 있어요 |
| 주요 버튼 — 확인 중 | 출근 인증하기 |
| 주요 버튼 — 인증됐다 | 대시보드로 돌아가기 |
| 주요 버튼 — 멀 때 | 위치 다시 확인하기 |
| QR 글 — 확인 중 | 위치가 안 잡히면 QR로 인증하기 |
| QR 글 — 멀 때 | 실내라 위치가 안 잡히면 QR로 인증하기 |

**「끝났어요」를 안 쓴다.** 「출근 인증이 끝났어요」는 근무가 끝났다는 뜻으로 읽힐 수 있다. 이 화면은 근무가 시작될 때 뜨는 것이라 정반대의 오해다. 「출근 인증했어요」는 능동이고 무엇을 했는지가 분명하다 — [writing.md](../writing.md#능동형과-긍정형을-먼저-쓴다)가 「누가 무엇을 했는지로 쓴다」고 정한 그대로다.

대시보드로 돌아가면 띠 아래에 「9:52에 출근 인증했어요」가 남는다. 같은 말이라 두 화면이 이어진다.

**인증됐을 때 거리를 같이 적는다.** 「9:52에 찍혔어요」만 적으면 시각만 남는 것으로 보인다. 거리를 붙여 어디서 찍혔는지도 기록에 남는다는 것을 알린다.

**「홀에서 조금 떨어져 있어요」다.** 「인증할 수 없어요」가 아니다. 320미터는 걸어서 좁혀지는 거리고 그렇게 하라는 것이 이 화면의 뜻이다.

### 모션

**퍼지는 원.** 내 점에서 원 셋이 순서를 두고 밖으로 퍼진다. 한 바퀴가 2.6초고 셋이 그 안에서 고르게 어긋난다.

[motion.md](../foundation/motion.md#자주-일어나는-것은-움직이지-않는다)가 자주 일어나는 것에 모션을 얹지 말라고 했는데, 같은 문서의 [축하할 순간](../foundation/motion.md#축하할-순간)이 「로딩은 원형 회전 뒤에 체크로 끝낸다」고 정했다. 이 화면이 정확히 그 자리다. 돌기만 하다 사라지면 끝난 것인지 실패한 것인지 모른다.

**인증되는 순간이 이 앱에서 가장 큰 모션이다.** [motion.md](../foundation/motion.md#축하할-순간)가 「강한 모션은 축하할 순간에만 쓴다」고 하고 「이 앱에서 확실한 축하는 하나다. 출근 인증 완료다」라고 못 박았다. 여기가 그 하나다.

터지는 순서로 일곱이 겹친다.

| 차례 | 무엇 | 어떻게 |
| --- | --- | --- |
| 1 | 섬광 | 브랜드 색이 지도 전체로 한 번 번졌다 사라진다. 660ms |
| 2 | 원판 | 브랜드 면이 한가운데서 2.7배까지 부풀며 사라진다. 520ms |
| 3 | 링 넷 | 테두리 원이 110ms 간격으로 뒤따라 3.1배까지 번진다. 테두리가 7px·5px·3px·2px로 갈수록 얇아진다 |
| 4 | 방사 | 긴 막대 열둘이 132px까지, 짧은 막대 여덟이 86px까지 뻗으며 사라진다. 짧은 쪽이 90ms쯤 늦게 나가 두 겹으로 터진다 |
| 5 | 원 | 지름 58px 흰 원이 0에서 1.4배까지 튀었다 제자리로 온다. 살짝 돌면서 커진다 |
| 6 | 체크 | 그 안에서 브랜드 색 체크가 300ms 뒤 왼쪽부터 그어진다. 520ms |
| 7 | 시트 | 제목·아래 줄·버튼이 60ms 간격으로 아래에서 올라온다 |

**색은 브랜드 하나다.** 초록을 안 쓴다.

처음에는 초록 원에 흰 체크였는데, 그러면 브라운으로 짜인 화면에서 초록 면과 흰 획이 가장 튀는 자리가 된다. 뒤집어서 **흰 원에 브랜드 색 체크**로 놓으면 남는 색이 우리 것뿐이다. 「잘 끝났다」는 뜻은 대시보드로 돌아갔을 때 띠 아래 초록 체크가 맡는다 — 이 화면은 터지는 순간이고 저기가 상태다.

**체크는 그어진다.** 다 그려진 체크가 나타나는 것이 아니라 획이 왼쪽부터 오른쪽으로 간다. 그리는 동안이 곧 기록되는 시간이라 결과가 먼저 뜨고 마는 것보다 오래 남는다.

**셋째 색을 더하지 않는다.** 색을 늘리면 축하가 아니라 오류처럼 읽힌다. 굵기와 크기와 시간차만으로 터지는 느낌을 만든다.

전체가 1초 안에 끝난다. **이 화면 밖으로는 안 나간다.** 대시보드로 돌아가면 띠 아래 체크 하나가 정적으로 남을 뿐이다.

**돌아가는 것은 여기서 멈춘다.** 확인 중에 퍼지던 원은 축하가 시작되면 사라진다. 두 모션이 겹치면 무엇이 끝난 신호인지 안 갈린다.

**시트가 아래에서 올라온다.** `--duration-slower`다. 화면 전환 자체가 지도로 바뀌는 것이라 시트만 따로 등장한다.

**움직임 줄이기.** 켠 사람에게는 퍼지는 원이 멈춘다. 정확도 원판은 남아 위치가 잡히는 범위를 그대로 보여준다. 축하도 섬광과 링과 방사가 빠지고, 체크는 그어지지 않고 다 그려진 채로 나타난다 — 결과는 그대로 전해지고 움직임만 사라진다.

---

## 사유 시트

인증 창이 닫힌 뒤 「사유 넣기」를 누르면 올라오는 바텀시트다. 쓰는 것부터 결과까지 이 시트 안에서 끝난다.

[components.md](../components.md#dialog와-바텀시트)의 Dialog와 바텀시트를 그대로 쓴다. 왼쪽이 「닫기」이고 오른쪽이 primary인 것도 그 문서가 정한 대로다.

### 짜임

| 자리 | 무엇 |
| --- | --- |
| 뒤 덮개 | 대시보드를 덮는다 |
| 손잡이 | 시트 맨 위 가운데 |
| 제목 | 왜 못 찍었는지 알려주세요 |
| 날짜 줄 | 어느 날 어느 근무의 사유인지 |
| 입력 | 라벨 줄 · 칸 · 필요할 때만 나오는 도움말 |
| 버튼 두 개 | 왼쪽 닫기, 오른쪽 보내기 |

**결과가 같은 시트 안에서 난다.** 시트가 사라졌다 다른 것이 뜨는 것이 아니라 시트의 내용만 결과로 바뀐다. 손잡이와 시트의 네 모서리가 그 자리에 그대로 있어서, 방금 누른 것에 대한 답이라는 것이 자리로 읽힌다.

**성공은 저절로 닫히고 실패는 안 닫힌다.** 성공은 1.65초 뒤 시트가 내려간다. 사람이 할 일이 남아 있지 않고, 닫힌 뒤의 대시보드에 결과가 두 자리(블록 아래 줄과 버튼)에 남아 있어서 놓칠 일이 없다. 실패는 다시 보낼지 닫을지를 사람이 정해야 해서 안 닫는다.

**덮개를 눌러도 안 닫힌다.** 쓰던 글이 있는데 바깥을 잘못 누르면 날아간다. 닫는 길은 「닫기」 하나다.

### 모든 자리

| 상태 | 화면 |
| --- | --- |
| 열자마자 | 빈 칸에 자리표시. 보내기 잠김 |
| 다섯 자 미만 | 보내기 잠긴 채 칸 아래에 도움말 |
| 서른 자 남음 | 라벨 줄 오른쪽에 글자 수가 나타난다 |
| 200자 | 숫자가 `critical` 색이 되고 더 안 들어간다 |
| 보내는 중 | 오른쪽 버튼에서 링이 돌고 닫기도 같이 잠긴다 |
| 보냄 | 원과 체크. 1.65초 뒤 저절로 닫힌다 |
| 못 보냄 | 원이 흔들리고 ✕. 쓴 글은 시트에 그대로 |
| 이미 보낸 뒤 다시 열기 | 읽기 전용 칸과 「확인 중」 알약. 버튼은 닫기 하나 |
| 반려된 뒤 | 시트가 아니라 다음에 앱을 열었을 때 대시보드 블록으로 |
| 키보드가 올라옴 | 시트가 키보드 위에 올라앉는다 |

**다섯 자가 최소다.** 「네」나 「아파서」로는 관리자가 판단할 것이 없다. 최소를 두면 사람이 한 문장은 쓴다.

**200자가 상한이다.** [domain/](../../domain/)에 길이 규칙이 없어서 여기서 정했다. 사유는 관리자가 여러 건을 훑는 글이고 한두 문장이면 충분하다. 상한에 닿아도 이미 쓴 글을 지우지 않는다 — 더 안 들어갈 뿐이다.

**글자 수는 서른 자가 남았을 때 나타난다.** 처음부터 세면 안 그래도 쓰기 싫은 칸에 숫자가 하나 더 붙는다. 상한이 가까워질 때만 알린다.

**보내는 동안 닫기까지 잠근다.** 중간에 닫으면 갔는지 안 갔는지 알 길이 없다. 대신 도는 시간이 끝나면 반드시 다음 화면이 온다 — 체크 아니면 ✕다. [motion.md](../foundation/motion.md#축하할-순간)가 「돌기만 하다 사라지면 끝난 것인지 실패한 것인지 모른다」고 한 자리다.

**키보드가 올라오면 시트째 올린다.** 가려지는 쪽만 밀면 입력 칸과 버튼 중 하나가 화면 밖으로 나간다. 둘 다 보여야 쓰고 바로 보낼 수 있다.

### 색

| 자리 | 토큰 |
| --- | --- |
| 뒤 덮개 | 라이트는 `bg.neutral-solid`, 다크는 팔레트의 `neutral-00`. 투명도 42% |
| 시트 면 | `bg.neutral` |
| 제목 | `fg.neutral` |
| 날짜 줄 | `fg.neutral-subtle` |
| 라벨 | `fg.neutral-muted` |
| 입력 칸 면 | `bg.neutral-weak` |
| 입력 칸 테두리 | `stroke.neutral` |
| 입력 칸 테두리 — 쓰는 중 | `stroke.brand-solid` |
| 자리표시 글자 | `fg.neutral-subtle` |
| 입력 글자 | `fg.neutral` |
| 글자 수 | `fg.neutral-subtle` |
| 글자 수 — 상한 | `fg.critical` |
| 도움말 | `fg.neutral-muted` |
| 성공 원 | `bg.neutral` 면에 `bg.brand-solid` 테두리 2px |
| 성공 체크와 번지는 링 | `bg.brand-solid` |
| 실패 원 | `bg.critical-weak` 면에 `bg.critical-solid` 테두리 2px |
| 실패 ✕ | `fg.critical` |
| 읽기 전용 칸 | `bg.neutral-weak`에 테두리 없음. 글자는 `fg.neutral-muted` |
| 「확인 중」 알약 | `bg.neutral-weak` 면에 `fg.neutral-muted` |
| 반려 블록 | `bg.critical-weak`. 아이콘은 `fg.critical` |

**성공은 브랜드 색이고 실패는 `critical`이다.** [color.md](../foundation/color.md)가 `critical`을 「오류와 되돌릴 수 없는 파괴. 결근, 삭제」로 정했다. 보내기 실패는 되돌릴 수 없는 것이 아니지만 사람이 알아야 할 나쁜 소식이고, 이 팔레트에서 나쁜 소식을 말하는 색은 그것뿐이다.

**오류 테두리를 안 쓴다.** [components.md](../components.md#input)의 Input이 오류 테두리를 `bg.critical-solid`로 정했다. 이 시트에서는 다섯 자 미만이어도 테두리를 안 물들인다. 덜 쓴 것은 틀린 것이 아니라 아직 안 쓴 것이고, 쓰는 도중에 붉어지면 재촉으로 읽힌다.

**색만으로 실패를 안 알린다.** 같은 문서가 「색약인 사람에게는 테두리 색 변화가 안 보인다」고 했다. 실패는 색 말고도 셋으로 말한다 — ✕ 모양, 흔들리는 움직임, 「사유를 보내지 못했어요」라는 문장이다.

**덮개가 다크에서 뒤집히면 안 된다.** `bg.neutral-solid`는 [tokens.md](../tokens.md)의 적응형 팔레트를 따라 다크에서 밝은 회색이 된다. 그대로 쓰면 화면을 흰 막이 덮는다. 덮개만 팔레트를 안 따르고 어두운 쪽에 고정한다. 아래 「규칙과 부딪힌 자리」에 적었다.

### 글자

| 자리 | 유틸 |
| --- | --- |
| 제목 | `text-lg font-bold` |
| 날짜 줄 | `text-sm` |
| 라벨 | `text-xs` |
| 글자 수 | `text-xs`. 상한에서 `font-semibold` |
| 입력 글자 | `text-sm` |
| 도움말 | `text-xs` |
| 버튼 라벨 | `text-sm font-medium` |
| 결과 제목 | `text-lg font-bold` |
| 결과 아래 줄 | `text-sm` |
| 「확인 중」 알약 | `text-xs font-semibold` |

날짜와 시각과 글자 수에 `tabular-nums`를 건다. 글자 수는 매 글자 바뀌는 숫자라 폭이 흔들리면 라벨 줄 전체가 떨린다.

### 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 시트 | 위쪽만 `rounded-lg` |
| 시트 안쪽 여백 | `px-6` `pt-2.5` `pb-6` |
| 시트 손잡이 | `w-8` `h-1` `rounded-full` |
| 제목과 날짜 줄 사이 | `mt-1` |
| 날짜 줄과 입력 덩이 사이 | `mt-4` |
| 라벨 줄과 칸 사이 | `mb-1.5` |
| 입력 칸 | `rounded-md` `p-3` `min-h-22` |
| 칸과 도움말 사이 | `mt-1.5` |
| 입력 덩이와 버튼 줄 사이 | `mt-4.5` |
| 버튼 둘 사이 | `gap-2` |
| 닫기 버튼 폭 | `basis-25` 고정 |
| 결과 표시 자리 | `size-24` |
| 결과 원 | `size-16` `rounded-full` |
| 결과 원과 제목 사이 | `mb-4` |
| 키보드가 올라왔을 때 시트 바닥 | `bottom-58` |

시트가 `rounded-lg`인 것은 [components.md](../components.md#dialog와-바텀시트)가 정한 값이다. 인증 화면의 시트가 `rounded-xl`인 것과 다르다 — 그쪽은 지도 위에 얹히는 화면의 일부고 이쪽은 대시보드를 덮는 다이얼로그다.

시트 아래 여백에 `env(safe-area-inset-bottom)`을 더한다.

### 문안

| 자리 | 문장 |
| --- | --- |
| 제목 | 왜 못 찍었는지 알려주세요 |
| 날짜 줄 | 9월 12일(토) · 10:00–19:00 근무 |
| 라벨 | 무슨 일이 있었나요 |
| 자리표시 | 가게에 있었지만 위치가 안 잡혔어요 |
| 도움말 — 다섯 자 미만 | 조금 더 자세히 써주세요 · 다섯 자 이상 |
| 도움말 — 상한 | 여기까지만 쓸 수 있어요 |
| 왼쪽 버튼 | 닫기 |
| 오른쪽 버튼 | 보내기 |
| 오른쪽 버튼 — 보내는 중 | 보내는 중 |
| 성공 제목 | 사유를 보냈어요 |
| 성공 아래 줄 | 관리자가 확인하면 알려드릴게요 |
| 실패 제목 | 사유를 보내지 못했어요 |
| 실패 아래 줄 | 쓴 내용은 그대로 있어요 · 다시 보내볼게요 |
| 실패 오른쪽 버튼 | 다시 보내기 |
| 이미 보낸 뒤 제목 | 보낸 사유예요 |
| 이미 보낸 뒤 날짜 줄 | 9월 12일(토) · 18:52에 보냈어요 |
| 이미 보낸 뒤 라벨 | 보낸 내용 |
| 상태 알약 | 확인 중 |
| 반려 블록 제목 | 사유가 받아들여지지 않았어요 |
| 반려 블록 아래 줄 | 관리자가 「위치 기록이 없어요」라고 남겼어요 |
| 반려 버튼 | 사유 다시 쓰기 |

**실패 문구가 사람을 안 탓한다.** 「쓴 내용은 그대로 있어요」를 먼저 말한다. 보내기가 실패했을 때 사람이 가장 먼저 걱정하는 것은 방금 쓴 글이 날아갔는지다. 무엇이 잘못됐는지는 그다음이고, 그것도 사람이 한 일이 아니다.

**「취소」가 아니라 「닫기」다.** [writing.md](../writing.md#다이얼로그-왼쪽-버튼은-닫기다)가 정한 대로다.

**제목이 질문이다.** 「사유 입력」이 아니라 「왜 못 찍었는지 알려주세요」다. 칸 하나를 채우는 일이라 무엇을 쓰는 칸인지 제목이 말해주면 라벨을 두 번 읽지 않아도 된다.

**「사유」를 화면에 안 쓴다.** 제목과 라벨은 「왜 못 찍었는지」와 「무슨 일이 있었나요」다. [writing.md](../writing.md#도메인-용어를-그대로-쓴다)의 용어 표에 「사유」가 없어서 정본 표기가 아니고, 그 말 자체가 서류의 말이다. 다만 대시보드의 버튼은 「사유 넣기」다 — 거기서는 무엇을 하러 가는지를 짧게 말해야 하고 「왜 못 찍었는지 쓰기」는 버튼에 길다.

### 모션

| 무엇 | 어떻게 | 길이 |
| --- | --- | --- |
| 시트 열기 | 아래에서 올라온다. 덮개가 같은 시간에 밝아진다 | `--duration-slow` |
| 보내는 중 | 오른쪽 버튼 안에서 링이 돈다 | 응답이 올 때까지 |
| 성공 | 링 둘이 밖으로 번지고, 흰 원이 튀어오르고, 체크가 왼쪽부터 그어진다 | 800ms |
| 실패 | 원이 좌우로 흔들리고 ✕가 두 획으로 차례로 그어진다 | 620ms |
| 시트 닫기 | 아래로 내려간다 | `--duration-slow` |

바텀시트가 `--duration-slow`인 것은 [motion.md](../foundation/motion.md)가 정한 값이다.

**축하는 인증 하나뿐이다.** [motion.md](../foundation/motion.md#축하할-순간)가 「이 앱에서 확실한 축하는 하나다. 출근 인증 완료다」라고 못 박았다. 사유를 보낸 것은 축하가 아니라 결과 확인이다.

그래서 같은 어휘를 쓰되 크기를 줄였다. 인증은 일곱 겹이 1050ms 동안 터지고, 여기는 링 둘과 원 하나와 획 하나가 800ms다. 섬광도 방사도 없다. **어휘가 같아야 「끝났다」로 읽히고 크기가 달라야 무엇이 더 큰 일인지 안다.**

**실패는 안 터진다.** 링이 없다. 밖으로 번지는 것은 좋은 소식의 모양이라 실패에 붙이면 뜻이 어긋난다. 실패는 제자리에서 흔들리고 멈춘다.

**✕도 그어진다.** 두 획이 180ms와 400ms에 차례로 간다. 성공의 체크와 같은 어휘라 「같은 자리에서 다른 답이 나왔다」로 읽힌다. 다 그려진 ✕가 나타나면 결과가 먼저 와 있던 것처럼 보인다.

**결과에서 뒤 화면이 안 움직인다.** 시트가 덮고 있는 동안 대시보드는 아무것도 안 바꾼다. 시트가 다 내려간 뒤 220ms에 버튼과 블록이 바뀐다.

**움직임 줄이기.** 켠 사람에게는 시트가 미끄러지지 않고 그 자리에 나타난다. 링과 흔들림이 빠지고 체크와 ✕는 그어지지 않고 다 그려진 채로 나타난다. 커서 깜빡임도 멈춘다.

---

## 값이 없을 때

**숫자를 쓰지 않는다.** `0일`이나 `0원` 대신 `–`다.

0은 「없다」가 아니라 「세어봤더니 0」이다. 근무표가 아직 안 나온 주에 `0원`을 띄우면 이번 주에 못 벌었다는 뜻으로 읽힌다. 사실은 아직 모르는 것이다.

띠도 두 경우를 다르게 그린다. [color.md](../foundation/color.md#근무표에서-색을-쓰는-법)가 근무 없음과 확정 전은 뜻이 반대라 다르게 그리라고 정했다.

| 상태 | 띠 | 값 |
| --- | --- | --- |
| 확정 전 | 트랙이 점선. 면이 없다 | `–` |
| 확정 뒤, 이번 주 배정이 없음 | 트랙이 실선. 막대만 없다 | `–` |
| 확정 뒤, 오늘만 근무가 없음 | 트랙이 실선. 막대만 없다 | 실제 값 |

점선 트랙은 근무표 달력 칸이 확정 전을 그리는 방식을 띠로 옮긴 것이다.

---

## 안 담은 것

**관리자 대시보드.** 관리자도 근무자라 이 화면을 보지만 관리자 모드는 다른 문서다.

**스플래시.** 앱을 열고 데이터가 준비되기까지의 화면이다.

**각 자리를 눌렀을 때 가는 화면.** 근무표, 급여 상세, 알림 목록, 교대 요청 상세, QR 인증이다. 출근 인증 화면과 사유 시트만 여기 담았다 — 대시보드의 주요 액션이 가는 곳이고 그 결과가 대시보드로 돌아와 남기 때문이다. [plan.md](../../plan.md)가 이 task의 범위 밖으로 적은 줄에 걸치는 자리라 여기 밝힌다.

**알림 목록 화면.** 대시보드에 세우는 한 줄만 여기 있다. 밀린 알림을 다 보는 화면은 따로다.

**지도 API를 무엇으로 쓸지.** 이 문서는 지도가 무엇을 그려야 하는지까지고 어느 서비스인지는 안 정했다.

---

## 규칙과 부딪힌 자리

디자인 시스템의 조항과 부딪힌 자리 열넷이다. 몰라서 어긴 것과 알고 정한 것은 다르다. 뒤에 오는 사람이 이 자리를 버그로 고치지 않게 남긴다.

**출근 인증이 하단 고정이 아니다.** [components.md](../components.md#bottomcta)가 BottomCTA를 「화면 하단에 고정되는 주요 액션 버튼」으로 정하고 「출근 인증이 여기다」라고 못 박았다. 이 화면에서는 띠 바로 아래에 있다.

띠가 이유다. 띠는 지금이 인증 창 안인지 밖인지를 그리는 조각이고 버튼은 그 창에 대한 답이다. 둘이 떨어져 있으면 왜 지금 눌리는지가 안 이어진다. 화면 아래에 고정하면 그 사이에 이번 주 근무와 급여가 끼어든다.

**그래서 `components.md`의 그 줄을 고쳐야 한다.** BottomCTA라는 조각을 없애자는 것이 아니다. 출근 인증을 그 조각의 예로 든 것을 뺀다. 이 화면 밖에서 BottomCTA가 필요한 자리가 나오면 그때 다른 예를 든다.

**브랜드 색이 한 화면에 셋이다.** [color.md](../foundation/color.md#브랜드-색을-아끼는-이유)가 브랜드 색이 나가는 자리를 넷으로 한정했다 — 주요 액션 버튼, 활성 탭, 강조 문구, 브랜드 마크다. 이 화면에는 인증 버튼과 브랜드 마크 말고 띠 막대, 지금 선, 알림의 안 읽음 점이 더 있다.

인증 화면까지 세면 자리가 더 는다. 축하의 섬광과 원판과 링과 방사가 전부 브랜드 색이다. 다만 그것들은 800ms 안에 사라지는 것이라 화면에 남는 자리로 세지 않았다.

무게가 갈린다. 누를 수 있는 브랜드 색은 인증 버튼 하나뿐이고 나머지는 전부 안 눌린다. 막대는 `bg.brand-band`의 옅은 면이고 지금 선은 2픽셀이며 안 읽음 점은 6픽셀이다. [components.md](../components.md#button)의 「한 화면에 primary는 하나다」는 지켜진다.

그래도 `color.md`의 목록을 넷으로 두고 이 화면을 예외로 적을지, 「눌리지 않는 표식」을 다섯째로 더할지는 정해야 한다. 지금은 정하지 않았다.

**띠 막대를 가리키는 토큰이 없다.** [tokens.md](../tokens.md)의 `bg` 표에서 `brand-100` 다음이 `brand-200`이고 그다음이 `brand-800`이다. `brand-300`을 가리키는 `bg` 토큰이 없다.

`bg.brand-band`로 이름을 붙여 표에 줄을 더한다. `bg.brand-weak`를 쓰면 안 되는 이유는 그것이 `brand-100`이라 트랙과의 명도 차가 0.060밖에 안 되고 막대가 트랙에 묻히기 때문이다.

**알림 줄이 ListRow가 아니다.** [components.md](../components.md#listrow)의 ListRow가 「근무 목록, 급여 내역, 알림 목록의 한 줄」로 정의됐다. 이 화면의 알림 줄은 그걸 안 쓴다.

ListRow는 목록의 한 줄이고 이 줄은 목록이 아니다. 하나만 서고 오른쪽에 ✕와 CTA가 세로로 붙고 답하면 내용이 바뀐다. 알림 목록 화면이 생기면 거기서는 ListRow를 쓴다. 같은 알림이 두 자리에서 다른 조각으로 그려지는 셈이라, 알림 목록 화면을 그릴 때 다시 본다.

**알림 블록의 「성공」을 안 쓴다.** [components.md](../components.md#알림-블록)의 알림 블록은 안내·성공·경고·오류 넷이다. 사유를 보낸 결과에 성공 블록을 쓰지 않고 시트 안에서 끝냈다. 성공 블록이 필요 없다는 뜻은 아니고, 이 자리에서는 결과가 이미 시트에서 났다.

**덮개 색을 가리키는 토큰이 없다.** [components.md](../components.md#dialog와-바텀시트)가 뒤 덮개를 「`bg.neutral-solid`에 투명도」로 정했는데, 그 토큰은 [tokens.md](../tokens.md)의 적응형 팔레트를 따라 다크에서 밝은 회색이 된다. 덮개가 밝아지면 화면을 흰 막이 덮는다.

덮개는 뒤를 어둡게 하는 것이 일이라 명도가 뒤집히면 안 되는 몇 안 되는 자리다. `bg.scrim`으로 이름을 붙여 두 테마 모두 어두운 값을 가리키게 한다. 이 화면에서는 라이트가 `neutral-1000`이고 다크가 `neutral-00`이다.

**Input의 오류 테두리를 안 쓴다.** [components.md](../components.md#input)가 오류 테두리를 `bg.critical-solid`로 정했다. 사유 칸은 다섯 자 미만이어도 테두리를 안 물들이고 아래 도움말로만 알린다. 아직 쓰는 중인 것을 틀렸다고 말하지 않기 위해서다. 보낸 뒤에 서버가 거절하는 오류가 생기면 그때는 그 조항을 쓴다.

**「사유」가 도메인 용어 표에 없다.** [writing.md](../writing.md#도메인-용어를-그대로-쓴다)의 표에 이 말이 없어서 정본 표기가 무엇인지 근거가 없다. 대시보드 버튼에만 쓰고 시트 안에서는 안 쓰기로 했는데, 표에 줄을 더하고 「쓴다/안 쓴다」를 정해야 한다.

**알림에 셋째 상태가 생긴다.** 지금 알림은 읽음과 안 읽음 둘인데 교대 요청에 답하면 「답했음」이 된다. 안 본 알림 수에서는 빠졌는데 줄은 남아 있는 상태다. [domain/notification.md](../../domain/notification.md#읽음)에 이 상태가 없다.

**「항목을 눌러야 사라진다」가 흔들린다.** 같은 조항이 대시보드 맨 위의 안 본 알림은 항목을 눌러야 사라진다고 정했다. 이제 누를 곳이 셋이다 — 본문과 CTA와 ✕다. ✕는 지우고 CTA는 옮기거나 답하는데, 본문을 누르면 무엇이 되는지 안 정했다.

**최소 터치 크기 규칙이 없다.** 알림의 ✕가 32픽셀이다. [spacing-shape.md](../foundation/spacing-shape.md)에 최소 터치 크기를 정한 조항이 없어서 이 값이 규칙에 맞는지 판단할 근거가 없다. 규칙을 세우고 그 값에 맞춘다. 이 화면에서 가장 작은 누를 것이라 규칙이 생기면 여기가 먼저 걸린다.

**경고 아이콘이 팔레트를 직접 쓴다.** [tokens.md](../tokens.md)에 `fg.warning`이 없다. 못 찍음 블록의 아이콘이 팔레트 `warning-500`을 직접 부른다. `fg.warning`을 더하거나 이 자리를 예외로 적어야 한다.

**값이 없을 때 `–`를 쓴다.** [writing.md](../writing.md#급여는-예상치라고-밝힌다)가 금액을 자르거나 숨기지 말라고 했다. 그 조항은 계산된 금액을 뭉개지 말라는 뜻이고 여기는 계산할 근무가 없는 자리다. 숨기는 것이 아니라 아직 없다는 것을 말한다. 예외로 적는다.

**하루 띠가 컴포넌트 표에 없다.** [components.md](../components.md)에 새 조각으로 올린다. 근무표 날짜 칸처럼 색 규칙이 따로 붙는 조각이라 컴포넌트 문서와 [color.md](../foundation/color.md#근무표에서-색을-쓰는-법) 양쪽에 걸친다.

---

## 아직 안 정한 것

임의로 채우지 않는다. 사람이 정하고 나서 이 절에서 뺀다.

**어느 지도를 쓸지.** 카카오맵과 네이버지도가 후보다. 둘 다 국내 좌표에 정확하고 다크 타일 세트를 준다. 무료 한도와 타일 스타일을 우리 팔레트에 맞출 수 있는지가 갈림길이다.

**홀 좌표를 어디 둘지.** 저장소가 PUBLIC이라 코드에 못 박지 않는다. 인증 화면이 거리를 숫자로 보여주니 앱을 쓰는 사람은 어차피 좌표에 가까워지는데, 그것과 저장소에 적히는 것은 다른 문제다.

**타일이 오기 전 몇 백 밀리초.** 지도 SDK가 붙으면 첫 로드에 시간이 걸린다. 그동안 화면을 무엇으로 채울지 안 정했다.

**위치 권한을 거부했을 때.** 지도 자체가 안 뜬다. 「멀다」와는 다른 화면이 필요하다. 브라우저는 한 번 거부하면 다시 묻지 않으니 「다시 시도」 버튼을 두면 눌러도 아무 일이 안 일어난다.

**인증이 끝나고 저절로 돌아갈지.** 지금은 「대시보드로 돌아가기」를 눌러야 한다. 저절로 넘기면 결과를 못 보고 지나갈 수 있다.

**알림 본문을 누르면 무엇이 되나.** CTA와 같게 할지, 아무것도 안 되게 할지다.

**교대를 잘못 수락하면 어디로 가나.** 되돌리는 길이 이 화면에 없다. [domain/swap.md](../../domain/swap.md)에도 수락을 무를 수 있는지가 없다.

**보낸 사유를 고칠 수 있나.** 다시 열면 읽기 전용으로 그렸는데 [domain/attendance.md](../../domain/attendance.md)에 수정과 취소가 없다. 관리자가 이미 봤을 수도 있어서 그냥 열어주기는 어렵다.

**사유를 언제까지 넣을 수 있나.** 인증 창은 18:00에 닫히는데 사유 창이 언제 닫히는지가 없다. 그날 안인지 다음 급여일 전까지인지 정해야 한다.

**반려된 뒤에 몇 번 다시 넣을 수 있나.** 같은 문서의 「아직 안 정한 것」에 「사유 승인이 거절되면」이 남아 있다. 이 문서의 반려 블록은 그것이 정해지기 전의 제안이다.

**답한 알림이 언제 목록에서 사라지나.** 안 본 알림 수에서는 빠졌는데 줄은 남아 있다. ✕를 눌러야 하는지, 다음에 앱을 열면 없어지는지 안 정했다.

**인증 창이 근무보다 먼저 닫히는 것.** 창은 18:00에 닫히는데 근무는 19:00에 끝난다. 아직 일하고 있는데 더는 찍을 수 없는 한 시간이 있다. 띠에서 인증 창을 따로 그리지 않기로 해서 이 한 시간을 미리 알릴 자리가 없다. 출근 직전 알림이 그 몫을 할지 봐야 한다.

**겸임과 교육 배정.** 한 사람이 하루에 두 자리를 맡을 때 띠에 막대가 둘이 되는지, 하나로 합치는지다. [domain/schedule.md](../../domain/schedule.md)가 교육생은 정규 자리를 안 먹고 덧붙는다고 했다.

**카운트다운을 언제부터 세나.** 지금은 하루 종일 센다. 자정에 앱을 열면 「9시간 뒤 출근 인증」이 뜬다. 몇 시간 전부터 세기 시작할지 정해야 한다.

**막대가 짧을 때 시각을 어디 두나.** 4시간 근무는 막대가 화면 폭의 27퍼센트라 375픽셀에서 85픽셀쯤 된다. 시각 둘이 안 들어간다. 막대 밖으로 뺄지, 하나만 남길지 안 정했다.
