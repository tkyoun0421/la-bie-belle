# P3-T08 기획 인터뷰 handoff

- 작업 식별자: P3-T08 (배정·확정 회귀 테스트 — 공백 메우기로 재정의)
- 현재 단계: 기획 승인 종료 → 다음 설계(RADIO) 인터뷰
- 기준 시각: 2026-08-16
- 기획 승인: user, 2026-08-16 (요약본 승인, `index.jsonl` `product_approval` 기록)

## 확인된 사실 (사용자 답변·요약본 승인)

- **공백 메우기로 재정의.** 원안(PRD 4~7절 규칙 고정)은 P3-T04~T10이 대부분 수행 —
  원안 유지 시 중복 작성이라는 조사 결과를 근거로 재정의를 택했다.
- **범위는 네 묶음 전부**: 축 단위 공백 + backlog 단언 계열 10건 + P3 UI 단위 10종 +
  e2e 위생 7건(부하성 타임아웃은 제품 코드 몫이라 제외).
- **제품 결정 필요 4건은 별도 task** — P3-T11 「확정 경고 표시·조회와 규칙-구현 괴리
  정리」 proposed 등록. 기획 순서는 다음 인터뷰에서.

## 근거 조사 (2026-08-16 커버리지 조사, 조사 상세는 이 파일이 요약 보존)

- pgTAP 17~23번 475단언·P3 단위 30여 파일·e2e 7 spec이 원안 3항목을 대부분 소진.
- 전 계층 무단언: CLOSED·PREPARING 상태 축(픽스처가 OPEN·CONFIRMED·CANCELLED뿐),
  P3 감사 actor(P2는 파일당 1~4건, 17~23번은 0건), 기본 포지션 지정·해제 전이,
  확정→배정표 단일 여정 e2e(roster spec이 CONFIRMED 직접 insert).
- 한 계층만: 확정 차단 4종(e2e 1종), 교육 칩 숨김(단위만), 마지막 예식 단독 변경
  재추천(케이스 없음), 전역 기본값 비전파(e2e만), 필요 0+정식 배정(없음), 교육생
  상한(pgTAP만).
- P3 UI 컴포넌트 10종 단위 테스트 전무 — P1·P2 feature는 전부 보유.

## 비목표

- 제품 코드·DB 스키마 변경(P3-T11 몫), TDD 증거·문서 정합 backlog 4건, 부하성
  타임아웃(backlog 329) 해소.

## 문서 정합화 결과

- phase 03: P3-T08 절 재작성(재정의 근거·네 묶음·인수 조건), P3-T11 절 신설.
- index.jsonl: P3-T08 `design_pending` + `product_approval` + summary·verification·
  depends_on(P3-T09·T10 추가) 갱신, P3-T11 proposed 신설.
- PRD·DOMAIN·DECISIONS: 해당 없음 — 테스트 전용 재정의라 제품 결정이 없다(제품 결정
  후보는 전부 P3-T11로 이동).

## 미결 사항 (다음 단계 소유)

- 네 묶음의 구체 파일·단언 설계, CLOSED·PREPARING 픽스처 확장 방식(e2e insertSchedule
  시그니처), UI 단위 10종의 테스트 깊이, backlog 흡수 줄의 정확한 목록 확정 —
  설계(RADIO) 몫.
- P3-T11 기획 인터뷰 시점 — P3-T08 종료 후 다음 인터뷰에서.

## 다음 행동

1. P3-T08 설계(RADIO) 인터뷰.
2. 커밋은 task 경계 규칙대로 이 세션이 수행.
