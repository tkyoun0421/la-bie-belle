# P0-T37 설계 인터뷰 handoff

## 2026-08-05 · 설계 종료

- 작업 식별자: P0-T37 (오류 코드 정본과 인터페이스 오류 계약)
- 현재 단계: 설계 종료 → 다음 개발
- 기준 시각: 2026-08-05

### 확정된 사실

- **개발 설계 승인(user, 2026-08-05)**. `docs/execution/radio/P0-T37-radio.md` revision 1이 Approved이고, index.jsonl에 `development_approval`(SHA-256 봉인)·`radio_ref`·`test_mode: tdd`·check 2종(`error-lint-test`, `error-registry-typecheck`)이 기록되어 `planned`다.

- **표기: UPPER_SNAKE** (사용자 결정). `ERROR_CODES.SCHEDULING_APPLY_CLOSED`처럼 TS 식별자로 접근 가능한 형태를 택했다. UPPER-KEBAB(문서 스펙 ID 전통)과 소문자 네임스페이스를 제시했으나 런타임 DX를 우선했다.

- **린트 판별: 패턴 전면 금지** (사용자 결정). 접두 6종 패턴에 걸리는 문자열 리터럴을 레지스트리 파일 밖에서 전면 금지한다. 유효한 코드도 리터럴로는 못 쓰고 반드시 레지스트리 멤버로 접근한다. 유효성 검사는 TS 유니언 타입이 담당하고 룰은 레지스트리를 읽지 않는다 — JSON SSOT 값 대조(fsd.json 선례)와 TS 정적 파싱 안을 제시했으나 결합 없는 단순성을 택했다.

### 코드베이스가 결정을 좁힌 것

- `tools/eslint-plugin-project/`에 커스텀 룰 7종과 RuleTester 테스트 관행이 이미 있어 룰 배치는 질문 없이 확정했다. vitest가 `tools/**/*.test.mjs`를 돌린다.
- `config/fsd.json`의 `config` 세그먼트가 `runtimeExports: "constants"`라 순수 상수 레지스트리의 배치 제약이 없고, `eslint.config.mjs`가 이미 `src/shared/config/env.ts`를 예약해 둔 선례가 위치를 정했다.
- 문구 톤은 PATTERNS의 "짧고 친근한 대화체"와 `인터넷 연결이 끊겼어요` 선례를 따라 해요체로 확정했다.

### 조정자가 판단해 RADIO에 넣은 것

레지스트리 경로·파일명, 룰 이름(`project/error-code-literal`), 응답 형태(`{ error: { code, correlationId } }`), COMMON 시드 4종의 코드명·HTTP·기본 문구, `DEV-ERR-03`·`DEV-ERR-04` 신설, 오탐 대조군을 포함한 위험 기반 테스트 표. 실질 트레이드오프가 없거나 선례가 정한 것들이며 승인 요약에 포함해 확정했다.

### 미결 사항

- correlation ID 생성·전파는 첫 서버 오류 경로(P0-T04 이후)가 `DEV-OBS-01` 아래에서 정한다.
- Server Action `code` 타입의 런타임 강제는 첫 Server Action task가 적용한다.

### 다음 행동

1. P0-T37이 `planned` 큐의 실행 후보다. 개발 루프가 선택하면 `in_progress` 전환 후 TDD로 진행한다 — RuleTester에서 실패하는 테스트를 먼저 만들고 룰 구현으로 GREEN을 얻는다.
2. 변경 허용 경로는 RADIO의 기계 판독 절이 소유한다. DEVELOPMENT.md 확장(DEV-ERR-03·04)과 레지스트리·룰·등록이 한 커밋 범위다.
3. P0-T37이 `done`이 되면 P0-T04가 실행 가능해진다(의존성 인코딩 완료).
