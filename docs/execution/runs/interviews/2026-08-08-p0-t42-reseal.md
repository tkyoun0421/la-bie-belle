# P0-T42 재봉인 인터뷰 handoff

## 2026-08-08 · 설계 재봉인 (revision 3)

- 작업 식별자: P0-T42 (Claude Code 사용량 리셋 자동 재개)
- 현재 단계: 설계 재승인 종료 → 다음 개발
- 기준 시각: 2026-08-08

### 배경

revision 2 봉인 파일이 봉인 후 수정으로 무결성이 깨졌다(승인 b788437408…, 실제 ca6d484e…). 봉인 시점 원본은 세션 transcript 전수 재구성, 서브 에이전트 기록, Orca workspace, git stash·dangling blob, 개행 변형 대조까지 모두 실패해 복원 불가로 확정했다. 사용자가 설계 인터뷰 재개를 선택해 본문 전체를 실제 CLI(2.1.224)·공식 문서와 대조한 뒤 revision 3으로 재봉인했다.

### 확정된 사실

- **개발 설계 재승인(user, 2026-08-08)**. revision 3, SHA-256 `a05dd0dee0431c519abf46b6333c6c9b3486d05dd0f7d1d9179c6988f60fd297`.
- StopFailure 훅·`claude respawn`·`claude --bg`·statusline `refreshInterval`은 모두 실재함을 공식 문서와 로컬 CLI로 확인했다.
- 훅 stdin의 실패 유형 필드는 `error_type`이다. 문서화된 유형은 10종이며 봉인본이 알던 6종에 `overloaded`·`model_not_found`·`oauth_org_not_allowed`·`max_output_tokens`가 추가된다.
- **`overloaded`는 rate_limit과 같은 bounded backoff 자동 재시도(user 결정)**. reset 시각 없이 backoff만 사용한다.
- **`model_not_found`·`oauth_org_not_allowed`·`max_output_tokens`는 `needs_user` 안전 중단(user 결정)**.
- statusline stdin의 `rate_limits.*.resets_at`은 Unix epoch 초이며 저장 시 ISO 8601 문자열로 정규화한다.
- `statusLine`은 settings.json 최상위 키다. 재시도 상한은 6회이며 초과 시 `needs_user`.
- **스코프 충돌 해소(user 결정)**: implementer 계약의 `.claude/**` 스테이징 금지와 RADIO 허용 경로가 충돌하며, 이 task에 한해 RADIO가 우선한다 — worker가 RADIO 코드펜스의 `.claude` 경로를 스테이징한다.

### 구현이 실제 계약과 어긋난 발견 (개발 단계 수정 대상)

- 상태 모듈이 존재하지 않는 `error_kind` 필드를 읽어 모든 실패가 `unknown` 처리됨.
- `resets_at` 문자열 가정으로 epoch 초 입력이 전부 null 저장됨.
- `statusLine`이 `hooks` 객체 안에 잘못 등록돼 statusline 미동작.
- respawn 실패 시 상한 없는 30초 재시도, `stop`이 background 세션을 중단하지 않음, 이벤트 멱등 처리 부재, malformed usage 입력이 armed 상태를 덮어씀.

### 다음 행동

1. implementer가 revision 3 기준으로 구현 공백을 TDD로 수정한다.
2. 검증 단계에서 check_ids 3종과 교차 검증을 수행한다.

## 2026-08-08 · revision 4 재봉인 (야간 인수)

- **개발 설계 재승인(user, 2026-08-08)**. revision 4, SHA-256 `57da38f3686f33b8a43f2abbe6c1ab200ffc5446022704faafc64c3263fb7693`. index.jsonl 반영 완료, gate:all 통과.
- 배경: revision 3 봉인·개발 커밋(fc54a1f) 후 사용자가 다른 탭에서 운영 확장을 직접 구현했고, 취침 전 마무리를 조정 세션에 위임하며 확장 편입을 승인했다.
- 편입 내용: ① stop 미결의 A안 확정 — supervisor만 중단, background 세션은 `claude agents` 화면 수동(CLI 2.1.224에 transcript 보존 공식 중단 명령 부재) ② `--watch` 감시 전용 모드와 `/loop-mode` 스킬 ③ 에피소드(직전 실패 1시간 이내) 기준 재시도 6회와 `last_failure_at` 허용 필드 ④ statusline의 사용자 statusline 위임 + loop-mode 표시, refreshInterval 5 ⑤ 변경 허용 경로에 `.claude/skills/loop-mode/**` 추가 ⑥ 인수 조건 5를 새 세션 생성 start 경로로 한정 명시(watch 제외).
- 야간 연속 루프 범위 승인(user, 2026-08-08): P0-T42 done 이후 planned 큐(P2-T03 → T04 → T05) 소진까지 검증·리팩토링 포함 순차 실행. critical·새 결정 필요 시 해당 task만 blocked 안전 중단.
- 다음 행동: implementer 마무리(인수 조건 5 구현·amend 커밋) → 검증(check 3종 + 교차 검증) → done → task 경계 커밋(인터뷰 산출물 + P2-T05 봉인) → P2-T03 디스패치.
