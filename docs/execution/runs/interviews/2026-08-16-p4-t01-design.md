# P4-T01 설계 인터뷰 handoff

- 작업 식별자: P4-T01 (알림과 outbox 기반)
- 현재 단계: 설계 승인 종료 → 개발(implementer 디스패치)
- 기준 시각: 2026-08-16
- 설계 승인: user, 2026-08-16 (RADIO revision 1 — SHA는 index.jsonl `development_approval`이 정본)

## 설계 요지 (정본은 RADIO)

- 마이그레이션 1개: 스키마 3종(notifications·notification_outbox·push_subscriptions,
  멱등성 unique 4요소) + 읽음 RPC 2종(definer, 본인 행만, 재읽음 시각 불변) +
  confirm_schedule 3번째 재정의(정식 배정자 ∪ 교육생 알림·outbox 결합, on conflict
  do nothing, 기존 계약 무수정).
- 화면: 알림함 page·탭 배지 mock을 실데이터로 교체, NotificationsView에
  onMarkRead·onMarkAllRead 필수 prop(P3-T11 F-02 교훈), notification-group을
  Seoul 달력 계산으로 전환(Intl.DateTimeFormat 관례, 새 의존성 없음).
- 테스트: pgTAP 24번 신설, Seoul 경계 단위(주입 now), e2e 신규 spec(밴드 528~559).
- outbox 소비 semantics(상태 전이·재시도 간격)는 T08, 구독 쓰기는 T02 몫.

## 정찰 근거 (설계 판단의 결정 재료)

- 알림함·탭 배지·클릭 이동이 전부 mock 완성 상태 — 실데이터 전환의 UI 신작 없음.
- 근무자 상세 라우트 `[id]`는 work_date를 받아 target `{screen, date}` 그대로 사용.
- date-fns-tz 미설치, Seoul 계산 관례는 `Intl.DateTimeFormat("en-CA",
  {timeZone:"Asia/Seoul"})` 8곳 — 새 의존성 없이 전환.
- mock 전제 기존 단언 전수: NotificationsView.test.tsx, notification-group 단위,
  tab-navigation.spec.ts:392(예상 급여 알림 클릭), motion-render-budget.spec.ts
  (알림 행 stagger) — 뒤 2개 e2e를 시딩 전환 용도 한정으로 허용 경로에 편입.
- e2e 밴드 마지막 사용 495~526 — 신규 spec은 528~559.

## 사용자 결정

- 설계 단계 신규 결정 없음 — RADIO revision 1 봉인 승인. 설계 보강 1건(확정 알림
  수신자 = 정식 배정자 ∪ 교육생 전원)은 요약에 명시해 함께 승인받았다.
