# P2-T05 설계 인터뷰 handoff

## 2026-08-08 · 설계 승인 완료

- 작업 식별자: P2-T05 (모집 운영 화면과 테스트)
- 현재 단계: 설계 승인 종료 → planned 전환 대기(index는 task 경계에서 반영)
- 기준 시각: 2026-08-08

### 확정된 사실

- **개발 설계 승인(user, 2026-08-08)**. RADIO `docs/execution/radio/P2-T05-radio.md` **revision 2**, SHA-256 `db59b1d46ae007e4841eff79457a33a3278712bd9c320601721e0aed35c4aa1f`. revision 1(`3a597755…`) 승인 직후 KST 판정 근거 표기 오류(DEV-TIME-03·04가 이미 MUST로 존재하는데 후보 규칙으로 기재)를 발견해 revision 2로 정정하고 재승인받았다(user, 2026-08-08). 결정 내용 변경 없음.
- **관리자 신청 현황 진입은 T04 관리 시트 확장**(user 결정) — 봉인된 시트 구조 무수정, 신청 현황 섹션(신청 수 + applied 이름 목록)만 추가.
- **신청 수는 달력 셀 배지 + 시트 양쪽 표시**(user 결정). 이름 목록은 시트 안에만.
- **홈 카드 임박 기준은 KST 오늘 포함 3일(D+0~D+2)**(user 결정 — "3일 이내" 경계를 D+2까지로 확정).
- **카드 선정은 신청 여부 무관 + 상태 표시**(user 결정) — 시간 기준 마감이라 신청자에게도 마감일이 철회 마지막 시점이라는 사실을 반영. 미신청은 신청 유도, applied는 「마감 전까지 변경 가능」 안내.
- **오늘/임박 판정은 KST 서버 계산**(user 결정) — 기존 MUST 규칙 `DEV-TIME-03`(달력·마감·월 경계 Asia/Seoul)·`DEV-TIME-04`(클라이언트 시각 비정본)의 기본 적용을 확인한 결정.
- 조회 경로는 기존 RLS(`applications_select_own_or_admin`·`profiles_select_admin`) 재사용으로 새 결정 없음. T03 이월 항목(신청 조회 인덱스)은 기존 유니크 인덱스가 커버해 추가 인덱스 없음으로 종결.

### 미결 사항

- P2-T03·T04 재봉인 발생 시 시트·달력 전제 재점검(파이프라이닝 안전선).

### 다음 행동

1. **index 반영은 다음 task 경계에서**: P2-T05에 `development_approval`(user, 2026-08-08, revision 2, 위 SHA-256) 기록과 `planned` 전환을 P0-T42 종료 후 커밋한다. 위 해시가 봉인 드리프트 감지 기준이다.
2. 결정 대장에 설계 확정 도메인 결정을 추가한다.
3. P2-T03·T04 구현 완료 후 개발 루프가 P2-T05를 실행한다.
