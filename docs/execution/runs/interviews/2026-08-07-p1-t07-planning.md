# P1-T07 기획 인터뷰 handoff

## 2026-08-07 · 기획 종료

- 작업 식별자: P1-T07 (권한 회귀 테스트)
- 현재 단계: 기획 종료 → 다음 설계
- 기준 시각: 2026-08-07

### 확정된 사실

- **기획 승인(user, 2026-08-07)**. 범위 정본은 `docs/execution/phases/01-identity-and-staff.md`의 P1-T07 절이다. `index.jsonl`의 `product_approval` 기록과 `design_pending` 전환은 파이프라이닝 규칙에 따라 P1-T06 실행 경계에서 커밋한다 — P1-T06 worker가 index를 수정 중이므로 스테이징 충돌을 피한다.

- **매트릭스 범위는 P1 행만** (사용자 결정, 2026-08-07). 본인 프로필, 가능 포지션·시급, 다른 사람 개인정보, 관리자 임명, 휴면·재활성화, 수동 휴면. 모집·배정·출퇴근·리허설 행은 해당 테이블이 없어 검증 불가이며 이후 phase가 자기 행을 확장한다.

- **전수 매트릭스 방식** (사용자 결정, 2026-08-07). 주체×자원 전 조합을 한 파일에서 기계적으로 순회한다. 기능별 pgTAP(04~10)과의 중복을 허용하고 회귀 안전망 역할을 맡는다.

- **사용자 경로 인수 조건은 pgTAP 주체 시뮬 + 기존 E2E 유지로 충족** (사용자 결정, 2026-08-07). 매트릭스별 신규 API 통합 테스트는 만들지 않는다.

- **주체 축은 명시 5종 + anon·dormant·departed** (사용자 결정, 2026-08-07). 팀장 후보는 팀장 포지션 보유만으로 추가 권한이 없음을 회귀로 단언한다 — 당일 팀장 권한은 확정 배정(P3)에서만 파생된다.

- **ARCHITECTURE 매트릭스 시급 행 정합화** (사용자 승인, 2026-08-07). PRD 80행 개정(2026-08-07, P1-T05 기획 — "개인 시급은 관리자와 본인이 수정한다")을 매트릭스가 반영하지 못하고 있었다. 문서 위계(PRD 우선)에 따라 "포지션 읽기·본인 시급 읽기와 수정"으로 갱신했고, DB 함수(`20260807020000_identity_worker_management.sql`의 본인·관리자 허용)와도 일치한다.

### 다시 열지 않은 것

- 매트릭스 각 행의 허용·거부 정책 자체 — ARCHITECTURE 10장이 정본이고 이 task는 검증만 한다.
- "수동 휴면 — 활성 신청·배정이 없을 때 가능" 조건 — 신청·배정 도메인 부재로 현재 공허하게 참. 조건 추가는 P2 이후 해당 도메인 task 소유.
- 테스트가 결함을 드러낼 때의 처리 — WORKFLOW의 기존 규칙(제안 또는 blocked 반환)이 소유하며 새 결정이 아니다.

### 조정자가 판단해 기록한 것

- PRD 316행("개인별 시급은 관리자가 입력하고 근무자는 본인 시급만 본다")은 초기 입력과 조회 공개 범위 서술이라 80행 개정과 모순으로 보지 않았다. 수정 권한의 정본은 80행이다.

### 비목표

P2~P7 행(모집 신청·배정표·출퇴근·리허설·완전 삭제 command) / 수동 휴면의 활성 신청·배정 조건 / 권한 동작 변경과 신규 기능.

### 미결 사항

- pending·rejected 주체를 축에 추가할지는 "비활성 상태 차단 검증 포함" 결정 안의 픽스처 세부라 설계(RADIO)가 정한다.
- `check_ids`(`rls-suite`·`api-authorization-suite`)의 최종 정의와 실행 명령 대응은 설계가 정한다. 신규 API 스위트를 만들지 않기로 했으므로 `api-authorization-suite`의 의미(기존 Server Action 단위 테스트·E2E와의 대응)를 설계에서 확정한다.
- 매트릭스 테스트 파일 구성(단일 파일 vs 자원별 분할, 조합 순회 방식)은 설계가 정한다.

### 다음 행동

- P1-T06 실행 경계에서 index의 `product_approval` 기록과 이 인터뷰 산출물(ARCHITECTURE·phase 문서 포함)을 커밋한다.
- P1-T06 완료 후 또는 실행과 겹쳐 P1-T07 설계 인터뷰를 진행한다. 의존 task는 P1-T05(done)·P1-T06(in_progress)이다.
