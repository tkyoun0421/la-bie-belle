# P3-T08 설계 인터뷰 handoff

- 작업 식별자: P3-T08 (배정·확정 회귀 테스트 — 공백 메우기)
- 현재 단계: 설계(RADIO) 승인 종료 → 다음 개발(implementer)
- 기준 시각: 2026-08-16
- 개발 설계 승인: user, 2026-08-16 (요약본 승인, revision 1 봉인)

## 조정자 설계 결정 (요약본에 포함해 승인됨)

- 네 묶음의 파일 단위 배치: pgTAP 17~23 보강, UI 단위 10종 신설(approval 관례 수준),
  여정 e2e 신설(`confirmation-roster-journey.spec.ts`, 밴드 `confirmationJourney 495~526`),
  e2e 위생 7건(support 2파일 포함).
- 동시 확정(P3-T06 F-04)은 pgTAP 단일 연결 한계로 병렬 실증 불가 — 재호출 LB029·revision
  불변·스냅샷 불변 단언 보강으로 대체 종결, backlog 줄에 사유 병기. 복사 동시성(P3-T02)은
  선삽입 후 복사 호출로 on-conflict 분기 실증.
- UI 컴포넌트 코드 무수정 — 테스트 불가 구조 발견 시 정지 조건.
- e2e `insertSchedule` status 유니언 4상태 확장(전이 트리거는 update만 다뤄 직접 insert
  가능 전제, 막히면 정지 조건).

## 코드 대조에서 확인된 사실

- 감사 actor 단언 관례: 15번 pgTAP `results_eq`. P3 파일(17~23)은 actor 단언 0건.
- UI 단위 관례: `ApprovalActionButtons.test.tsx`(testing-library + userEvent).
- fsd.json: ui 세그먼트 unitTest exempt·`verifiedBy ["component","e2e"]` — 컴포넌트
  테스트 추가가 관례 경로.
- e2e 밴드 다음 빈 구간 495부터.

## 봉인

- RADIO: `docs/execution/radio/P3-T08-radio.md` revision 1, Approved.
- SHA-256과 `development_approval`은 `index.jsonl`이 정본이다.

## 다음 행동

1. P3-T08 `in_progress` 전환과 implementer 디스패치.
2. 개발 완료 후 검증 — check IDs: verify, assignment-confirmation-e2e.
