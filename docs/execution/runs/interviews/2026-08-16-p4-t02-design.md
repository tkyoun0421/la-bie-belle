# P4-T02 설계(RADIO) 인터뷰 기록

- 작업 식별자: P4-T02 (PWA 설치와 Web Push 구독)
- 현재 단계: 설계 승인 종료 → 개발 착수
- 기준 시각: 2026-08-16
- 설계 승인: user, 2026-08-16 (RADIO revision 1 요약 승인, `index.jsonl`
  `development_approval`에 SHA-256 봉인)

## 진행 방식

기획 handoff의 정찰 대상을 코드·문서 대조로 모두 해소해 사용자 추가 질문 없이
요약 승인 한 번으로 닫았다. 기획 승인(2026-08-16)이 소유한 제품 결정(진입점
두 곳, dev 스크립트, P7-T06 이월)은 다시 열지 않았다.

## 정찰 결과 (RADIO 전제로 봉인)

- Service Worker 등록: Next.js 16 가이드
  (`node_modules/next/dist/docs/01-app/02-guides/progressive-web-apps.md`)의
  번들 방식 — `navigator.serviceWorker.register(new URL(<워커 파일>,
  import.meta.url), { scope: "/", updateViaCache: "none" })`. `next.config`
  headers 변경 불필요(필요해지면 정지 조건).
- SW 엔트리 위치: `src/features/push/lib/push-service-worker.ts`. app 계층에
  두면 appLayer unitTest required(exemptFiles는 page 계열뿐)에 걸리는 것은
  같고 features 훅이 app 파일을 상향 참조하게 되므로, 같은 슬라이스 lib에
  둔다. 배선 파일도 `self` 스텁 단위 테스트 대상.
- env·manifest: P0에 준비 완료 — `.env.example`의 VAPID 3종,
  `src/shared/model/env.ts`의 public 키 필수 검증, `src/app/manifest.ts`.
  이 task의 env 변경 없음.
- 쓰기 RPC: `save_push_subscription`(endpoint upsert — disabled 해제·소유자
  재지정) + `remove_push_subscription`(본인 행 disabled_at, 삭제 없음).
  테이블·RLS 무수정.
- 기존 단언 대조(봉인 체크리스트): `tests/e2e/tab-navigation.spec.ts`는
  더보기 목록 단언 없음 — 무수정. `src/views/more/ui/__tests__/
  MoreView.test.tsx`가 링크 개수 고정 단언(`toHaveLength(2)`) — 2→3 갱신을
  허용 경로·용도 한정에 포함.
- e2e 밴드: notifications가 528~559 사용 중 — 사전 안내 시나리오는 561~592.
- `shared/ui`에 토글 없음 — button·bottom-sheet 재사용, 신설 없음(신설
  필요해지면 정지 조건).

## 설계 판단 (RADIO가 정본)

- 구독 상태 정본은 브라우저 `pushManager`, DB는 발송 대상 목록 — 어긋나면
  화면은 꺼짐 표시, 다음 켜기의 upsert로 수렴.
- `pushManager.subscribe` 실호출은 자동 테스트에서 배제(CI 푸시 서비스
  왕복 불안정) — 훅 단위는 모킹, e2e는 시트·안내·화면 분기까지, 실수신은
  체크리스트 문서 몫.
- 푸시 페이로드 계약 `{ title, body, target }`은 P4-T01 notifications 행과
  동일 모양 — T03·T08이 재사용.

## 봉인 정보

- RADIO: `docs/execution/radio/P4-T02-radio.md` revision 1
- SHA-256: `147ea02ef9d2b14a25be2ae64a388c4bb246cf6f3cdbbc46658635d34fbba759`
- index: `status: planned`, `radio_ref`, `development_approval` 기록

## 재봉인 (revision 2, 2026-08-16)

implementer가 정지 조건 1 접촉으로 반환 — Turbopack이 `new URL(<파일>,
import.meta.url)` SW 참조를 번들 없이 정적 자산 원본 복사로 처리해(MIME
`video/mp2t`, `@/` 별칭·TypeScript 미변환) revision 1의 번들 등록이
프로덕션 빌드에서 성립하지 않음을 확인했다. 선택지 4개(번들 로더, serwist,
public 독립 스크립트, Route Handler) 중 사용자가 **public 독립 SW** 승인:
`public/push-service-worker.js` classic script, 훅은
`register("/push-service-worker.js", { scope: "/", updateViaCache: "none" })`,
`model/push-display`·`lib` 배선 폐지, 단위 테스트는
`src/features/push/lib/__tests__/`에서 실파일 직접 import. 루트 서빙이라
`Service-Worker-Allowed` 헤더 문제도 없고 next.config·의존성 무추가.

- revision 2 SHA-256:
  `d69380dab93e5697ef1804f661489a5478b2c47666908f5ab17e2410345937c5`

## 재봉인 2차 (revision 3, 2026-08-16)

교차 검증이 high 2건을 확정해 사용자 승인으로 다시 재봉인했다.

- F-01 유령 구독: 켜기 도중 저장 실패 시 브라우저 구독 보상 해지를 불변
  규칙에 추가.
- F-02 계정 전환 미재귀속: 브라우저 구독이 존재하는 마운트에서 재귀속
  upsert 1왕복을 허용 — Optimizations 「설정 화면·시트 서버 왕복 0」을 이
  경우에 한해 갱신. 인수 조건 6에 두 동작의 훅 단위 회귀 테스트 추가.
- revision 3 SHA-256:
  `7f232a1cf8d28ec19bdbaf217e28ff4c18d2fe2b331baa737e4721d308cf2598`

수정 라운드는 dcd49f8으로 완료(RED→GREEN 신규 1쌍, push 슬라이스 37/37).

## 다음 단계

마감 — index done 전환, 검증 기록(review 75, backlog 11건) 봉합. push는
병렬 디자인 세션이 ScheduleView가 참조하는 미커밋 모듈 3종을 커밋할
때까지 보류(사용자 결정, 2026-08-16).
