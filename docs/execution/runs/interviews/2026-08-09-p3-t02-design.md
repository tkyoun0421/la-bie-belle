# P3-T02 설계 인터뷰 handoff

- 작업 식별자: P3-T02 (포지션 기본 설정과 스케줄 필요 인원)
- 현재 단계: 설계 승인 종료 → 다음 개발
- 기준 시각: 2026-08-09

## 확정된 사실

- **개발 설계 승인(user, 2026-08-09)**. revision 1, SHA-256
  `fd9bfbb75b97af146c529875bf00a9923ca1cb920de1f1cdb2b1847452b455be`.
- 승인 방식: 설계 요약본을 별도 메시지로 전달하고 "승인 작업해"로 확인받았다.
- 설계 인터뷰 확정 2건(AskUserQuestion):
  - 새 포지션의 확정 전 스케줄 강제 반영은 **DB 트리거**가 실행한다("DB 트리거 (Recommended)" 선택).
  - 확정 스케줄 안내는 이번에 **준비 화면 배너+추가 버튼까지**("준비 화면 안내까지 (Recommended)" 선택).
    포지션 추가 시점 목록 모달과 확정 스케줄 수동 추가 개방(revision 연동)은 P3-T06 소유.
- 요약본으로 함께 승인된 설계 골격: `schedule_position_requirements` 테이블(인원값만 저장,
  구조는 positions FK 참조), 복사 함수 멱등·동시성, 수정 함수의 CONFIRMED·CANCELLED 거부,
  삭제 판정 FK restrict → `SCHEDULING_POSITION_IN_USE` 안내, `/admin/positions` 신설 +
  준비 화면 확장(기존 예식 흐름 무수정 용도 한정), 감사 전후 값 병기.
- P3-T01 교차 검증 교훈을 봉인에 선반영: E2E 픽스처 재실행 내성(F-04), 감사 전후 값(F-05),
  04번 default-deny 정책 수 단언 갱신 허용+기록(F-08).

## 미결 사항

- 없음. RADIO 미결 절의 확정 스케줄 개방·모달은 P3-T06 인터뷰 소유로 명시했다.

## 다음 행동

1. index `planned` 전환 + 이 기록 커밋(in_progress 0 상태라 gate:scope 무제한).
2. implementer 디스패치 — planned → in_progress 전환 후 TDD 구현.
