# P2-T05 재봉인 인터뷰 handoff

## 2026-08-08 · 설계 재봉인 (revision 3, 아침 승인)

- 작업 식별자: P2-T05 (모집 운영 화면과 테스트)
- 현재 단계: 설계 재승인 종료 → 다음 개발 재개
- 기준 시각: 2026-08-08 아침

### 배경

야간 자동 진행 중 implementer가 코드 0줄 작성 상태에서 스코프 갭을 발견하고 blocked로
반환했다(`docs/execution/runs/P2-T05/decision-signal.json`). 기술 인수 조건 1(관리자 모집
달력 셀의 applied 신청 수 배지 — 2026-08-08 설계 인터뷰에서 확정된 제품 결정)은 날짜 셀을
전담 렌더하는 `src/shared/ui/calendar.tsx`를 확장해야만 구현 가능한데, 이 파일이 변경 허용
경로에 없었다. DayPicker 내부 구조에 의존하는 오버레이 우회는 검토 후 기각됐다.

### 확정된 사실

- **개발 설계 재승인(user, 2026-08-08)**. revision 3, SHA-256
  `1feb1840a59458656e9dc011115618f3f8a19f746a4d7c496e3dffc245685e98`.
- 승인 방식: 아침 보고의 AskUserQuestion에서 "재봉인 — calendar.tsx 한정 (Recommended)" 선택.
  세 선택지(파일 한정 재봉인 / shared/ui 전체 개방 / 배지만 후속 task 분리) 중 첫 번째다.
- 편입 내용: 변경 허용 경로에 `src/shared/ui/calendar.tsx` 한 파일 추가. Architecture에
  Calendar 최소 확장을 명시 — `dateStates` 항목의 선택적 신청 수 필드, 값이 있을 때만
  `CalendarDayButton`이 숫자 배지 렌더, 기존 유니온·공개 prop·소비처 무수정(하위 호환).
- 그 외 봉인 결정 변경 없음. product_approval은 재승인 불요 — 제품 범위(배지+시트 양쪽 표시)는
  그대로이고 구현 경로만 보정했다.
- `ui` 세그먼트는 unit 테스트 면제(component·e2e 검증)이며
  `src/shared/ui/__tests__/calendar.test.tsx`가 존재해 tdd-guard 차단이 없음을 확인했다.
- index 반영과 status `blocked` → `planned` 전환은 P0-T42 done 경계 커밋에서 수행한다
  (in_progress 1개 규칙 — P0-T42 검증이 먼저 끝나야 P2-T05를 착수한다).

### 다음 행동

1. P0-T42 검증 종료·done 후 경계 커밋: index `development_approval` revision 3 갱신 + status
   `planned` 전환 + 이 기록.
2. P2-T05 implementer 재개(직전 blocked 반환 지점부터 — 코드 산출물 없음, 설계 분석은
   decision-signal의 confirmed 절 참조).
