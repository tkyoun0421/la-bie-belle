# P3-T02 재봉인 인터뷰 handoff

## 2026-08-09 · 설계 재봉인 (revision 2, 허브 파일 편입)

- 작업 식별자: P3-T02 (포지션 기본 설정과 스케줄 필요 인원)
- 현재 단계: 설계 재승인 종료 → 개발 재개
- 기준 시각: 2026-08-09

### 배경

implementer가 착수 직후 코드 0줄 상태에서 허용 경로 갭을 발견하고 blocked로 반환했다
(`docs/execution/runs/P3-T02/decision-signal.json`, 커밋 9986d02). RADIO 범위 ④가 커밋한
admin 허브 진입 링크의 실제 위치는 `src/app/(protected)/admin/page.tsx`(기존 4개 링크
인라인 나열, `views/admin/` 컴포넌트 분리 없음)인데, 허용 경로 코드펜스가 이 파일을
커버하지 않았다 — RADIO 작성 시 조정자가 허브를 `src/views/admin/**`으로 가정한 오기다.

### 확정된 사실

- **개발 설계 재승인(user, 2026-08-09)**. revision 2, SHA-256
  `7f9601daba3ef32c81c670a962509c61bafd16d7dbc7a4a2eb18c581e1613ff2`.
- 승인 방식: AskUserQuestion에서 "재봉인 — 허브 파일 추가 (Recommended)" 선택.
  선택지(허브 파일 추가 / 링크 제외 후속 이관) 중 첫 번째다.
- 정정 내용: 허용 경로에 `src/app/(protected)/admin/page.tsx` 한 파일 추가(용도는 포지션
  관리 진입 링크 1개 추가 한정), Architecture 표기를 실제 위치로 정정. 이미 승인된 범위의
  순수 이행 경로라 설계 실질 무변경(P1-T04 rev2·P2-T05 rev3 전례).
- 제품 범위 변경 없음 — product_approval 재승인 불요.
- index 반영: `development_approval` revision 2 갱신. status는 blocked 유지로 커밋하고
  implementer가 재개 시 `in_progress`로 전환한다.

### 다음 행동

1. implementer 재개: blocked → in_progress 전환, handoff의 구현 순서 메모대로 TDD 착수.

## 2026-08-09 · 설계 재봉인 (revision 3, 확정 스케줄 복사 제외)

- 작업 식별자: P3-T02
- 현재 단계: 설계 재승인 종료 → 검증 수정 라운드
- 기준 시각: 2026-08-09

### 배경

개발 커밋 1357c87의 교차 검증(opus·codex)에서 결과 모순이 확정됐다 — revision 2의 인수
조건 2가 복사의 거부 대상을 CANCELLED로만 적어, 표가 없는 CONFIRMED 스케줄도 준비 화면
진입만으로 그 시점 활성 포지션 전부가 기본 인원수로 생성된다. 그런데 set·remove 함수는
CONFIRMED를 거부하므로 그렇게 생긴 표는 P3-T06 전까지 한 줄도 고칠 수 없고, 확정 후 추가된
포지션까지 포함돼 PRD 7장의 "확정 스케줄은 자동으로 바뀌지 않되"와 결과가 어긋난다.
codex는 "설계 재봉인이 필요한 실질 결함", opus는 "봉인 문구 안의 선택이라 범위 밖 개선"으로
판단이 갈렸고, 봉인 문구와 제품 규칙의 충돌이라 사용자 결정을 받았다.

### 확정된 사실

- **개발 설계 재승인(user, 2026-08-09)**. revision 3, SHA-256
  `7d55892eb28aacc4db7e626eb749ec2e78f11a096711fa0afe0ca0b7abb5793f`.
- 승인 방식: AskUserQuestion에서 "재봉인 — 확정은 복사도 안 함 (Recommended)" 선택.
  선택지(재봉인 / 현행 유지 후 T06 이관) 중 첫 번째다.
- 정정 내용: 불변 규칙 "복사는 멱등이다"를 "복사는 멱등이며 확정·취소 스케줄을 제외한다"로
  확장하고, 인수 조건 2의 거부 대상과 위험 표 2행 주요 실패 칸에 CONFIRMED를 추가했다.
  나머지 설계는 무변경.
- 제품 범위 변경 없음 — PRD 7장의 승인된 규칙과 결과를 일치시키는 정정이라 product_approval
  재승인 불요.
- 같은 AskUserQuestion에서 **수정 라운드 범위**도 승인됐다: "high + 봉인·PRD 위반 3건" —
  F-01(admin 직접 DML 우회, high), F-02(시스템 포지션 개명 UI 차단), F-03(조회 실패가
  인원값 덮어쓰기 유도), F-04(새 테이블 비관리자 거부 pgTAP 부재). 나머지 medium 3건과
  low 3건은 backlog로 보낸다.
- index 반영: `development_approval` revision 3 갱신. status는 blocked로 커밋하고
  implementer가 수정 라운드 재개 시 `in_progress`로 전환한다.

### 다음 행동

1. implementer 재개: blocked → in_progress 전환, 확정 발견 F-01~F-04 + revision 3 반영
   (복사 함수의 CONFIRMED 거부 + pgTAP) 수정 라운드.
2. 수정 후 리뷰어 재확인 → 결과 파일·backlog 기록 → done → ci-finisher push.
