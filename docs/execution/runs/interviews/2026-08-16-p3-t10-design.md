# P3-T10 설계 인터뷰 handoff

- 작업 식별자: P3-T10 (e2e work_date 밴드 정리)
- 현재 단계: 설계(RADIO) 승인 종료 → 다음 개발(implementer)
- 기준 시각: 2026-08-16
- 개발 설계 승인: user, 2026-08-16 (요약본 승인, revision 1 봉인)

## 조정자 설계 결정 (요약본에 포함해 승인됨)

- 밴드 2개 신설: `recruitmentManage`(429~460)·`recruitmentBulkOpen`(462~493) — 기존 최댓값
  427 뒤 비겹침, 폭 32개월 관례.
- 같은 달 다중 날짜 헬퍼 `workDatesInSameMonth(band, count)` 신설(2~27일·중복 없음·오름차순,
  마감일은 그 달 1일). recruitment 두 spec의 같은 달 구조 보존용.
- 독립 추첨 3 spec은 `position-requirements.spec.ts` 관례(모듈 레벨 `workDatesInBand(밴드, 2)`)로
  통일. post-confirmation-changes의 무효 정리 블록 2곳 제거.
- 검증: `test_mode: verification` — db reset 없이 다섯 spec 연속 2회 실행 GREEN이 핵심 증거,
  `pnpm verify` 전체 GREEN, backlog 3건(P3-T06 F-06·P3-T07 F-02·P3-T09 F-03) 완료 체크.

## 코드 대조에서 확인된 사실

- 두 recruitment spec 모두 `?month=YYYY-MM` URL 직접 진입 — 원거리 밴드 이전에 달 탐색
  장애물 없음.
- recruitment-manage: 고정 날짜 12·18일+마감 1일(현재 월+2). recruitment-open 테스트 1:
  고정 날짜 10·2·3일(현재 월+1), 마감은 선택일 중 최소.
- 독립 추첨 3 spec은 각각 `workDateInBand` 2회 호출. post-confirmation-changes 정리 블록의
  `schedules` 삭제는 append-only 트리거에 거부되는데 오류 무시(105~111·241~245행).

## 봉인

- RADIO: `docs/execution/radio/P3-T10-radio.md` revision 1, Approved.
- SHA-256과 `development_approval`은 `index.jsonl`이 정본이다.

## 다음 행동

1. P3-T10 `in_progress` 전환과 implementer 디스패치.
2. 개발 완료 후 검증 — check ID: recruitment-e2e-band.
