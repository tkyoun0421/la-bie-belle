# P0-T38 기획 인터뷰 handoff

## 2026-08-05 · 기획 종료

- 작업 식별자: P0-T38 (import 표기와 테스트 배치 규칙)
- 현재 단계: 기획 종료 → 다음 설계
- 기준 시각: 2026-08-05

### 확정된 사실

- **기획 승인(user, 2026-08-05)**. `docs/execution/phases/index.jsonl`의 P0-T38이 `design_pending`이고 `product_approval`이 기록됐다. 범위 정본은 `docs/execution/phases/00-foundation.md`의 P0-T38 절이다.

- **발단 2건이 하나의 task로 묶였다.** P0-T04 진행 중 사용자가 ① `src/`의 상대경로 import를 lint로 막고 alias만 쓰게 할 것, ② colocated 테스트(소스 옆 `*.test.*`)를 별도 폴더로 분리할 것을 요청했다. 둘 다 "표기·배치의 정본을 정하고 기계로 강제한다"는 같은 성격이라 P0-T38로 등록했다. P0-T04가 `in_progress`인 동안은 `gate:scope`가 등록 커밋을 막아(봉인된 변경 허용 경로 밖) P0-T04 `done` 직후로 등록을 미뤘다.

- **import 표기: 전면 alias** (사용자 결정, 2026-08-05). `src/` 전체에서 `./`·`../` 상대 import를 금지하고 `@/`만 허용한다. 조정자는 "`../` 금지, 같은 폴더 `./` 허용" 안을 제시했으나 사용자가 표기 1종 통일을 택했다. 동적 `import()`도 검사 대상이다(현행 위반 중 1건이 동적 import).

- **테스트 배치: 세그먼트 `__tests__/` 하위 폴더** (사용자 결정, 2026-08-05). tdd-guard(`.claude/hooks/tdd-guard.sh`의 배치 후보 5종)와 vitest(`src/**` glob) 모두 현행 구성이 이미 지원해 도구 변경이 0건이고, `tools/eslint-plugin-project/rules/__tests__/` 관행과도 일치한다. 루트 `tests/` 미러 안은 vitest·tsconfig 개편 비용과 tdd-guard 정밀도 하락으로 제외했다.

- **기존 위반 일괄 수정 포함** (사용자 결정). 착수 시점 현황(조정자 집계): colocated 테스트 9건, 그중 상대 import 사용 9건(정적 8·동적 1). `src/` 비테스트 소스의 상대 import는 0건 — P0-T04 소스 코드는 이미 전부 alias를 쓴다.

### 다시 열지 않은 것

- **레이어 방향 규칙** — `layer-direction` 룰이 이미 소유한다. P0-T38은 방향이 아니라 표기만 다룬다.
- **오류 코드 리터럴 차단** — `error-code-literal` 룰(P0-T37)이 소유한다. 룰 구현 관행(RuleTester 테스트, `lib/resolve-path.mjs` 경로 판별)은 P0-T38이 따를 선례다.

### 조정자가 판단해 기록한 것

- **적용 범위는 `src/`만.** tools/·harness/·scripts/는 각자의 관행(상대 import)을 유지한다. 현행 `pnpm lint`가 `src/`만 보는 구조와 일치한다.
- **E2E 테스트는 대상 밖.** 루트 `tests/`는 Playwright 소유 그대로다.

### 미결 사항

- `eslint --fix` 자동 변환 fixer 구현 여부 — 설계(RADIO)가 정한다.
- DEV-* 규칙 ID 신설 여부와 위치 — 설계가 정한다. 신설 시 기존 ID 전수 확인을 선행한다(P0-T37의 DEV-ERR-03 충돌 재발 방지).
- `test_mode`와 `check_ids`는 설계 인계 시점에 기록한다. lint 룰 task라 P0-T37 선례(`tdd`, RuleTester)를 따를 가능성이 크다.

### 다음 행동

1. P0-T38 설계 인터뷰(RADIO)를 진행한다. 룰 2종의 구현 방식·fixer 여부·DEV-* ID·기존 테스트 이동 절차가 결정 대상이다.
2. RADIO 승인·봉인 후 개발 루프의 실행 후보가 된다.
