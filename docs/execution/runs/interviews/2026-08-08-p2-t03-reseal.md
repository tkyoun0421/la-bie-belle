# P2-T03 재봉인 기록 (revision 2)

## 2026-08-08 · 허용 경로 누락 보정

- 작업 식별자: P2-T03 (근무자 달력과 다중 신청)
- 계기: 개발 단계 중 implementer가 [질문]으로 반환 — RADIO가 요구한 schedule 페이지 서버 컴포넌트 전환의 필연적 파급(`src/app/preview/page.dev.tsx`의 `ScheduleView` 호출 2곳에서 사라진 콜백 prop 두 줄 삭제)이 변경 허용 경로 밖이라 `gate:scope`가 커밋을 막았다.
- **재봉인 승인(user, 2026-08-08, 파일 한정)**. revision 2, SHA-256 `2dcb8ff80901b7bb3891705e301d39236ec0b63239fa5c3b92231a8b8405fd48`. 변경은 허용 경로에 `src/app/preview/page.dev.tsx` 한 파일 추가뿐이며 결정 내용(설계)은 무변경. `src/app/preview/**` 전체 개방안은 봉인 범위가 넓어져 기각.
- index `development_approval` revision 2 반영, P2-T03 `blocked → in_progress` 복귀. 위 해시가 봉인 드리프트 감지 기준이다.

## 다음 행동

1. implementer 재개 — 스테이징(재봉인된 RADIO·index 포함)·`pnpm verify`·커밋.
2. 커밋 후 검증 단계(교차 리뷰) 디스패치.
