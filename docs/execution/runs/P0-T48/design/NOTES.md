# P0-T48 디자인 라운드 기록

한 라운드에 화면 하나를 정한다. 앞 라운드에서 정한 규칙은 다음 화면에 그대로 적용하고, 어떤 화면에서 다르게 가기로 하면 **그 자리에 근거를 남긴다.** 시안 파일(`proposal.html`)은 라운드마다 덮어쓴다. 되돌아볼 근거는 파일 더미가 아니라 이 기록이 갖는다.

**확정된 화면은 `design/confirmed/<화면>.html`에 세운다.** `proposal.html`이 다음 화면 라운드에 덮이므로, 옮겨두지 않으면 코드로 옮길 계약이 git 이력 안에만 남는다. 지금 있는 것 — `confirmed/home.html`(라운드 27 확정본, `ee44b40`에서 꺼냈다). 규칙의 정본은 [`/publish-ui` 스킬](../../../../../.claude/skills/publish-ui/SKILL.md)이다.

## 확정된 규칙

여기 있는 것은 다음 화면에 자동으로 적용된다.

### 시각 언어 (라운드 1, 2026-08-17)

- 지면 `#f1f3f6`, 카드 흰색, 그림자 없음. 구획은 배경색 차이로만 나눈다.
  라운드 13 예외 — **한 카드 안에서 행을 나누는 hairline `#e5e8eb`는 허용한다.** 행 어법(아래
  「행」 절)이 선 없이는 성립하지 않아서다. 카드 사이에는 여전히 선을 쓰지 않는다.
- 카드 모서리 `radius-xl` 20px. 화면 좌우 여백 16px, 카드 사이 12px, 카드 안쪽 16px.
- 화면에서 가장 강한 파랑(`#0052ff`)은 지금 눌러야 할 버튼 하나다.
- 상태는 채움이 진할수록 확정에 가깝다. 모집 중은 색 없음, 신청함 `#f2f6fd`, 확정 `#e4edfd` + `#0c3f9c` 글자.
- 달력 셀에는 숫자만 둔다. 배지·점·범례를 넣지 않는다. 오늘은 숫자 아래 밑줄 2px.

### 타이포 위계 (라운드 2, 2026-08-17)

- 화면 제목 22/30, 금액 26/34, 카드 제목 18/26, 값 16/24, 메타 13/18, 버튼 14/20.
- 스케일 8단계는 건드리지 않는다. 바꾼 것은 어느 자리에 어느 단계를 쓰는지뿐이다.
- 결과로 `display` 32/40을 쓰는 자리가 없어졌다. 이 단계를 표에서 뺄지는 미결.
- 카운트다운은 금액과 같은 26/34에 `tabular-nums`를 쓴다(라운드 8). 새 단계를 만들지 않았다.
- **라운드 19(2026-08-17)가 무게를 감량했다** — 화면 제목 600 · 카운트다운 600 · 행 값 500 ·
  블록 이름표 500 · D 배지 600. 700은 화면에서 사라졌다. 사용자: "Bold를 많이 쓰는 경향 주의".

### 아이콘 (라운드 3, 라운드 5에서 개정)

- 기본은 Lucide 라인이다. 기본 24px, 보조 20px, 터치 영역 44×44px.
- **하단 탭의 선택 상태만 예외로 채운다.** 채움 색은 `gray-800`, 선택 안 된 탭은 `gray-700` 선이다.
  라운드 3은 "채운 아이콘은 쓰지 않는다, 선택은 굵기로"였는데 라운드 5에서 뒤집었다.
  근거: 선택 탭을 action 색에서 떼어내면 화면의 파랑이 눌러야 할 버튼 하나에만 남아
  라운드 1의 규칙이 오히려 선명해진다. 굵기 2 → 2.25 차이는 24px에서 거의 읽히지 않기도 했다.
- 대가가 둘 있다. Lucide에 채운 세트가 없어 **채움은 실루엣에서 딴 파생 자산**이 되고,
  `전체`의 `Menu`는 선 세 개라 채울 도형이 없어 **`LayoutGrid`로 교체**했다(선 상태는 Lucide 정품).

### 앱 셸 (라운드 4·5, 2026-08-17)

- 하단 탭은 `홈 · 일정 · 급여 · 전체` 넷이다. 알림은 헤더 오른쪽 종이고 안 읽음은 점으로 알린다.
- 예상 급여를 `전체` 서랍에서 꺼내 탭으로 올렸다. 근무자가 이 앱을 쓰는 이유인데 두 번 눌러야
  나왔다(`AppShellTabBar.tsx:15-17`이 `/pay`를 `more` 탭 활성으로 묶고 있었다).
- 헤더는 모든 화면이 같은 모양이다 — 왼쪽 화면 제목, 오른쪽 종.
- **헤더는 지면 위에 떠 있고 뒤가 비친다**(라운드 25). 스크롤을 내리면 카드가 헤더 뒤로
  지나가며 흐릿하게 보인다 — `backdrop-filter: blur(9px)` + 지면색 **50%** 배경(2차에서 14px ·
  68%를 옅게 풀었다, 사용자). 상태 표시줄 자리(24)와 헤더(56)를 **한 레이어 80으로 묶어**
  실제 폰처럼 콘텐츠가 시계 자리 뒤까지 지나간다. 지면과 같은 색을 쓰므로 맨 위에서는 경계가
  보이지 않는다 — 선을 긋지 않는다.
- **선택 표시는 셋뿐이다** — 선에서 채움으로, 색 `gray-700` → `gray-800`(#3a3f4a), 그리고 누르는 순간의 눌림.
  선택 탭 뒤에 회색 블록을 깔아봤다가 뺐다. 선택돼 있는 내내 남아 채움과 색이 이미 하는 말을
  한 번 더 했다.
- **눌림 모션** — 두 번 눌린다. 크게 한 번(`scale(1.22, 0.78)`) 눌려 양옆이 퍼졌다 되튀어
  (`0.95, 1.06`) 돌아오고, 반동으로 한 번 더 작게(`1.1, 0.91`) 눌린 뒤 제자리. 420ms,
  기준점은 아이콘 바닥, `transform`만 움직인다.
- 아이콘·글씨는 탭 칸 안에서 가운데 정렬이고 간격은 3px다. 라벨은 `<span>`으로 감싼다 —
  맨 텍스트 노드로 두면 위치 지정된 형제에게 덮인다.

### 금액 가림 (라운드 3·5, 라운드 18에서 개정)

- **내 근무가 있는 날에는 금액이 자동으로 가려진다.** 근무 없는 날은 보인 채로 시작한다.
  라운드 3의 「기본이 가림 + 눈 아이콘」을 라운드 18이 뒤집었다(2026-08-17 사용자 승인) —
  가리는 이유가 「근무 중 동료 옆 노출」이라, 기준도 토·일(첫 지시)이 아니라 근무일로 갔다.
- **눈 아이콘은 없다. 금액 자체를 누르면 화면 전체 금액이 열린다** — 「스위치는 앱 전체 하나」는
  유지된다(홈과 예상 급여 화면이 같은 상태를 본다). **화면을 벗어나면 도로 가려진다.**
- 가려진 동안 실제 값을 렌더하지 않는다. 고정 폭 더미를 **blur 7px**로 깔고 `aria-hidden`을 준다.
  블러만 걸면 글자 폭이 자릿수를 드러내고 화면 낭독기가 값을 그대로 읽는다.
  라운드 22에서 숨김 블러를 7 → 5 → 3.5 → 2.5로 세 번 줄였다가 **오해로 판명되어 7px 원복** —
  사용자의 "블러 약하게"는 숨김이 아니라 **리빌 블러**를 가리킨 것이었다(2026-08-17 정정).
- **가려진 동안에는 애니메이션이 없다**(라운드 22) — 눌림 스케일도 없다.
- **리빌 모션** — 손으로 열면 **블러는 즉시 풀리고**, 옅은 회백(#b7bec9) 빛이 좁은 띠로 숫자를
  왼쪽에서 오른쪽으로 은은하게 훑는다(1.5초, `background-clip: text` 그라디언트 스윕).
  **손으로 열 때만** 나온다 — 화면 복귀·상태 전환으로 열릴 땐 없다. 조정 이력: 파랑 0.7초 →
  흰색 1.1초 → 1.5초 → 블러 페이드 추가(오해) → **즉시 풀림 + 빛 강도 낮춤**(전부 2026-08-17
  사용자). **빛이 도는 중에 다시 가려지면 훑기는 즉시 사라진다**(라운드 23) — 가림 적용이
  `revealing`을 걷는다.
- 「화면 설정」은 **「근무일 자동 가림」 켜고 끄기**를 소유한다.
- 최종 저장 위치는 **계정**이다. 다만 그건 `profiles` 컬럼·RLS·서버 왕복이 붙어 이 task의
  「서버·데이터 변경 안 함」 비목표 밖이라, P0-T48은 화면과 스위치를 기기 저장으로 붙이고
  계정 저장은 별도 task가 저장 계층만 갈아끼운다. UI는 그대로 살아남는다. (라운드 5 결정 유지)

### 홈의 구조 (라운드 6, 라운드 12~16에서 개정)

- 홈은 **위에서 아래로 블록 다섯**이다 — `알림 한 장` · `오늘 출퇴근` · `이번 주` ·
  `다가오는 근무` · `급여`. 블록 자리는 항상 같고 안의 내용만 상태에 따라 바뀐다.
  라운드 6은 넷(`알림 · 오늘 · 이번 달 일정 · 급여`)이었는데, 라운드 14~16에서 콘텐츠를
  늘리면서 `이번 주`가 들어오고 `이번 달 일정`이 `다가오는 근무`로 바뀌었다.
- **카드는 주제, 행은 항목이다**(라운드 13, 토스 홈 참조). 카드 하나가 항목 하나였던
  라운드 6~11의 구조를 뒤집었다. 좌우 병치 카드는 **출근 카드 하나에만** 남는다 — 홈에서 큰
  숫자를 세는 자리가 하나면 그 자리가 곧 「지금 할 일」이라는 뜻이 된다.
- **우선순위 하나만 노출하는 방식을 버렸다.** `home-priority.ts`의
  `attendance → deadline-application → confirmation-change → next-shift → empty`는 넷이 동시에
  살아 있는 날 셋을 숨긴다. 마감을 놓치는 대가가 초점을 지키는 이득보다 크다. (라운드 6, 유지)
- **홈은 스크롤한다.** 라운드 6의 「홈은 스크롤하지 않는다」는 라운드 16에서 폐기했다(사용자
  승인) — 휑한 홈보다 콘텐츠를 늘리는 쪽을 택했다. 최상단을 넘어 당기면 새로고침은 그대로
  남는다(기존 `RouterPullToRefresh`).
- 데이터가 없는 블록은 사라지지 않고 **없다고 말한다.** **예외는 둘이다** — 알림 블록(라운드 6)과
  **오늘 블록**(라운드 29). 둘 다 통째로 접히고 아래 블록이 올라와 그 자리를 먹는다.
- **근무 없는 날엔 오늘 블록이 없다**(라운드 29 확정, 사용자). 「이번 주」가 두 번째 자리로
  올라온다. 라운드 6의 「블록 자리는 항상 같다」보다 라운드 6·8의 「오늘 블록은 근무 당일에만
  선다」가 이겼다 — 할 행동이 없는 날 「해야 하는 행동」 블록이 자리를 지킬 이유가 없다.
- 블록 이름표는 지면 위 메타 13/18 600이다. 알림 블록에는 이름표를 두지 않는다. (유지)

### 빈 블록 (라운드 30·31, 2026-08-18)

행 카드가 빌 때의 모양은 **하나**다 — 급여와 다가오는 근무가 같은 판에서 같은 모양으로 닫혔다.

- **앵커 없는 quiet 한 줄 + 더보기 유지.** 문장은 16/24 **400** `--color-text`(`.empty-line`),
  왼쪽 앵커 32×32 자리를 비우고 카드 왼쪽에서 시작한다. 카드 높이 약 92.
- **문장 행은 안 눌린다.** 목적지는 더보기 하나다 — 빈 카드가 같은 목적지를 두 번 팔지 않는다.
- **배지에 낱말을 넣지 않는다.** 라운드 22의 「없음」 배지를 걷었다. 배지는 D-n 숫자의 것이다.
- 문구는 블록마다 다르다 — 「아직 계산할 급여가 없어요」 · 「다가오는 근무가 없어요」.
- **값 자리가 있는 빈 행은 `—`다**(라운드 30). 0원이 아니고 블러도 탭도 없다.
- 대가를 하나 안다 — 값이 차면 앵커 32가 나타나 문장이 오른쪽으로 밀린다.

### 알림 블록 (라운드 6~8)

- **한 번에 카드 한 장만** 보인다. 남은 개수를 세지 않고 뒤에 겹친 카드도 보이지 않는다.
- **오른쪽 `X`를 눌러 끈다. 스와이프가 아니다.** 위로 당겨 넘기는 안을 먼저 그렸다가 뒤집었다 —
  당김 제스처는 최상단 새로고침과 방향이 겹치고, 무엇보다 사용자가 원한 건 명시적인 버튼이었다.
- 끄면 **그날 하루는 다시 뜨지 않는다.** 지속은 기기 저장이고, 금액 가림 설정과 같은 계층이다.
- **끄기는 읽음이 아니라 미루기다**(라운드 33 확정, 사용자). 헤더의 **종 점은 홈 카드를 다 꺼도
  남는다** — 점의 뜻은 「알림 화면에 안 읽은 것이 있다」이고, 점을 끄는 길은 **알림 화면에서 읽거나
  「모두 읽음」**뿐이다. 홈 블록에 안 오는 알림(마감 임박 · 다음 근무)도 점을 만든다.
  「모두 읽음」의 모양은 알림 화면 몫이라 **P0-T51**이 그린다.
- 다음 알림은 **애니메이션으로 들어온다.** 누름 `scale 0.88` 120ms → 카드가 8px 올라가며 사라짐
  160ms → 다음 카드가 14px 아래에서 올라옴 280ms. 교체 동안 **블록 높이는 그대로** 있어서 아래
  세 블록이 흔들리지 않는다.
- 마지막 장을 끄면 블록이 접히고(260ms) 아래 블록이 같은 시간에 올라온다.
- **무엇이 이 블록에 오나** — 한 번 읽고 끝나는 사건만이다.

  | 홈에 뜨는 것 | 자리 | 왜 |
  | --- | --- | --- |
  | 스케줄 확정 · 포지션 확인 | 알림 | 한 번 확인하면 끝난다 |
  | 공석 · 새 모집 · 신청 가능 | 알림 | 지나가면 없어지는 기회다 |
  | 확정 후 배정 변경 | 알림 | 한 번 확인하면 끝난다 |
  | 변경 요청 승인·미반영 | 알림 | 결과 통보라 읽으면 끝난다 |
  | 출근·퇴근 인증 | 오늘 | 읽는 게 아니라 해야 하는 행동이다. 끌 수 없다 |
  | 신청 마감 임박 | 일정 | 마감 전까지 계속 유효한 상태다. 꺼서 없앨 것이 아니다 |
  | 다음 근무 | 일정 | 사건이 아니라 이번 달 스케줄의 상태다 |

- 카드 모양 — 왼쪽에 그림 자산 28px, 가운데 값 16/24 + 메타 13/18(누르면 해당 화면으로),
  오른쪽에 끄기. **끄기 아이콘은 17px에 선 1.8**이고 누르는 영역은 44×44를 지킨다.
  보이는 것만 작아진 것이며, 영역을 줄이면 본문을 누르려다 알림을 끄게 된다. 카드 높이 70px.

### 그림 자산 (라운드 7, 2026-08-17)

- 알림 왼쪽 그림은 **아이콘이 아니라 그림 자산**이다. 색이 토큰이 아니라 자산에 박힌 고정값이라
  `gate:tokens` 검사 대상 밖이고, 라운드 1의 「그림자 없음」도 자산 안의 명암에는 적용되지 않는다.
  대신 개수로 통제한다 — **용도 하나에 그림 하나.** 알림 다섯(라운드 7)에 더해 급여 행 앵커
  셋이 있다(라운드 23 낱개 검수 후 확정): **초록 지폐 + ₩**(지난주 — `green` 그라디언트 재사용,
  애플 `💵` 짜임) · **호박·파랑·초록 막대 셋**(월 합계 — 전부 기존 그라디언트, 애플 `📊` 짜임) ·
  **보라 오름 꺾은선 + 화살촉**(누적 — 선 굵기는 `🔄`와 같은 4.2, 종이 없이 선 자체로 선다).
  보라(#b18cff→#6a3bd8)는 라운드 23이 추가한 고정색이다 — 애플 `📈`의 빨간 선은 「빨강 금지」에
  걸린다. 라운드 13~16의 동전 더미(통화 기호가 ¥로 읽혔다)·사각 배경 차트·추이는 폐기됐고,
  파랑 밴드 달력 앵커는 라운드 22의 D 배지 전환으로 자리가 없어졌다.
- **왼쪽 앵커 자리는 32×32 고정**이다 — 그림도 D 배지도 같은 칸을 쓴다. 알림 카드의 그림만
  28px(라운드 8 결정 유지).
- 어법은 **애플 이모지**다 — 다색 그라디언트, 위가 밝고 아래가 어두운 입체, 배경 블록 없이 그림
  자체로 선다. 2톤 실루엣으로 먼저 그렸다가 버렸다. 색을 넣어도 아이콘으로 읽혔다.
- 연한 바탕 원을 깔지 않는다. 그림이 이미 색을 갖고 있어 두 번 말하는 셈이 된다.
- 기본 28px. **24px 미만으로 쓰지 않는다** — 손 든 사람의 눈코입이 20px에서 뭉친다.
  더 작은 자리에는 그림을 아예 쓰지 않는다.
- 흰 종이가 들어가는 그림(달력·공고)은 흰 카드 위에서 실루엣이 녹아 **0.8px 옅은 테두리**를 넣는다.
- 다섯 그림과 색

  | 알림 | 무엇을 그렸나 | 색 |
  | --- | --- | --- |
  | 스케줄 확정 | 파란 밴드 달력 + 체크. 애플 `📅`의 빨간 밴드를 파랑으로 | 흰 종이 · 파랑 · 링 회색 |
  | 공석 · 새 모집 | 손 든 사람. 애플 `🙋`와 같은 자세, 셔츠만 초록 | 얼굴 노랑 · 머리 갈색 · 초록 |
  | 확정 후 변경 | 순환 화살표 둘. 애플 `🔄`와 같은 형태, 색만 호박 | 호박 |
  | 변경 요청 승인 | 애플 `✅`와 같은 초록 사각 + 흰 체크 | 초록 |
  | 변경 요청 미반영 | 같은 사각의 회색 판 + 흰 가로줄 | 회색 |

- **얼굴은 애플 기본과 같은 노란색이다.** 특정 피부색을 고르지 않기 위한 선택이고 애플이 노랑을
  쓰는 이유와 같다.
- **빨강을 쓰지 않는다.** 미반영도 실패가 아니라 처리가 끝난 결과다. 홈에서 빨강은 위치 권한 실패
  같은 진짜 오류에만 남긴다.
- 진짜 이모지(폰트)는 쓰지 않는다. `📅`는 코드포인트일 뿐이고 그림은 기기의 이모지 폰트가 그려서
  애플·삼성·구글이 서로 다른 것을 보여준다. 컬러 이모지 웹폰트를 싣는 길도 재봤는데 Noto Color
  Emoji 전체가 약 10MB이고 서브셋해도 COLRv1·CBDT 포맷 편차가 커서
  `harness/lib/bundle-budget.ts:8`의 예산(`500 * 1024`)에 안 들어간다.
- 남의 세트(Fluent Emoji Flat MIT / Twemoji CC-BY / Noto OFL)에서 SVG만 골라 싣는 길도 열려 있다.
  화면 쪽 구조가 같아서 — 알림 종류 하나에 그림 하나를 대응시키는 표 — 나중에 파일만 갈아끼울 수 있다.

### 오늘 출퇴근 블록 (라운드 6·8)

- 출퇴근 인증은 알림이 아니라 **해야 하는 행동**이라 알림 블록에서 뺐다. 근무 당일에만 두 번째
  자리에 서고 **끌 수 없다.** 그래서 화면에서 가장 강한 파랑은 이 블록 하나가 갖는다.
- **카드 안은 좌우 병치다**(라운드 8에서 세 안 중 선택). 왼쪽에 남은 시간 26/34 + 메타
  「출근 시각까지」, 오른쪽에 포지션 값 16/24 + 근무 시간 메타. 그 아래 버튼. 카드 높이 152px.
  시계 중심(최소 정보)·근무 중심(정보 충실) 두 안보다 짧고 시선이 두 번만 움직인다.
- 라운드 14~16에서 카드에 **발치가 생겼다** — hairline 아래 아바타 겹침 + 「4명 · 예식 14:00」
  캡션 한 줄. 라운드 8의 「예식 시각은 상세로 미룬다」를 뒤집은 것이다. 근거는
  `confirmation.ts`의 `roster`·`ceremonyTime`이고, **줄 위에서는 다른 근무자를 수만 세고 이름을
  쓰지 않는다** — 이름은 모달이 갖는다.
- **발치가 오늘의 포지션 배치도를 연다**(라운드 27). 줄 전체가 버튼이고 캡션은 「4명 ·
  예식 14:00 · 포지션 보기」, 오른쪽에 화살표 20이다. 열리는 것은 다가오는 근무와 **같은 바닥
  시트**다. 오늘 근무는 「다가오는 근무」 블록에 나오지 않으므로 오늘의 명단을 볼 자리가 여기뿐이다.
- 포지션 이름이 길면 오른쪽이 줄바꿈되어 좌우 균형이 깨진다. 말줄임 기준이 필요하고 그 계산은
  UI가 아니라 `model`이 한다.
- **카운팅은 두 종류다.** 인증 창이 열리기 전에는 열릴 때까지, 열린 뒤에는 예정 시각까지 센다.
  창은 `PRD.md:298-300`이 정해뒀다 — 출근은 예정 출근 **2시간 전**부터, 퇴근은 예정 퇴근
  **1시간 전**부터. 시안에 있던 「20분 전」은 근거 없는 값이라 뺐다.
- 세 상태 — 열리기 전(회색 버튼 + 흐린 시계 + `09:00부터 인증 가능`), 열린 뒤(파랑 버튼 +
  `출근 시각까지`), 예정 시각을 지난 뒤(`+0:12:04`로 뒤집힘). **늦어도 인증은 된다** — 막으면
  기록이 아예 안 남는다.

### 행 (라운드 13~16)

- 골격 — 왼쪽 앵커 32×32(그림 또는 D 배지) · 가운데 **라벨 13/18 위 + 값 18/26 아래** ·
  오른쪽은 알약(높이 34) · 아이콘 버튼(터치 44) · 화살표(20) 셋 중 **하나만**. 행 상하 12,
  행 사이 hairline, 행 카드 안쪽 상하 4(행의 12와 합쳐 첫/끝이 16). **한 행에 목적지 하나.**
  카드 맨 아래에 「… 보기 ›」 더보기 행을 둘 수 있다.
- **D 배지가 라운드 9~11의 「왼쪽 큰 자리」 규칙을 이어받았다.** 32px, radius 11, 글자 13/18
  600. 색은 **급함 층위 셋**이다(라운드 27) — `D-1` 진한 틴트 `#cfe0fc` · `D-2` 틴트 `#e4edfd`
  (둘 다 글자 `#0c3f9c`) · `D-3`부터 회색. `없음`도 회색이다. **D-0은 없다** — 오늘은 이 블록에
  들어오지 않는다.
- 행의 **세 번째 줄(안내 문구)은 금지**다. 라운드 15에서 붙였다가 뺐다 — 「확정되면
  알려드려요」류는 홈이 할 말이 아니고, 적용 시급은 급여 화면 몫이다.
- caption은 **「신청 마감까지」로 통일**하고 절대 날짜·시각은 적지 않는다(라운드 10~11 결정,
  행 전환 뒤에도 유지).

### 다가오는 근무 블록 (라운드 6 「이번 달 일정」 → 라운드 16 → 라운드 22에서 전면 개정, 확정)

- **확정 근무만 담는다.** 모집·신청·확정 대기는 홈에서 뺐다 — 공석·새 모집은 알림 블록,
  신청·마감은 일정 화면 몫. 라운드 16의 다섯 상태 표와 파랑 D-2 신청 행은 폐기됐다.
  마감 임박 표시를 홈에서 잃는 대신 알림 한 장이 그 역할을 진다(2026-08-17 사용자 확정).
- **내일(D-1)부터 담는다**(라운드 27). 오늘 근무는 「오늘」 카드가 통째로 맡으므로 이 블록에
  들어오지 않는다 — 같은 근무를 두 블록이 말하지 않는다. D-0 배지는 만들지 않았다.
- 가까운 순(D-n 오름차순) **최대 3건.** 행은 **날짜가 값으로 위** + 「포지션 · 시간」 캡션
  아래(날짜가 주인공). 날짜 값은 **16/24** — 행 값 18에서 이 블록만 한 단계 내렸다(사용자,
  계층 재확정 때 예외로 반영). 왼쪽은 D 배지(라운드 27부터 D-2까지 파랑 틴트), 오른쪽은 알약
  `확인` 하나 — 무게 400(라운드 26).
- `확인`(또는 행)을 누르면 그날 **포지션 배치도 모달** — 바닥 시트 260ms + dim, 날짜·예식 시각
  헤더, 포지션별 명단, 내 이름은 파랑. **「명단은 상세 화면 몫」(라운드 14~16)의 예외**다 —
  상세 화면 이동을 모달이 대신한다.
- 없음 상태는 위 「빈 블록」 절이 소유한다(라운드 31) — **앵커 없는 안 눌리는 한 줄**
  「다가오는 근무가 없어요」(16/24 400) + 더보기 「전체 일정 보기」. 「없음」 배지는 걷혔다.

### 이번 주 블록 (라운드 16 신설, 라운드 20·21에서 개정)

- **스트립은 확정 근무만 보여준다.** 신청·모집·마감 상태는 이 블록 소관이 아니다 — 일정 화면과
  「다가오는 근무」 몫. 라운드 16의 「신청한 날 옅은 틴트」와 라운드 1 달력 어법 재사용은 폐기.
- 근무 있는 날은 **검은 숫자(strong)**, 없는 날은 흐린 숫자(weak)이고 눌리지 않는다. 틴트는
  **선택된 날에만** 깔린다 — 파랑 배경이 하단 탭과 같은 눌림-되튐 420ms(같은 키프레임)으로
  잡힌다.
- **오늘은 칸을 두르는 실선 1px `#e5e8eb`다**(라운드 32 확정). 라운드 20이 밑줄을 뺀 근거는
  「위 「오늘」 카드가 이미 말한다」였는데 **라운드 29에서 그 카드가 없는 날이 생겼다** — 근무 없는
  날 홈에 오늘을 말하는 자리가 하나도 없어진다. 표시는 **일정 화면 달력과 같은 것**을 쓴다
  (라운드 28) — 새 색이 안 늘고 어법이 하나다. **빈 주만이 아니라 늘 켜진다.**
- 근무 있는 날을 누르면 발치가 **그날 정보**(날짜 · 포지션 · 시간 · 그날 예상 급여)로 바뀌고,
  다시 누르면 주 요약으로 돌아온다.
- **발치는 한 줄 좌우 병치**(라운드 21, 네 안 중 나) — 왼쪽 캡션 13(「주급 · 3회 15시간」),
  오른쪽 금액 18/26. 주급이 26에서 내려와 **홈의 큰 숫자는 카운트다운 하나**가 됐다.
  빈 주는 「이번 주 근무가 없어요」 + 「—」(0원이 아니라 없음, 급여 블록과 같은 원칙).
- **주급이 주가 된다**(사용자: "월 급여는 주가 아니고 우린 주급"). 다만 `PRD.md:346`은
  「날짜별 금액과 월별 합계」라 **기획 미결**이다 — 주의 경계(월요일 시작인지 급여 주기인지) ·
  월별 합계를 남길지 · 달에 걸친 근무의 귀속을 기획이 정한 뒤에 확정된다. 시안은 월~일 가정.

### 급여 블록 (라운드 6, 라운드 14~16에서 행 셋으로 개정, 라운드 23에서 확정)

- 행 셋 — 지난주 · 이번 달 · 연 누적. 앵커는 초록 지폐·다색 막대·보라 꺾은선(그림 자산 절,
  라운드 23 확정).
- **상하 여백은 행 규칙 그대로다**(라운드 23) — 행 상하 12 · 라벨-값 1 · 카드 상하 4. 1차에
  행 14·카드 8 안을 냈으나 가(현행)로 닫혔다. 행 규칙 개정 없음.
- **「받은 총급여」는 만들 수 없다.** 「받은」은 실제 지급 기록인데 `PRD.md:375`가 실제 급여
  정산·지급 완료·급여명세서를 MVP에서 뺐고 `WORKER-FLOWS.md:117`은 그 표현 자체를 금지한다.
  그래서 **「누적 예상 급여」**다 — 예정 시간 계산 값의 합이라 INV-PAY-01을 지킨다.
- **빈 행은 `—`다**(라운드 30). 0원이 아니다 — 0원은 계산 결과처럼 보이는데 실제로는 계산할
  근무가 없다. `—`는 **가릴 것이 없어 블러도 탭도 없다**(라운드 18의 금액 가림 밖이다).
  라벨에서 **0인 갈래는 안 적는다** — 「지난주 · 0회 0시간」이 아니라 **「지난주」**다.
- **셋이 다 비면 한 줄로 접힌다**(라운드 30 확정, 사용자) — 앵커 없는 행 하나
  **「아직 계산할 급여가 없어요」**(16/24 400 `--color-text`) + 「주별로 모두 보기」 더보기.
  블록은 남고 안의 내용만 준다. 카드 높이 약 92.
- **지난 달 fallback을 걷었다**(라운드 30). 아래 표 둘째 상태(「이번 달 확정 없음 → `7월 예상
  급여`」)는 카드 하나였던 라운드 6의 것이다. 행 셋이 된 지금은 **「지난주」 행이 달 초의 지난 달
  돈을 이미 덮고**(8월 3일의 지난주는 7월 말이다), 가운데 행 이름만 달을 넘나들면 위아래 두 행과
  기간이 어긋나 읽힌다. 자리는 **지난주 · 이번 달 · 연 누적**으로 고정한다.
- 라운드 6의 원칙은 행 구조에서도 유지된다. 이름이 값의 근거다. 아래 표의 상태 셋은 **라운드 30이
  카드 하나 시절의 기록으로 내렸다** — 지금 사는 것은 위 세 줄이다.

  | 상태 | 이름 | 값 | 덧붙이는 말 |
  | --- | --- | --- | --- |
  | 이번 달 확정 있음 | `8월 예상 급여` | 가린 금액(라운드 18 — 눈 없음, 금액 탭) | 확정 근무 6회 · 리허설 1회 |
  | 이번 달 확정 없음 · 지난 달 있음 | `7월 예상 급여` | 가린 금액(위와 같음) | 8월은 확정되면 이 자리가 8월로 바뀌어요 |
  | 둘 다 없음 | `예상 급여` | `—` | 확정된 근무가 아직 없어 계산할 금액이 없어요 |

- **0원이 아니라 없음이다.** 0원은 계산 결과처럼 보이는데 실제로는 계산할 근무가 없다. 숨길 값이
  없으니 눈 아이콘도 없다.
- 지난 달 fallback은 `PRD.md:18` **INV-PAY-01**과 부딪히지 않는다. 지난 달 값도 확정 기준이라
  신청분을 금액으로 계산하는 일이 없다. 신청분까지 미리 금액으로 보여주려면 INV-PAY-01을 고쳐야
  하고 그건 PRD 불변식이라 기획으로 돌려보낼 일이다.

### 계층 (라운드 17 제안 → 라운드 24에서 새 무게로 확정, 2026-08-17 사용자 승인)

**글자 여섯 단계.** 스케일 8단계(`FOUNDATIONS.md:56-63`)는 그대로고 배정과 무게만 정한다.
**화면에 700이 한 글자도 없다** — 가장 무거운 것이 600, 값은 500이다.

| 단계 | 역할 | 무게·색 | 서는 자리 |
| --- | --- | --- | --- |
| 26/34 | 큰 숫자 | 600 strong tabular | **카운트다운 하나뿐** — 주급이 18로 내려와(라운드 21) 자리가 하나다 |
| 22/30 | 화면 제목 | 600 strong | 「홈」 |
| 18/26 | 행 값 | 500 strong tabular | 급여 행 금액 · 주급 발치 · 「—」. **다가오는 근무만 16/24**(라운드 22 예외) |
| 16/24 | 문장 값과 그 아래 | 문장 400 / 숫자·날짜 500, strong | 알림 문장 · 오늘 포지션 · 모달 제목(600)과 명단 · 주간 근무일 숫자 · 다가오는 근무 날짜 |
| 14/20 | 행동 | 파란 버튼 600 / 더보기 400 | 크기가 같아도 무게가 급함을 가른다 |
| 13/18 | 메타 | 이름표 500 muted / 라벨·캡션 400 muted / D 배지 600 / **알약 400**(라운드 26) / 탭 400(선택 600) | 이름표 · 라벨 · 캡션 · 배지 · 알약 · 탭 라벨 |

- **단계 밖 값은 쓰지 않는다.** `display` 32/40은 자리가 없고, 15/22는 라운드 24가 걷었다.
- **아이템 안의 순서 규칙 셋** — ① 지표(숫자가 주인공): 라벨 13 위 · 값 18 아래 — 급여 행.
  ② 문장(읽을 것이 주인공): 문장 16 위 · 캡션 13 아래 — 알림·오늘 카드·다가오는 근무(날짜가
  주인공이라 날짜가 위). ③ **한 줄 좌우**(라운드 21 신설): 왼쪽 캡션 13 · 오른쪽 값 18 —
  주급 발치. 쌓으면 홈에 큰 숫자가 둘이 되어 카운트다운과 싸운다.
- **배치 계층** — 지면 좌우 16 · 블록 사이 12 · 이름표 아래 6 / 카드 안 16(예외 둘: 행 카드
  상하 4, 주간 카드 상하 14) / 행 상하 12 + hairline(라운드 23이 14·8을 재보고 12로 남겼다) /
  앵커 32, 본문과 14(알림 그림만 28, 본문과 12) / 오른쪽 자리 셋 중 하나만(알림 카드는 끄기
  44 때문에 카드 오른쪽 여백이 8).
- 라운드 17이 실제로 바꾼 코드는 둘이었다 — 주간 카드 좌우 여백 12→16, 주간 날짜 tabular.

### 로딩 · 오프라인 · 에러 (라운드 34, 2026-08-18 확정)

**전역 규칙이다. 화면마다 다시 묻지 않는다.** 승인 문서가 이미 소유한 것
(`PATTERNS.md:75-82` · `COMPONENTS.md:137-141`)은 모양만 준 것이고, **에러는 새로 정한 것**이다.

**로딩 — skeleton.**

- **아는 것은 가리지 않는다.** 화면 제목 · 블록 이름표 · 요일과 날짜는 서버 없이 아는 값이라
  덮지 않는다. 회색 막대는 **서버에서 와야 하는 것**에만 깐다.
- 막대 색 **`--color-border` `#e5e8eb`**, radius **6**. 앵커 자리는 32×32 radius 11,
  아바타·알약 자리는 pill, 버튼 자리는 52 radius 16. 흰 카드 위에서 `surface-weak`은 거의 안
  보였다. **새 색이 안 늘었다.**
- **반짝임(shimmer)을 넣지 않는다.** 움직임은 눌림·리빌·시트처럼 뜻이 있는 순간에만 쓴다.
  `FOUNDATIONS.md:145`는 반복 애니메이션을 허용하지만 강제하지 않는다.
- 카드 골격·여백·블록 순서는 실제 화면과 같다(`PATTERNS.md:75` 「구조를 유지하는 skeleton」).
- **작은 갱신은 skeleton을 안 쓴다**(`PATTERNS.md:76`) — 기존 내용을 두고 해당 영역만 진행 표시.
  최상단 당겨 새로고침의 스피너가 그 꼴이다.

**오프라인 — 헤더 레이어의 배너.**

- 라운드 25가 상태 표시줄(24)+헤더(56)를 한 레이어 80으로 묶었으므로 배너(40)는 **그 아래 셋째
  줄**로 들어가 **한 레이어 120**이 된다. 콘텐츠가 그 뒤로 지나가는 것은 그대로다.
- 색은 **`--color-warning-surface` `#fff7d6` + `--color-warning` `#765500`**
  (`globals.css:20-21` 기존 토큰). 빨강이 아니다 — 오류가 아니라 읽기 전용 상태다.
- 문구 **「인터넷 연결이 끊겼어요 · 지금은 보기만 할 수 있어요」**. `COMPONENTS.md:138`의 문구에
  읽기 전용을 한 줄로 붙였다.
- **변경 버튼은 죽고 라벨이 이유를 말한다** — 출근 인증 버튼이 「연결되면 인증할 수 있어요」로
  바뀐다. `COMPONENTS.md:141`의 「이유와 함께 비활성화」를 라벨로 지킨다.
- 이미 보이던 값은 그대로 둔다(`PATTERNS.md:82`). 복구되면 「최신 내용을 불러왔어요」 토스트
  하나(`COMPONENTS.md:139`).

**에러 — 일부는 블록별, 전부는 한 장**(사용자 선택).

- **일부 실패는 그 블록 자리에서** 말한다 — 앵커 없는 한 줄 「〈블록 이름〉을 불러오지 못했어요」 +
  오른쪽 **「다시 시도」 알약**(quiet, 높이 34). 산 블록은 산다. 빈 블록 어법(라운드 30·31)과 같은
  골격이고 오른쪽 자리 하나만 알약으로 채운 것이다.
- **아무것도 못 받았으면 화면 한 장** — 콘텐츠를 걷고 가운데 「불러오지 못했어요」(16/24) ·
  「잠시 뒤에 다시 시도해 주세요」(13/18 캡션) · 「다시 시도」 quiet 버튼(높이 44).
  **헤더와 탭바는 남는다** — 다른 탭으로 나갈 길을 막지 않는다.
- **경계는 「전부 실패」다.** 하나라도 받았으면 블록별로 간다.
- **빨강을 쓰지 않는다.** 되돌릴 수 있는 실패이고 바로 옆에 「다시 시도」가 있다.
  `COMPONENTS.md:61`이 danger를 허용하지만 강제하지 않고, 라운드 7의 「빨강은 진짜 오류에만」과
  일관되게 남긴다.

### 일정 화면 (라운드 28, 2026-08-18 확정)

**달력 셀 — 규칙 셋.** ① 선택하면 뒤에 파란 네모박스 `#cfe0fc` + 되튐 420ms. 추가든 취소든
**이번에 손댄 칸**이면 같은 박스다. ② **박스 안 숫자 색이 방향**을 말한다 — 검정이 넣는 중,
muted가 빼는 중. ③ **근무하는 날은 숫자 아래 점** — 확정은 파랑 `#0c3f9c`, 지난 근무는 회색
`#a8acb3`, **변경 요청 중이면 속이 빈다**.

| 상태 | 박스 | 숫자 | 점 |
| --- | --- | --- | --- |
| 모집 없음 · 마감 · 지난 근무 | — | 회색 `#7c828a` | 지난 근무만 회색 점 |
| 신청 가능 | — | 검정 500 | — |
| 신청 완료 | — | 파랑 `#0c3f9c` 500 | — |
| 확정 | — | 파랑 `#0c3f9c` 500 | 파란 점 5px |
| ＋ 선택 | 파랑 `#cfe0fc` | 검정 500 | — |
| − 선택 | 파랑 `#cfe0fc` | muted `#7c828a` | — |

- **오늘은 칸 전체를 두르는 실선 1px `#e5e8eb`.** 라운드 1의 밑줄을 개정한다. 새 색이 아니다.
- **손댈 수 없는 날은 회색 하나**다. 누를 수 있는지는 색이 아니라 **되튐 유무**가 진다.
- 셀에 배지·범례를 넣지 않는다(라운드 1 유지). **점만 예외**이고 뜻은 「내가 근무하는(했던) 날」
  하나다.

**달력 발치 세 줄** — ① 요약(신청 가능 → 신청 → 근무 순, **저장된 것만**, 0인 갈래는 안 적는다)
② 가장 급한 마감 한 줄(문장 `#5b616e`, 날짜만 잉크 500) ③ `N일 모두 선택` quiet 버튼.
**고를 게 없으면 비활성 + 「선택할 날짜가 없어요」**다. 「모두 해제」는 없다 — 무르는 건 날짜를
직접 누른다.

**두 뷰의 역할.** 달력은 **내 달 전체**(확정·지난 근무·신청·모집), 목록은 **모집만**
(열린 회차 + 지난 회차). 조작 단위가 달력은 날짜 하나, 목록은 회차 전체다.

**목록.** 카드 하나가 모집 회차 하나다. 머리행은 왼쪽 **D 배지** · 라벨 「신청 기간 · 근무 N일」 ·
값 **`7월 28일 ~ 8월 18일`**(모집 기간) · 오른쪽 「N일 모두 선택」 알약. 날짜 행은 **16/24 400**
(머리행만 18/26 500). **마감 임박순**으로 쌓고, **지난 모집은 접힌 채 아래로 무한 스크롤**한다
(배지 「끝」, 두 회차씩, 끝나면 「더 없어요」). 카드 사이 12.

**낱말.** 담는 말과 내는 말을 가른다 — 「모두 선택」·「선택됨」은 담기, 「신청하기」·「신청 완료」는
내기. 저장하면 바가 걷히고 **「신청이 완료되었어요」 토스트 하나**다(undo 없음).

**하단 변경 바.** 변경이 0이면 없고 1개부터 240ms로 올라오며 **탭바 자리를 덮는다**(높이 64).
개수를 추가·취소로 나눠 센다.

**배정표 바닥 시트.** 확정·지난 근무 날짜를 누르면 라운드 22 어법 그대로 열린다 — 16/24 600
제목, 상하 10 좌우 0 행, 행 사이 hairline, 본인은 파랑 500. **닫기는 머리 오른쪽 ✕**, 겹겹
시트는 머리 왼쪽 ←(홈 확정본도 같이 고쳤다). 발치에 `근무 변경 요청` quiet 버튼 → 유형 둘
(`근무 취소` · `포지션 교대 요청`) → 교대는 **자격 있는 상대만** 오르는 겹이 하나 더.

**빈 상태.** 열린 모집이 없으면 달력 발치 둘째 줄과 목록이 똑같이 **「지금은 열려 있는 모집이
없어요」**, 마감 카드 없음, 버튼 비활성. 아무것도 없으면 요약 줄도 사라진다.

## 라운드 기록

### 라운드 1 — 전역 틀 (2026-08-17)

8월 16일 세션에서 고른 방향(회색 지면 + 흰 카드, C3안 색)을 전역 틀로 정리했다. 홈·일정 달력·일정 목록·예상 급여 네 화면에 얹어 확인했다.

### 라운드 2 — 타이포 위계 (2026-08-17)

"큰 폰트가 너무 크다"는 지적. 원인은 스케일이 아니라 배치였다 — 화면 제목에 `display` 32/40을 쓰고 있었고 `FOUNDATIONS.md:57`은 원래 제목을 `headline-lg` 26/34로 정해뒀다. 세 안(A 26/32, B 22/32, C 22/26)을 같은 홈 화면으로 비교해 **C**를 골랐다.

### 라운드 3 — 알림 자리·아이콘·금액 가림 (2026-08-17)

- 알림을 하단 탭에서 헤더 오른쪽 종으로 옮겼다. 안 읽음은 종 위 점으로 알린다.
- 아이콘은 라인/채움 중 라인으로 통일했다. Lucide가 stroke 기반이고 `FOUNDATIONS.md:127-128`이 이미 "24px 2px stroke"와 "선택은 굵기 변화로"를 정해뒀다.
- 금액 가림을 넣었다. 기본이 가림이다.

### 라운드 4 — 앱 셸 (2026-08-17)

라운드 3의 시안이 하단 탭을 `홈 · 일정 · 전체` 셋으로 그렸는데 계층이 맞지 않는다는 지적. 예상 급여가 `전체` 아래 2단계에 있고 알림이 헤더로 빠져 탭 자리가 하나 비었다. 세 안(A 급여를 탭으로, B 현행 유지, C 전체를 헤더로)을 비교해 **A**를 골랐다.

### 라운드 5 — 선택 탭의 모양과 움직임 (2026-08-17)

선택 표현을 "라인에 색"에서 "어두운 채움"으로 바꿨다. 움직임은 세 가지(통통 scale, 좌우 wobble, 솟구침)를 먼저 냈다가 전부 버리고 **눌림**으로 갔다 — 위아래로 눌리며 양옆이 퍼지는 형태다. 이후 세 번 더 고쳤다.

1. 채움을 `ink-950`에서 `gray-800`(#3a3f4a)로 낮췄다. 팔레트에 `ink-950` #0a0b0d와 `gray-700`
   #5b616e 사이가 비어 있어 새로 만든 단계다. 선택 안 된 탭이 이미 `gray-700`이라 그걸 쓰면
   채움 말고는 구별이 사라진다. 2026-08-17 이름 확정.
2. 선택 탭 뒤 회색 블록을 뺐다. 처음엔 셀 전체를 덮었다가 좌우 8px·위 6px 들여 줄였는데, 결국
   "선택돼 있는 내내 유지되는 것이면 채움과 색이 이미 하는 말"이라 없앴다.
3. 눌림을 두 번으로 늘렸다. 크게 한 번, 반동으로 작게 한 번.

그 과정에서 버그 하나를 잡았다. 블록이 `position: absolute`인데 라벨이 `<span>` 없는 맨 텍스트 노드라 덮여 사라졌다. 아이콘은 `<span class="glyph">`에 싸여 있어 살아남아서 글씨만 없어 보였다.

### 라운드 6 — 홈의 구조 (2026-08-17)

세 안(하나만 / 하나 + 놓칠 것만 줄로 / 우선순위 스택)을 냈는데 셋 다 아니었다. 사용자가 **고정 4블록**을 제시했고 그 구조로 다시 그렸다. 그 뒤로 다섯 번 고쳤다.

1. 첫 블록을 **가로 슬라이드**로 그렸다 → 슬라이드가 아니라 **끌 수 있는 카드**였다.
2. 넘김을 **위로 당기기 + 천장 닿음 이펙트**로 만들었다 → 스와이프가 아니라 **`X` 버튼**이었다.
3. 남은 개수 `1 / 3`을 붙였다 → **개수는 표시하지 않는다.**
4. 블록 안에 스크롤이 생기게 뒀다 → 블록 안에도 화면에도 **스크롤은 없다.** 스크롤은 최상단을
   넘어 당길 때 새로고침으로만 쓴다.
5. 출퇴근 인증을 첫 블록 슬라이드에 넣었다 → **알림에서 빼서 두 번째 자리에 고정**했다.
   알림은 읽고 끝나는 사건이고 인증은 해야 하는 행동이라는 구분이 여기서 나왔다.

프레임 높이도 340×640에서 340×736으로 고쳤다. 640은 실제 폰 비율보다 짧아서 없는 잘림을 만들고 있었다(Playwright 기준 기기는 Pixel 5, 393×851).

카드 바깥 배치를 가로 타일·하단 고정 바로 바꿔보는 라운드를 한 번 더 냈다가 되돌렸다 — "배치가 아니라 카드 안 내용이 문제"라는 지적이었다. 하단 고정 바는 `PATTERNS.md:15`가 하단 고정 버튼을 「깊은 작업」에만 허용하는 것과도 부딪혔다.

### 라운드 7 — 알림 그림 (2026-08-17)

이모지를 쓰고 싶은데 기기마다 다르게 보이는 게 문제였다. 원인은 이모지가 그림이 아니라 글자여서다. 세 길(웹폰트 / 남의 세트에서 SVG만 / 직접 그리기)을 재고 **직접 그리기**로 갔다.

첫 시안은 2톤 실루엣이었고 "너무 이상하다, 애플처럼"이라는 지적을 받았다. 어법을 바꿨다 — 다색 그라디언트 + 입체 + 배경 없음. 그다음 두 번 더 고쳤다.

1. 크기 36 → 32 → **28px**.
2. 공석을 「공고 종이 + 초록 더하기 배지」로 그렸다가 애플 `🙋`처럼 **손 든 사람**으로 다시 그렸다.

### 라운드 8 — 출근 카드 안 (2026-08-17)

세 안(시계 중심 184px / 근무 중심 190px / 좌우 병치 152px)을 비교해 **좌우 병치**를 골랐다. 같은 라운드에서 끄기 아이콘을 17px·선 1.8로 줄였다.

### 라운드 9~11 — 일정 카드와 빈 자리 (2026-08-17)

일정 카드를 출근 카드와 같은 좌우 병치로 골랐다(가안, 홈 전체 프레임에 얹어 비교). caption을 「신청 마감까지」로 통일하고 절대 날짜·시각을 안 쓰기로 했다. 빈 자리는 「왼쪽 큰 자리를 비우지 않는다」(A안) — 확정 대기는 「대기」를 글자로. 왼쪽 큰 자리의 뜻을 정하는 세 안(이름표 추종 / 크기 이원화 / 블록 합침)까지 냈으나, 라운드 12의 "너무 동일한 형태로 쭉 나열되는 느낌" 지적으로 방향 자체가 바뀌었다.

### 라운드 12~13 — 행 어법 전환 (2026-08-17)

나열감의 원인은 좌우 병치를 일정 카드에도 쓴 것이었다. 무게 차이 세 안(흰 카드는 행동에만 / 아래 둘 합침 / 어법 분리)을 낸 뒤, 사용자가 토스 홈 캡처를 참조로 제시해 **행 어법**으로 다시 짰다 — 그림 앵커 32 + 라벨 위·값 아래 + 알약 + 한 카드에 행 여러 개 + 더보기 행. 세 안 중 **나(출근만 카드, 나머지 행)** 채택. 라운드 9~11의 좌우 병치 일정 카드는 폐기되고 왼쪽 큰 자리 규칙은 D 배지가 이어받았다. 이 전환이 라운드 1 「선 없음」(hairline 예외), 라운드 2 값·메타 순서, 라운드 6 네 블록, 라운드 7 그림 개수를 건드린다는 것을 명시하고 진행했다.

### 라운드 14~16 — 콘텐츠 확충과 주급 (2026-08-17)

"휑하다"는 지적에 세 축(오늘 깊게 / 이번 달 깊게 / 주제별 카드)을 냈고, "급여도 이번·저번·총, 우린 주급이라 주급이 중요" 지시로 주급 중심 재편. 라운드 15는 행마다 셋째 줄을 붙였다가 "텍스트가 아니라 컨텐츠"라는 지적으로 뒤집고, 라운드 16에서 **항목을 늘리고 글을 줄였다** — 7일 스트립, 다가오는 근무 세 건, 급여 세 줄, 더보기 둘. 사용자 승인("응 이거 좋은데"). 이 승인으로 무스크롤 규칙이 폐기됐다. 오늘 카드에 발치(아바타 + 예식 시각)가 들어와 라운드 8의 「예식은 상세로」가 뒤집혔다. 라운드 15부터 proposal.html은 **인터랙티브 프로토타입**이다 — 카운트다운·금액 가림·알림 끄기·탭 모션·당겨 새로고침이 실제로 동작하고 상태 조작판이 붙어 있다(`design:build`가 `<script>`를 보존한다).

### 라운드 17 — 계층 정리 (2026-08-17, 보류)

글자 여섯 단계, 두 줄 순서 규칙, 배치 계층을 표 셋으로 봉했다(위 「계층」 절). 사용자 확인 전에 세션이 넘어갔고, 8월 17일 사용자가 확정을 보류했다("바꿀 게 많다") — Bold 과다 지적이 계층 표의 무게 열을 건드린다. Bold 감량 라운드 뒤 다시 확정받는다.

### 라운드 18 — 금액 가림 방식 (2026-08-17, 확정)

라운드 17 확정을 미루고 가림 방식부터 고쳤다(사용자 지시). 눈 아이콘을 빼고 **자동 블러 + 금액 탭으로 해제**로 바꿨다 — 주급 발치의 눈이 사라지고 금액 자체가 누르는 자리가 됐다. 더미 렌더 + `aria-hidden` 규칙은 유지. 파생 물음 넷을 물어 같은 날 답을 받았다 — ① 기준은 토·일 고정(처음 지시)이 아니라 **근무 있는 날** ② 해제는 **화면 전체** ③ 지속은 **화면 이탈까지** ④ 설정 스위치는 **자동 가림 on/off**로 남김. 기준이 근무일로 정해지면서 조작판의 「요일」 그룹을 뺐다 — 「오늘」(근무일/근무 없는 날)이 이미 그 축이다. 2026-08-17 사용자 승인("응 좋아")으로 확정, 위 「금액 가림」 절에 반영.

### 라운드 19~21 — 굵기 · 이번 주 스트립 · 발치 (2026-08-17, 확정)

한 판에 두 축을 조작판으로 걸었다. **라운드 19 굵기** — "Bold가 너무 많다"는 지적. 세 안: 현행(700 중심) · 감량(제목·큰 숫자·행 값·D 배지 전부 600, 이름표 500) · 최소(값 500, 제목만 600, 이름표 400). 기본 선택은 감량. **라운드 20 이번 주** — "기존 일정 화면 구조와 너무 다르다"는 지적. 실물 일정 화면(`ScheduleView.tsx` + `shared/ui/calendar.tsx`)은 달력/목록 토글에 셀 상태 여섯(모집 없음·신청 가능·선택됨·신청·마감·확정), 셀 전체 틴트 + 셀 안 상태 글자, 오늘은 파란 링이다. 어법 세 안: 가(숫자만+틴트, 현행 — 신청 가능·마감을 구분 못 함) · 나(일정 셀 어법 그대로 이식: 42×44 셀, 상태 글자, 오늘 링) · 다(틴트만 이식, 상태 글자 없이, 오늘 밑줄 유지). 케이스 넷(확정 3회 · 확정·모집 섞임 · 신청만 · 빈 주)을 조작판으로 오간다. 신청만·빈 주는 확정 근무가 없어 주급이 「—」다(INV-PAY-01 — 신청분을 금액으로 계산하지 않는다). **어법 나를 고르면 라운드 1의 「달력 셀에는 숫자만, 배지 없음, 오늘 밑줄」 규칙 개정이 딸려온다.**

**5차(같은 날, 확정)** — 발치를 **나(한 줄 좌우 병치)**로 확정("나가 좋은 것 같고"). 리빌을 1.1초 → **1.5초**로, 블러를 7 → **5px**로 풀었다(사용자 "더 느리게 · 블러 약하게"). 굵기·스트립·발치 전부 확정 규칙 절에 반영했다. 주급이 26에서 18로 내려오면서 라운드 17 계층 표의 「큰 숫자 최대 두 곳(카운트다운·주급)」이 낡았다 — 재확정 때 「카운트다운 하나」로 고쳐 받는다. 다음은 라운드 22(다가오는 근무 배치).

**4차(같은 날)** — 사용자가 스트립 선택·리빌 방향을 승인("응 좋은데"). 리빌 빛을 파랑에서 **흰색**으로, 700ms에서 **1100ms**로 늦추고 띠를 넓혔다(35/50/65). 그리고 3차 네 안이 과녁을 잘못 짚은 것을 정정 — 고쳐달라던 곳은 급여 블록이 아니라 **이번 주 카드의 발치(주급 자리)**였다. 급여 벤치를 걷고 발치 네 안으로 교체: **가**(현행 — 라벨 위·금액 26 아래) · **나**(행처럼 한 줄 좌우, 금액 18 — 홈의 큰 숫자가 카운트다운 하나로 준다) · **다**(근무·시간·주급 스탯 세 칸) · **라**(금액 위·캡션 아래 — 두 줄 순서 규칙을 뒤집는 안). 각각 주 요약/날 선택 두 상태로 그렸다. 라운드 21은 이 발치 선택이다.

**3차(같은 날)** — ① 스트립 선택을 테두리에서 **파랑 배경 깔림**으로 바꾸고, 애니메이션은 하단 탭의 눌림-되튐(420ms, 같은 키프레임)을 그대로 이어받았다("아래 라우팅 섹션처럼"). 선택 안 된 근무일은 틴트 없이 **검은 숫자만**, 근무 없는 날은 흐린 숫자. ② **금액 리빌** — 블러가 벗겨질 때 빛이 숫자를 왼쪽→오른쪽으로 훑는다(700ms, `background-clip: text` 그라디언트 스윕, 끝나면 클래스 제거). ③ 급여 블록이 아쉽다는 지적에 **네 안**을 기기 아래 벤치로 냈다 — 가(현행 정리, 행 상하 12→14) · 나(그림 앵커 제거) · 다(8월을 26으로 크게 + 발치 두 줄) · 라(그림 대신 글자 배지 주·8월·연). 급여 안 선택은 라운드 21로 잡는다.

**2차(같은 날)** — 세 안 비교를 보고 사용자가 방향을 새로 줬고 비교판은 걷었다. ① 굵기 확정 방향: **카운트다운·주급 600, 덜 중요한 것 500** — 제목 600 · 행 값 500 · 이름표 500 · D 배지 600, "Bold를 많이 쓰는 경향 주의". ② **오늘 밑줄(검은 그림자) 삭제** — 스트립에서 오늘 표시가 없어졌다. ③ 어법 문제 자체를 재정의: 홈 스트립은 **신청·모집을 다루지 않고 확정 근무만 보여준다.** 근무 있는 날만 누를 수 있고, 누르면 **파란 테두리가 애니메이션(240ms 눌림-되튐)으로 잡히며** 발치가 그날 정보(포지션·시간·금액)로 바뀐다. 해제하거나 아무것도 안 고르면 주 요약(주급·근무 횟수·시간). 케이스는 근무 3회 · 근무 1회 · 빈 주 셋으로 줄었다 — 신청·마감 케이스는 이 블록의 소관이 아니게 됐다. 일정 셀 어법(상태 여섯)은 일정 화면 라운드의 몫으로 돌아간다.

### 라운드 22 — 다가오는 근무 배치 (2026-08-17, 확정)

"다가오는 근무도 배치를 좀 수정하자"는 지적. 같은 내용(확정 둘 · 열린 모집 하나 · 더보기)을 네 배치로 벤치에 냈다 — **가**(현행: D 배지 앵커, 날짜가 값) · **나**(포지션이 주인공: 날짜·시간이 라벨, 값이 포지션) · **다**(날짜 상자 앵커: D 배지 대신 「8월/21」 상자, 며칠 남았는지 표시가 사라진다) · **라**(한 줄 압축: 앵커·두 줄 없이 — 행 골격 「라벨 위·값 아래」를 벗어나는 안이라 고르면 행 규칙 개정이 딸려온다).

**6차(같은 날, 확정)** — 사용자 승인("응"). 날짜 값을 18 → **16/24**로 이 블록만 내렸다(`.upco` 스코프). 위 확정 규칙 절에 반영. 모집·신청 홈 제외와 배치도 모달의 명단 예외도 이 승인에 묶였다.

**5차(같은 날)** — 4차의 블러 페이드도 아니었다: "블러는 바로 풀려야 함, 줄일 건 빛의 강도". unblur 페이드를 걷고 **블러 즉시 풀림 + 빛을 흰색에서 옅은 회백(#b7bec9)으로, 띠 35/50/65 → 40/50/60으로 좁힘**. 확정 규칙 절의 리빌 항목에 반영.

**4차(같은 날)** — 블러 완화가 **오해로 판명**("금액 숨김 블러가 아니라 금액 리빌 블러"). 숨김 블러를 7px로 원복하고, 리빌을 블러 페이드로 다시 만들었다 — 열 때 실값이 4px 블러에서 0.9초 ease-out으로 풀리며 흰 빛 훑기(1.5초)와 겹친다. `animationend`는 `shine` 종료에만 클래스를 걷는다(unblur가 먼저 끝나며 빛을 죽이던 것 방지).

**3차(같은 날)** — ① 계층 지적("계층이 반대로 되었어, 날짜를 더 우선으로") — 날짜를 **값(18)으로 위에**, 포지션·시간을 캡션으로 아래에 뒤집었다. 두 줄 순서 규칙의 「문장이 주인공이면 문장 위·캡션 아래」 꼴이다(날짜가 주인공). 없음 행은 「다가오는 근무가 없어요」 한 줄로 줄였다. ② 블러 3.5 → **2.5px**(세 번째 완화). ③ **가려진 동안에는 애니메이션 금지** — 눌림 스케일 없음, 빛 훑기는 **손으로 열 때만**(화면 복귀·조작판 전환으로 열릴 땐 안 나온다). 리빌 트리거를 상태 전환 감지에서 탭 핸들러로 옮겼다.

**2차(같은 날)** — 벤치를 걷고 사용자가 방향을 직접 줬다: **확정 근무만 · 가까운 순 · 최대 3건 · 날짜 위/포지션·시간 아래 · 오른쪽 알약 `확인` → 포지션 배치도 모달 · 맨 아래 「전체 일정 보기」.** 그대로 기기에 넣었다 — 모달은 바닥 시트(260ms 슬라이드 + dim), 포지션별 명단에 내 이름 파랑. 모집·신청 행이 홈에서 빠지는 게 딸려온 결정이고(위 개정 제안 참조), 마감 임박(D-2 파랑)을 홈에서 잃는다 — 공석·새 모집 알림이 그 역할을 이어받는지 확인 필요. 같은 판에서 발치 아래 여백(card-week 하단 4 → 16)과 블러 5 → 3.5px도 조정했다(사용자). 조작판 「모집」 그룹은 「다가오는 근무」(3건/1건/없음)로 바뀌었다.

### 라운드 23 — 급여 블록 (2026-08-17, 확정)

"상하 마진·패딩이 부자연스럽다"는 지적과 행 앵커 그림 셋의 낱개 검수 미결을 한 판에 걸었다.

**3차(같은 날, 확정)** — 사용자 승인("너무 좋아 이렇게 홈은 고정") — 여백 가 · 컬러풀 그림
셋(보라 포함) 확정, **홈 화면 고정**. 수정 하나 — 리빌 빛 훑기(1.5초)가 도는 중에 다시 가려지면
애니메이션이 남아 블러 더미 위를 훑었다. **가림 적용 시 `revealing`을 즉시 걷어 재가림 순간
빛이 사라진다**(사용자 지시). 「가려진 동안에는 애니메이션이 없다」(라운드 22)의 마무리로 확정
규칙 리빌 항목에 반영.

**2차(같은 날)** — **여백은 가(현행) 확정** — 행 상하 12 · 라벨-값 1 · 카드 상하 4 그대로,
행 규칙 개정 없음. 비교판은 걷었다. 그림은 1차의 회백 세 벌이 되돌아왔다("현재 사용하고 있는
이모지와 맞췄으면, 흰색·회색 같은 색보단 좀 컬러풀한 게") — 알림 그림과 같은 채도로 다시 그렸다:
지난주 **초록 지폐 + ₩**(기존 `green` 그라디언트 재사용, 애플 `💵` 짜임) · 8월 **호박·파랑·초록
막대 셋**(전부 기존 그라디언트, 애플 `📊` 짜임) · 누적 **보라 오름 꺾은선 + 화살촉**(선 굵기는
`🔄`와 같은 4.2, 종이 없이 선 자체로 선다). 보라(#b18cff→#6a3bd8)는 새 고정색 — 애플 `📈`의
빨간 선은 「빨강 금지」에 걸려 못 쓴다. 배경 블록 없음 어법은 유지.

**1차** — ① **여백 세 안**을 조작판 「급여 여백」으로 걸었다 — 가(현행: 행 상하 12 · 라벨-값 1 ·
카드 상하 4) · 나(행 14 · 라벨-값 2 — 두 줄 행이 한 줄 더보기 12와 리듬을 가른다) · 다(행 12
유지 · 카드 상하 8 — 모서리와의 간격만 벌린다). 기본 선택은 나. **나·다를 고르면 행 규칙(행
상하 12) 개정이 딸려오고 다가오는 근무의 두 줄 행에도 같은 값이 적용된다.** ② **그림 낱개
검수** — 현행 셋을 크게 펼쳐 문제를 적었다: 지난주 동전 더미는 새긴 통화 기호(가로 두 줄 +
세로 한 줄)가 ¥로 읽히고(원화는 ₩) NOTES의 「지폐」 기록과도 어긋난다; 8월·누적의 라운드 사각
배경은 「배경 블록 없이 그림 자체로 선다」 어법 위반이라 앱 아이콘처럼 읽히고, 32px 파랑 판은
출근 버튼의 파랑과 경쟁한다. 어법대로 다시 그린 세 벌(회색 지폐 + ₩ · 파랑 막대 셋 · 흰 종이 +
회백 꺾은선 + 오름 화살촉, 종이 계열은 0.8 테두리)을 「급여 그림」 토글로 나란히 냈다. 기본
선택은 다시 그림.

### 라운드 24 — 계층 재확정 (2026-08-17, 확정)

홈이 고정되자 보류돼 있던 라운드 17 표 셋을 지금 화면의 실제 값으로 맞춰 다시 냈다. 큰 변경
셋 — ① 무게 열이 라운드 19 감량을 따라 700에서 600·500으로 내려왔다. ② 26/34는 **카운트다운
하나**다(주급이 18로 내려와 「최대 두 곳」이 낡았다). ③ 두 줄 순서 규칙에 **한 줄 좌우**가
세 번째 꼴로 들어왔다(라운드 21 발치).

표를 화면 코드와 한 줄씩 대조하다 **어긋난 곳 셋**을 찾았다.

| 어디 | 표에 있던 것 | 화면의 실제 | 어느 쪽으로 |
| --- | --- | --- | --- |
| 알약 `확인` | 14/20 600 행동 단계 | 13/18 600 | **둘 다 낮췄다** — 크기는 화면대로 13/18(높이 34에 14/20이면 눌린다), 무게는 라운드 26이 400으로 더 내렸다 |
| 더보기 행 | 14/20 600 | 14/20 400 회색 | **화면대로** — 「전체 일정 보기」가 파란 버튼과 같은 무게일 이유가 없다 |
| 배치도 모달 명단 | 표에 없던 자리 | 15/22 — 스케일 밖 | **규칙대로** — 16/24로 올리고 내 이름은 600 → 500 파랑 |

같은 판에서 죽은 CSS도 걷었다 — `.amount-big`(26/34 700) · `.datebox` · `.lrow` · `.spec` ·
`.paybench` · `.pcol-sub` · `ring` 그라디언트 · `#payCard`. 폐기된 배치의 잔해라 두면 다음
사람이 살아 있는 규칙으로 읽는다. 라운드 23의 그림 비교 토글(`급여 그림`)도 확정본만 남기고
걷었다.

**사용자 승인("내가 마지막 한 걸로 확정")으로 위 「계층」 절이 확정 규칙이 됐다.** 표에 맞춰
시안 코드도 이미 고쳐져 있어 이 확정으로 딸려오는 코드 변경은 없다. 남는 것은 문서 개정
둘뿐이고 개발 단계 몫이다 — `FOUNDATIONS.md:53`의 굵기 목록에 **500**을 들이는 것과, 스케일
표의 굵기 열을 라운드 24 표로 대체하는 것.

### 라운드 25 — 헤더 블러 (2026-08-17, 확정)

"홈 알림 헤더가 스크롤 내려갈 땐 밑에 있는 아이템이 블러 효과로 보일 수 있도록"(사용자).
헤더가 flex 형제라 지면을 잘라 콘텐츠가 그 선에서 사라지던 것을, 헤더를 콘텐츠 **위에 띄우고**
뒤를 흐리게 받는 짜임으로 바꿨다. 상태 표시줄(24) + 헤더(56)를 한 레이어 80으로 묶어
`phone-body`의 위 여백이 80이 됐다. 값은 위 「앱 셸」 절이 갖는다.

**2차(같은 날)** — "조금 더 옅게"(사용자). 흐림 14 → **9px**, 덮는 지면색 68 → **50%**.
뒤 카드의 윤곽이 더 살아난다.

`FOUNDATIONS.md`에 블러 항목이 없다 — 금액 숨김 7px(라운드 18)과 이 헤더 9px 둘 다 문서
개정 대상이다. 둘은 목적이 다르다: 숨김은 **읽지 못하게** 하는 것이고 헤더는 **뒤에 뭔가
있다고 알리는** 것이다.

### 라운드 26 — 알약 무게 (2026-08-17, 확정)

"확인 버튼의 font-weight가 너무 커"(사용자). 알약을 600 → **400**으로 내렸다. 크기 13/18은
그대로다. 라운드 24 계층 표의 13/18 줄과 「어긋났던 곳」 표를 함께 고쳤다.

### 라운드 27 — D 배지 급함 층위와 오늘의 포지션 (2026-08-17, 확정)

"D-1 D-2는 우리 브랜드 컬러를 살짝살짝 좀 더 색을 다르게 해서 넣어"(사용자). 배지가 전부
회색이라 내일 근무와 다음 주 근무가 같은 무게로 보였다.

- **D-2까지만 파랑 틴트다.** D-1은 `--color-action-tint-strong` `#cfe0fc`(새 토큰) + `#0c3f9c`
  글자, D-2는 기존 `--color-action-tint` `#e4edfd` + 같은 글자, **D-3부터 회색**이다.
  **진한 `#0052ff`는 쓰지 않는다** — 그 파랑은 눌러야 할 버튼 하나의 것이다(라운드 1).
  라운드 13의 「급하면 파랑, 아니면 회색」 두 갈래를 세 갈래로 나눈 것이다.
- **D-0은 만들지 않는다.** 배지 안 셋(잉크 채움 · 파랑 채움 · 틴트)을 벤치에 냈다가 전부
  걷었다 — 사용자가 물음 자체를 다시 잡았다("오늘에 붙여서 포지션 확인을 할 수 있도록 하는
  방향으로, 어차피 저거 있으니까"). **오늘 근무는 「다가오는 근무」에 들어오지 않는다.** 같은
  근무를 두 블록이 말하면 홈이 자기 말을 반복하는 셈이라, 이 블록은 **내일(D-1)부터 시작한다.**
- **오늘의 포지션 배치도는 「오늘」 카드의 발치가 연다.** 아바타 줄을 버튼으로 만들고 캡션을
  「4명 · 예식 14:00 · 포지션 보기」로, 오른쪽에 화살표 20을 붙였다. 열리는 것은 라운드 22가
  만든 **같은 바닥 시트**다 — 모달을 한 벌 더 만들지 않았다. 라운드 14~16의 발치(아바타 +
  예식 시각)가 이 라운드에서 목적지를 얻은 것이고, 라운드 22의 「배치도 모달은 다가오는 근무
  행이 연다」가 「오늘 카드도 연다」로 넓어졌다.
- 기기의 세 행을 D-1(8/19 화) · D-2(8/20 수) · D-4(8/22 금)로 바꿔 세 층위가 한눈에 보이게 했다.

### 라운드 28 — 일정 화면, 담음 표시 (2026-08-17, 제안)

홈이 닫혀 다음 화면으로 넘어왔다. 라운드 20이 「일정 셀 어법(상태 여섯)은 일정 화면 라운드의
몫」으로 미뤄둔 빚을 여는 판이다.

**1차에서 과녁을 잘못 잡았다.** 셀 어법을 세 안(숫자만 · 라벨 한 줄 · 달력에서 신청 제거)으로
다시 열었는데, 사용자가 "예전에 한번 정했던 것 같은데"라고 짚었고 맞았다 —
`interviews/2026-08-16-design-system-overhaul.md:15`가 **「달력 셀은 숫자 하나만, 범례·`open`
표시용 점·셀 안 배지를 모두 뺀다, 오늘은 숫자 아래 밑줄」**을 이미 사용자 선택으로 정해뒀고
라운드 1이 확정 규칙으로 옮겼다. 같은 문서 20~21줄이 **두 뷰 구조(기본 달력)**와 **마감 묶음
목록**도 닫아뒀다. 즉 라벨 안과 목록 소유 안은 **부결된 방향을 다시 낸 것**이라 걷었다.
라운드 20이 남긴 빚은 「어법을 고르는 것」이 아니라 **「이미 고른 어법을 여섯 상태에 적용하는
것」**이었다.

**2차에서 물음을 다시 잡았다.** 1차 정정 뒤에도 「담았다」를 어떻게 말할지로 두 안을 냈는데,
사용자가 "안 복잡하게, 지금 상태가 너무 많다"고 짚었다. 안을 고르는 게 아니라 **상태를 줄이는**
쪽으로 갔다 — **여섯을 넷으로.**

**수단을 둘로 나눈다 — 채움은 내가 관여한 날, 숫자 밝기는 지금 할 수 있는 정도.** 셀에 글자를
못 쓰니 두 축(홀의 사정 · 내 사정)을 두 수단에 갈라 맡긴 것이고, 둘이 서로 간섭하지 않아
범례가 필요 없다.

- **합친 것 ① 담음 = 신청함.** 「아직 저장 전인가」는 **하단 바가 이미 소유한다** — 「N일
  변경했어요 · 신청하기」가 떠 있으면 저장 전이고 저장하면 바가 사라진다. 셀이 한 번 더 말할
  이유가 없다. 담는 순간의 눌림-되튐(420ms)이 그 자리에서 답한다. 목록 알약도 같이 합쳤다 —
  「신청」/「신청함」 둘뿐이다.
- **합친 것 ② 마감은 판을 안 쓴다.** 회색판은 라운드 1의 「구획은 배경색 차이로만, 선·판 없음」과
  이질적이었고 파랑 채움과 경쟁했다. 숫자를 한 단계 흐리게(muted) 하면 충분하다.
  `WORKER-FLOWS.md:53`의 「흐리게 막지 않고 상세를 열 수 있어야」는 **disabled를 금지한 것**이라
  눌리기만 하면 지킨다. 판이 사라져 **채움은 파랑 하나의 뜻**만 갖는다.
- 숫자 밝기 셋 — 신청 가능 검정 500 · 마감 muted 400 · 모집 없음 흐림 400(누를 수 없음).
  내가 넣은 날과 확정은 `#0c3f9c` 500.

**3차에서 취소가 빠져 있던 것을 잡았다**(사용자: "신청하기도 있지만 취소하기도 같이 되는 거잖아,
어떤 게 +되고 어떤 게 −되는지 시각적으로 판단할 수 있었으면"). `신청하기` 한 번은 추가와 취소를
같이 담는데(`useApplicationBatch.ts:152-153`의 `applyDates` · `withdrawDates`) 2차 시안은 취소한
날을 「신청 가능」으로 되돌려버려 **내가 뭘 뺐는지 흔적이 없었다.** 축을 둘로 갈랐다 — **최종
신청 상태**(안 함 · 신청 · 확정)와 **이번에 바꾼 것**(＋ 추가 · − 취소). 후자를 어느 수단에
맡기느냐로 두 안.

| 안 | 이번에 바꾼 것 | 평소 신청함 | 대가 |
| --- | --- | --- | --- |
| **가 — 네모칸** | 손댄 칸에만 네모(파랑 추가 · 회색 취소). 저장하면 걷힌다 | **날짜 색만** | 라운드 1의 「신청함 `#f2f6fd`」 배정이 없어지고, 채움이 **「확정」과 「편집 중」 두 뜻**을 갖는다 |
| **나 — 테두리** | 손댄 칸에 2px 테두리(파랑 추가 · 회색 취소) | 채움 `#f2f6fd` | 셀의 수단이 **셋**이 된다(채움 · 숫자 색 · 테두리) |

- 가는 사용자가 직접 준 방향이다("선택한 것은 뒤에 배경 네모칸, 신청한 부분은 날짜 색상만") —
  홈 「이번 주」 스트립의 선택 어법과 같은 뜻이 된다. 기본 선택은 가다.
- **어느 쪽이든 하단 바가 둘을 따로 센다** — 「신청 +2 · 취소 −1」. 개수만 세던 실물
  (`0개 변경`)에서 방향이 갈라진 것이고, 목록 알약도 넷이 된다(＋ 신청 · − 취소 · 신청함 · 신청).
- **오늘 밑줄을 잉크색으로 고정했다.** `currentColor`라 오늘이 모집 없는 날이면 밑줄까지 흐려져
  거의 안 보였다(사용자가 "밑줄이 오늘이란 뜻인가"라고 물은 것이 그 증상이다). 폭 14 → 18.
  실물의 파란 링은 여전히 안 쓴다.

**4차 — 고친 것 셋과 취소 세 안**(사용자: "선택하면 날짜가 왜 저렇게 되지 · 지나간 확정은 회색
백그라운드 · 신청과 취소는 초록 빨강 · 취소할 때 달력에서 저렇게 나오는 건 아닌 것 같다").

- **담는 순간 날짜가 파래지던 것을 걷었다.** 네모칸이 「이번에 바꿈」을 말하는데 숫자 색까지 같이
  움직여 한 가지를 두 번 말하고 있었다. **추가해도 숫자는 검정**, 네모칸만 생긴다. 정리된 뜻 —
  **파랑은 서버에 남은 것**이고 저장하고 나서야 파랑이 된다. 3차의 「네모냐 테두리냐」 두 안은
  네모로 닫혔다(사용자 지시).
- **지나간 확정을 갈랐다** — 회색 채움 `#f2f4f6` + muted 숫자. 이미 끝난 근무가 「지금 할 일」
  색을 가질 이유가 없다. 달력 발치도 「지난 근무 N회 · 확정 N회」로 나눠 센다.
- **＋/− 를 초록 `#0d6b3c` · 빨강 `#c23934`으로**(사용자 지시). **승인 규칙 둘과 부딪힌다** —
  `COMPONENTS.md:61`(「danger는 오류·위험에만, `취소됨` 같은 업무 상태는 neutral」)과 라운드 7
  (「빨강은 진짜 오류에만」). 취소는 오류가 아니라 정상 행동이다. **범위를 하단 바와 목록 알약으로
  좁히고** 달력 셀은 파랑·회색을 지켰다. 개정 목록에 행을 올렸다.

**취소 표시 세 안** — 3차의 회색 네모가 「이 날은 회색 상태다」로 읽혀 실제 뜻(**지금 빼는 중**)과
어긋났다. 갈리는 것은 **달력이 취소를 말하느냐, 말한다면 무엇으로냐**다.

**5차 — 규칙 두 줄로 닫았다**(사용자: "취소는 색이 빠지는 거 · 선택되면 네모 박스 · red가 걸리면
넘어가지 마, 취소는 회색 · 17 19는 왜 네모박스가 걸려 있나, 확정이면 날짜 색상만 · 지난 확정도
선택이랑 헷갈리니 색상으로만"). 취소 세 안 중 **나(색이 빠지는 중)**로 닫혔고, 나머지 지적이
채움의 뜻을 하나로 정리했다.

**7차에서 규칙이 세 줄로 닫혔다**(사용자: "취소도 네모박스 색은 동일하게 하고 글씨를 mute 쪽으로 ·
확정을 수정하는 게 좋겠다" → "확정된 날짜는 밑에 동그란 표시를 하는 게 어때"). 5·6차의 두 줄
규칙에서 **박스 색이 방향을 지던 것**을 걷고 **숫자 색**에 넘겼으며, 확정에 **점**을 줬다.

**① 선택하면 뒤에 네모박스가 생긴다.** 추가든 취소든 **이번에 손댄 칸**은 전부 **같은 파랑 박스**
`#cfe0fc`를 얻는다 — 앱 전체가 쓰는 선택 어법이다(하단 탭·홈 「이번 주」 스트립과 같은 되튐
420ms). **② 박스 안 숫자 색이 방향을 말한다** — 검정이 넣는 중, muted가 빼는 중.
**③ 근무하는 날은 숫자 아래 점이 붙는다** — 앞으로의 확정은 파란 점, 지나간 근무는 회색 점.

| 상태 | 네모박스 | 숫자 | 점 |
| --- | --- | --- | --- |
| 모집 없음 | — | 회색 흐림 `#a8acb3` (안 눌림) | — |
| 마감 | — | 회색 muted `#7c828a` | — |
| 신청 가능 | — | 검정 500 | — |
| 신청함 | — | 파랑 `#0c3f9c` 500 | — |
| **확정** | — | 파랑 `#0c3f9c` 500 | **파란 점 5px** |
| **지난 근무** | — | 회색 진함 `#5b616e` | **회색 점 5px** |
| **＋ 선택 — 넣는 중** | **파랑 `#cfe0fc`** + 되튐 | 검정 500 | — |
| **− 선택 — 빼는 중** | **파랑 `#cfe0fc`** + 되튐 | **muted `#7c828a`** | — |

- **박스 색을 하나로 합쳤다**(사용자 지시). 6차는 박스가 파랑(추가)·회색(취소) 둘이었는데,
  박스가 **「이번에 손댄 것」** 하나만 뜻한다면 색이 갈릴 이유가 없다. 방향은 숫자가 진다 —
  넣는 중이면 앞으로 검정으로 살 날이니 검정, 빼는 중이면 곧 없어질 날이니 muted다.
- **확정에 점을 줬다. 라운드 1의 「셀에 배지·점·범례를 넣지 않는다」와 정면으로 부딪히는
  예외다.** 받아들인 근거 — 점의 뜻을 **하나로 못 박는다.** 점은 **「내가 근무하는(했던) 날」**
  이고 다른 어떤 상태도 점을 못 갖는다. 원래 금지의 과녁이던 「`open` 표시용 점」·범례는 그대로
  금지다. 개정 목록에 행을 올렸다.
- **점이 들어오면서 파랑이 하나로 줄었다.** 6차가 만든 `--color-action-mid` `#2f62c4`를 **도로
  걷었다** — 신청함과 확정을 숫자 농도로 가르던 일을 점이 대신하므로 숫자는 `#0c3f9c` 하나면
  된다. **이 라운드가 더한 새 색이 없다.**
- **채움이 셀에서 상태를 말하는 일을 그만뒀다.** 라운드 1의 「신청함 `#f2f6fd` · 확정 `#e4edfd`」
  배정이 셀에서는 없어진다. 채움은 **선택 하나**의 것이고, 상태는 숫자 색과 점이 진다.
  개정 목록에 행을 올렸다.
- **점과 오늘 밑줄은 층이 다르다** — 밑줄은 숫자에 붙고(`.dnum` 안, 2px 잉크색) 점은 **박스 밖
  아래**(`bottom: 4px`)다. 오늘이면서 확정인 날에 둘이 같이 서도 안 겹친다(시안의 8월 17일).
  셀 높이 46 → 50.
- **흐린 파랑(opacity)을 걷었다**(6차, 사용자: "시인성이 너무 안 좋다"). 5차는 신청함 55% ·
  취소 28%로 농도를 만들었는데 옅은 파랑이 회색과 구별되지 않고 작은 숫자에서 더 안 보였다.
  실제 색으로 갈랐고, 7차에서 그마저 하나로 줄었다. 브랜드 원색 `#0052ff`는 여전히 안 쓴다
  (라운드 1 — 버튼 하나의 것).
- **취소도 박스를 얻는다**(6차, 사용자: "선택되면 뒤에 백그라운드 네모 박스가 생기는 게 우리
  디자인 규칙"). 5차가 취소에서 박스를 뺐던 것을 되돌렸다 — 박스의 뜻이 「확정 상태」가 아니라
  **「이번에 손댄 것」**이라 추가·취소 양쪽에 붙는 게 규칙에 맞다. 4차의 회색 박스가 어색했던
  진짜 이유는 박스가 아니라 **숫자까지 muted로 죽인 것**이었다 — 7차에서 그 muted가 **선택된
  칸에서만** 뜻을 갖는 자리로 돌아왔다.
- **지난 근무는 채움을 잃었다**(6차, 사용자: "지난 확정일자들도 선택이랑 헷갈리니까 색상으로만
  구분하자"). 4차의 회색 채움 `#f2f4f6`을 걷었다. 회색 축은 **지난 근무 `#5b616e` → 마감
  `#7c828a` → 모집 없음 `#a8acb3`** 순으로 흐려진다. **지난 근무가 파랑이 아닌 근거** — 라운드
  1의 「가장 강한 파랑은 지금 눌러야 할 것」에 따라 파랑은 앞으로의 일이고 지난 것은 색을
  잃는다. 점만 남아 「일한 날」을 지킨다.
- **빨강을 걷었다**(사용자). `COMPONENTS.md:61`·라운드 7과 부딪히던 것이 사라졌다 — 취소는 muted
  회색이다. 초록도 새 색이 아니다: `globals.css:16`의 `--color-success` `#087a4b` 그대로이고,
  쓰이는 자리는 **하단 바와 목록 알약의 「신청 +N」**뿐이다(사용자가 "밑에서 시각적 효과를"이라
  한 그 자리). 달력 셀에는 초록이 없다.
- 3차의 「네모냐 테두리냐」, 4차의 취소 세 안은 모두 걷었다.
- **오늘은 밑줄 2px**(라운드 1 유지). 실물의 파란 링은 안 쓴다 — 링이 「선택」과 겹쳐 읽히는데
  이 화면에는 진짜 선택이 따로 있다. 홈 스트립이 밑줄을 뺀 것은 위 「오늘」 카드가 이미 오늘을
  말해서였고(라운드 20), 달력에는 그런 카드가 없다.
- **목록은 마감 묶음을 행 어법으로 옮겼다.** 묶음 머리행은 D 배지(라운드 27 급함 층위 그대로 —
  마감이 내일이면 진한 틴트) · 마감 날짜 값 · 알약 「모두 담기」. 그 아래 날짜 행은 **앵커를
  비운다** — 카드가 이미 마감 하나로 묶여 있어 행마다 배지를 두면 같은 말을 다섯 번 한다.
  홈과 다르게 가는 것 둘: ① **절대 날짜를 적는다**(라운드 10~11의 「신청 마감까지」 통일은 홈
  몫이고, 여기는 마감일을 확인하러 오는 화면이다) ② **행이 한 줄이다**(모집 단계엔 포지션도
  예식 시각도 없어 아랫줄에 적을 것이 없다). 값 16/24는 라운드 22 예외를 그대로 잇는다.
- **하단 변경 바는 변경이 있을 때만 올라온다**(240ms). 실물은 `0개 변경` + 비활성 버튼을 늘
  띄워 화면 아래 84를 상시로 먹는다. `PATTERNS.md:15`가 하단 고정을 「깊은 작업」에만 허용하는
  것과도 이쪽이 맞는다 — 상시 점유가 아니라 담는 동안만이다.
- **세그먼트**는 지면 위 알약이다 — `surface-weak` 통 안에 선택 칸만 흰색, 14/20 선택 500 ·
  비선택 400 muted. 계층 표 14/20 줄에 이 배정이 는다.
- 시안의 달은 **실제 2026년 8월**이다(1일 토, 오늘 17일 월). 홈 목 데이터는 「8월 18일 월요일」로
  요일이 실제 달력과 어긋나 있는데, 홈은 확정본이라 건드리지 않고 달력만 실제 요일을 따랐다 —
  달력은 요일이 틀리면 화면 자체가 깨져 보인다.

#### 8차 이후 — 셀을 닫고 화면 전체로 넘어갔다 (같은 날)

- **점을 숫자에 붙였다**(`bottom` 4 → 7, 칸 높이 50 → 46). 판(38) 바로 아래라 숫자 덩어리로 읽힌다.
- **손댈 수 없는 날을 회색 하나로 합쳤다** — 모집 없음 `#a8acb3` · 마감 `#7c828a` · 지난 근무
  `#5b616e` 셋을 **`#7c828a` 하나**로. 농도 차가 뜻을 안 실어 화면만 어지러웠고, 지난 근무는
  **점**이 이미 말한다. 누를 수 있는지는 색이 아니라 **되튐 유무**가 진다.
- **달력 발치 문장을 셀 표시와 같은 갈래로 맞췄다.** 넷을 셋으로 줄이고 순서를 **신청 가능 →
  신청 → 근무**로 뒤집었다(사용자: 급한 순). 「8월 ·」 접두는 걷었다 — 달력 머리가 이미 달을
  말한다. **값이 0인 갈래는 안 나온다.**
- **발치 숫자는 저장된 것만 센다**(사용자: "신청을 해야 변하는 거야"). 앞 판은 담기만 해도
  숫자가 움직여 안 낸 것을 낸 것처럼 셌다. 역할이 셋으로 갈렸다 — **셀**은 손대는 중까지,
  **발치**는 서버에 남은 것만, **하단 바**는 그 차이만.

#### 오늘 표시 — 네 판 만에 닫혔다

밑줄(라운드 1) → 판만 두른 잉크 테두리 → 이번 주 밴드 → 브랜드 파랑 테두리 → **칸 전체를 두르는
실선 1px `#e5e8eb`**. 물음을 바꾼 자리가 있다 — **셀 안이 포화**라서(채움=선택 · 숫자 색=상태 ·
점=근무) 무엇을 얹어도 시끄럽거나 안 읽힌다는 것.

- 밑줄이 안 읽힌 이유는 색이 아니라 **자리**였다. 숫자 밑 2px은 오늘 표시가 아니라 글자 장식으로
  읽힌다.
- **칸을 통째로 두르니 숫자와 점이 한 덩어리로 그 안에 든다.**
- 브랜드 파랑 `#0052ff`는 사용자 지시로 한 번 들어갔다가 **"너무 도드라진다"**로 내려왔다.
  결과적으로 **라운드 1의 「`#0052ff`는 버튼 하나의 것」이 그대로 산다.** `#e5e8eb`는 라운드 1이
  「카드 안 실선」으로 이미 허락한 색이라 **새 색이 안 늘었다.**
- `inset box-shadow`라 칸 크기가 안 밀린다. 더 잡히게 하려면 `#a8acb3`까지가 한계다.
- **`COMPONENTS.md:64-85`의 「오늘은 action outline」이 뼈대째 살아났다.**

#### 확정 날짜를 누르면 — 배정표 바닥 시트

`WORKER-FLOWS.md:68-71`이 이미 닫아둔 것을 그대로 옮겼다 — 기본 펼침 · 포지션 단위 묶음 ·
제목엔 이름만 · 구분선 아래 교육생 · 포지션 고정 순서 아홉.

- **여는 자리는 라운드 22의 바닥 시트**다. 라운드 27이 「오늘」 카드 발치에 같은 시트를 물렸으니
  **배정표를 여는 문이 앱에서 하나**가 된다. 지난 근무도 같은 시트다.
- **`:71`과 다르게 간다.** 문서는 「본인 행은 action tint」인데 **라운드 22 확정본이 파랑 500
  글자**로 이미 정해뒀다. 시트가 하나라 두 어법을 못 쓴다.
- **시트를 새로 그렸다가 되물렀다.** 그립 막대·18/26 제목·라디우스 박스 행·틴트 본인 행·발치
  파란 버튼으로 만들었는데 `confirmed/home.html`과 전부 어긋났다. 라디우스 박스 행은 라운드 1의
  「선·판 없음」과도 부딪혔다. **확정본을 열어 그대로 맞췄다.**
- **닫기는 발치 버튼에서 머리 ✕로** 옮겼다(사용자). 겹겹 시트의 `뒤로`도 머리 왼쪽 ←다.
  **덤으로 시트에서 파란 버튼이 사라졌다.** **홈 확정본도 같이 고쳤다.**

#### 근무 변경 요청 — 취소와 포지션 교대

- `WORKER-FLOWS.md:78-80`대로 **`근무 변경 요청` 하나**로 묶었다. 파란 버튼이 아니다.
- **두 「취소」의 문을 갈랐다.** 마감 전 신청 취소는 하단 바가 처리하고 확인창이 없다(`:54`).
  확정 근무 취소는 관리자 확인을 거친다.
- **「요청 중」을 다섯째 상태로 안 늘렸다.** 점의 **속을 비웠다**. 라운드 1의 「채움이 진할수록
  확정에 가깝다」 그대로라 **뜻이 하나 늘 뿐 기호가 안 는다.**
- **포지션 교대는 상대를 고르는 겹이 하나 더 붙는다.** 본인 · 같은 포지션 · 교육생 · **내가 맡을
  수 없는 포지션**을 걷어낸 뒤 남는 사람만 오른다. 못 고르는 이름을 흐리게 두지 않고 아예 빼고
  머리에 이유를 적었다.
- **문구를 두 번 손봤다.** 관리자 문의 안내는 기능이 생기면 거짓말이라 걷었고, 그다음 머리
  캡션이 발치와 같은 말을 반복해 **머리는 날짜만** 지게 했다.

#### 화면 구조가 뒤집혔다 — 마감은 한 달에 하나다

**사용자 정정**: 여러 스케줄을 조금씩 열지 않는다. **7월 말에 8월치를 쭉 받는다.** 보통 마감
하나, 가끔 추가 모집. 그전까지 시안은 **마감 묶음 셋**을 전제로 지어져 있었다.

- **목록을 통째로 걷었다가 되살렸다.** 마감이 하나면 「마감별 묶음」이 성립을 안 해 목록이
  달력의 열화판이 된다 — 세그먼트까지 걷고 달력 하나로 갔다(사용자 선택). 그 뒤 **"목록 버전도
  있었으면"**으로 되살아났고, 이번엔 **다른 축**을 갖고 돌아왔다.

| | 달력 | 목록 |
| --- | --- | --- |
| 보이는 것 | 확정 · 지난 근무 · 신청 · 모집 **전부** | **모집만** (열린 것 + 지난 회차) |
| 답하는 물음 | 이 달에 얼마나 일하나, 어디가 비었나 | 지금 뭘 정해야 하나 |
| 조작 단위 | 날짜 하나 | **회차 전체**(「모두 선택」) |
| 순서 | 시간순 | **마감 임박순** |

- **머리행은 모집 기간이다** — `7월 28일 ~ 8월 18일`, 라벨 「신청 기간 · 근무 8일」, 왼쪽 D 배지.
  「마감」이라는 말을 걷었고(사용자), 날짜 두 개만으로는 **근무 기간인지 접수 기간인지** 모른다는
  지적에 라벨이 그걸 말하게 했다. 「8일 모집」도 같은 병이라 **「근무 8일」**로 고쳤다.
- **지난 모집은 접힌 채 아래로 쌓이고 무한 스크롤**이다(사용자). 배지 자리는 「끝」, 값 색은 한
  단계 낮다. 두 회차씩 붙고 다 떨어지면 「더 없어요」.
- **추가 모집은 마감 임박순으로 위**다(사용자). 달력 발치 문장도 가장 급한 마감을 말한다.

#### 달력 발치가 세 줄을 진다

```
신청 가능 8일 · 신청 2일 · 근무 7일        저장된 것만, 급한 순
8월 18일까지 신청할 수 있어요               가장 급한 마감. 날짜만 잉크 500
[ 8일 모두 선택 ]                          quiet 버튼
```

- 둘째 줄은 문장 `#5b616e`, **날짜만 잉크 500**. 크기는 13/18 그대로다 — 필요한 건 위계 승격이
  아니라 **같은 줄 안에서 어느 낱말이 중요한가**였다.
- **「모두 선택」은 숨지 않는다.** 열린 날이 없으면 **비활성 + 「선택할 날이 없어요」**다.
  사라지면 「원래 없는 화면인가」 싶고, 남으면 **왜 못 누르는지**가 그 자리에서 답이 된다.

#### 낱말 — 담는 말과 내는 말을 갈랐다

- **「모두 신청」 → 「모두 선택」.** 그 버튼은 담기만 하고 내는 건 하단 「신청하기」다.
- **알약 「＋ 신청」 → 「선택됨」**, **「신청함」 → 「신청 완료」**(사용자).
- **「모두 해제」가 취소를 만들던 것을 고쳤다.** 열린 날을 전부 비워 이미 신청해둔 날까지 **취소
  선택**이 됐다. 「해제」는 **이번에 담은 것만** 되돌리는 말이다.

#### 하단 변경 바 — undo를 걷었다

- **바가 탭바를 덮는다**(사용자). 담는 동안은 화면 이동이 아니라 **결정을 끝내는 게 할 일**이다.
- **저장하면 「신청이 완료되었어요」 토스트 하나**다. `WORKER-FLOWS.md:47-48`의 undo를 없앤다.

#### 홈 확정본과 대조해 고친 것

| | 이 화면이었던 것 | 홈 확정본 | 처리 |
| --- | --- | --- | --- |
| 행 값 | 16/24 | **18/26 500** | 홈으로. **앞 판의 「다가오는 근무는 16/24」는 내 오기였다** |
| 행 순서 | 값 위 · 라벨 아래 | **라벨 위 · 값 아래** | 홈으로 |
| `.button` | `padding: 0 22px` | 패딩 없음 | 홈으로. 다만 **하단 바 안에서만** 44/좌우 20 — 홈 버튼은 카드에 꽉 차 있어 패딩이 필요 없었던 것이라 그대로 걷으니 글자에 붙었다 |
| `.pill` | flex + gap 4 | grid | **그대로** — 크기·글자는 같고 알약 안에 ＋/− 아이콘이 들어와서다 |

- **목록 날짜 행은 16/24 400이다.** 홈의 18/26 500은 카드마다 **한 줄씩만** 서는 값인데 여기는
  여덟 줄이 이어져 같은 무게로 쌓으면 목록이 통째로 굵어진다. **머리행만** 18/26 500이다.
- 목록 카드 사이는 **12**다(앱 카드 간격).

#### 빈 상태를 이 라운드에서 정했다

- **열린 모집 없음**(확정 근무만 남은 달) · **아무것도 없음**(모집도 근무도 없는 달) 둘 다 발치
  둘째 줄이 **「지금은 열려 있는 모집이 없어요」**, 마감 카드 없음, 「모두 선택」 비활성, 목록도
  같은 문구. **뷰를 바꿔도 같은 답**이 나와야 해서 문구를 한 벌로 맞췄다.
- 윗줄 요약은 근무가 있으면 남고 아무것도 없으면 사라진다 — **없는 걸 「0일」로 세지 않는다.**
- 같은 지시로 `publish-ui` 스킬의 「대표 상태 하나만 그린다」를 뒤집었다(후속 절 참조).

**확정됐다**(2026-08-18, 사용자). `design/confirmed/schedule.html`로 세웠다. 아래 「일정 화면」
절이 확정 규칙이다.

### 라운드 29 — 홈의 빈 상태 ① 오늘 근무 없는 날 (2026-08-18, 확정)

홈은 라운드 23에서 닫혔지만 라운드 28이 **빈 상태를 시안에서 정하도록** 규칙을 바꿔
(`publish-ui` 스킬) 그 부분만 다시 연다. 다섯을 한 라운드에 하나씩 정한다 — **오늘 근무 없는
날**(이 판) · 급여 0원 · 다가오는 근무 없음 · 이번 주 근무 없음 · 알림 없음. 뒤의 넷은 이미
시안에 그려져 있어 검수가 몫이고, **오늘 블록만 아무것도 정해져 있지 않다.**

**확정 규칙 둘이 서로 부딪힌다** — 라운드 6이 「블록 자리는 항상 같고 안의 내용만 바뀐다 ·
데이터가 없는 블록은 사라지지 않고 없다고 말한다(알림만 예외)」를 정했는데, 같은 라운드 6·8이
「오늘 블록은 **근무 당일에만** 선다」도 정해뒀다. 시안 코드(`data-var="today:open|wait"`)는
뒤쪽을 따라 블록을 통째로 걷고 있다. 승인 문서가 아니라 **NOTES 안의 충돌**이라 개정 목록이
아니라 이 라운드가 하나를 고른다.

| 안 | 카드 안 | 대가 |
| --- | --- | --- |
| **가 — 한 줄** | 「오늘은 근무가 없어요」 16/24 400 quiet 한 줄. 카드 높이 56 | 가장 조용하다. 자리만 지키고 아래 넷의 말을 안 뺏는다 |
| **나 — 한 줄 + 발치** | 위에 같은 한 줄, hairline 아래 「다음 근무 8월 19일 화 · 홀서빙」 + 화살표 | **바로 아래 「다가오는 근무」 첫 행과 같은 말**을 한다. 라운드 27이 D-0을 안 만든 이유가 그것이었다 |
| **다 — 블록 걷기** | 없음. 「이번 주」가 두 번째 자리로 올라온다 | 라운드 6의 **「블록 자리는 항상 같다」**와 **「없다고 말한다」** 둘과 부딪힌다. 날마다 홈의 순서가 달라진다 |

기본 선택은 가였으나 **사용자가 다(블록 걷기)를 골랐다.** 확정 규칙 절의 「홈의 구조」에 반영했다 —
**통째로 접히는 블록이 둘이 됐다**(알림 · 오늘). 시안 코드는 이미 그 모양이라 되돌렸고, 이 판에서
만든 `.swap`은 걷었다. `.empty-line`(16/24 400 `--color-text`)만 남겼다 — 라운드 30이 쓴다.

**딸려 나온 것 둘.**

- **오늘을 말하는 자리가 근무 없는 날엔 화면에서 사라진다.** 라운드 20이 「이번 주」 스트립에서
  오늘 밑줄을 뺀 근거가 「위 「오늘」 카드가 이미 오늘을 말한다」였는데, 그 카드가 없는 날엔
  아무것도 오늘을 말하지 않는다. 일정 화면 달력은 라운드 28에서 **칸을 두르는 실선**으로 오늘을
  말하기로 했다. 라운드 32(이번 주 근무 없음)에서 같이 정한다.
- **「다가오는 근무 없음」 행의 무게가 어긋나 있다.** `.trow-value`(500)를 쓰는데 라운드 24 계층
  표의 16/24 줄은 **문장이면 400**이다. 「다가오는 근무가 없어요」는 문장이라 400이 맞다.
  라운드 31에서 `.empty-line`으로 맞춘다.

### 라운드 30 — 홈의 빈 상태 ② 급여 0원 (2026-08-18, 확정)

행 하나가 비는 것은 안이 갈리지 않는다 — **이미 정해진 규칙의 적용**이다.

- **0원이 아니라 「—」다**(라운드 6·23). 0원은 계산 결과처럼 보이는데 실제로는 계산할 근무가 없다.
- **「—」는 가릴 것이 없다** — 블러도 탭도 없고 `.amt`가 아니다. 라운드 18의 금액 가림 밖이다.
- **라벨에서 0인 갈래는 안 적는다**(라운드 28 달력 발치 규칙 재사용) — 「지난주 · 0회 0시간」이
  아니라 **「지난주」**다.
- **지난 달 fallback을 걷는다.** NOTES 급여 블록 표의 둘째 상태(「이번 달 확정 없음 → `7월 예상
  급여`로 이름이 바뀐다」)는 카드 하나였던 라운드 6의 것이다. 행 셋이 된 지금은 **「지난주」 행이
  달 초의 지난 달 돈을 이미 덮고**(8월 3일의 지난주는 7월 말이다), 가운데 행의 이름만 달을
  넘나들면 위아래 두 행과 기간이 어긋나 읽힌다. 자리는 **지난주 · 이번 달 · 연 누적**으로 고정한다.

갈리는 것은 **셋이 한꺼번에 빌 때**다 — 갓 들어온 근무자의 첫 화면이고, 같은 날 「이번 주」와
「다가오는 근무」도 함께 비어 있다.

| 안 | 모양 | 대가 |
| --- | --- | --- |
| **가 — 행 셋** | 세 행 그대로, 값 전부 `—`, 그림 앵커 유지 | 자리가 늘 같아 값이 차면 같은 줄에 들어찬다. 대신 **줄임표 셋과 컬러 그림 셋이 정보 없이 172px을 먹는다** |
| **나 — 한 줄** | 앵커 없는 행 하나 「아직 계산할 급여가 없어요」 + 「주별로 모두 보기」 | 블록은 남고 안의 내용만 준다 — 라운드 6의 「없다고 말한다」 그대로. 급여 블록 표 셋째 상태를 행 어법으로 옮긴 꼴. 행 셋이 접혔다 펴지는 전환이 하나 는다 |
| **다 — 앵커 없이** | 세 행 유지, 그림만 걷고 값 `—` | **「왼쪽 앵커 자리는 32×32 고정」(라운드 7)이 흔들린다.** 값이 차면 그림이 갑자기 나타나 행이 옆으로 밀린다 |

**사용자가 나(한 줄)를 골랐다.** 확정 규칙 절의 「급여 블록」에 반영했다. 조작판에 「급여」
(셋 다 있음 · 지난주만 없음 · 전부 없음)를 남기고 안 비교 토글은 걷었다.

### 라운드 31 — 홈의 빈 상태 ③ 다가오는 근무 없음 (2026-08-18, 확정)

**두 줄이 같은 곳으로 간다.** 빈 블록에 「없음」 배지 행과 「전체 일정 보기」 더보기 둘이 서는데
눌러보면 **둘 다 일정 화면**이다. 「한 행에 목적지 하나」(라운드 13)는 지켰지만 한 카드가 같은
목적지를 두 번 판다. 같은 판에서 **무게도 고친다** — 문장인데 `.trow-value`(500)라 라운드 24
계층 표(16/24 문장은 400)와 어긋나 있었다. `.empty-line`으로 맞췄다(안이 갈리지 않는 적용).

| 안 | 모양 | 대가 |
| --- | --- | --- |
| **가 — 「없음」 배지** | 현행. 배지 행(눌림) + 더보기 | 앵커 32×32가 늘 채워져 값이 차도 행이 안 밀린다. 대가 셋 — **배지에 낱말이 든다**(다른 배지는 전부 D-n 숫자) · 두 줄이 같은 곳으로 간다 · 「없음」을 두 번 말한다 |
| **나 — 앵커 없이** | 앵커 없는 문장 행(**안 눌림**) + 더보기 | 라운드 30 급여와 **같은 모양**이라 「빈 블록 = 앵커 없는 quiet 한 줄 + 더보기」가 앱의 규칙이 된다. 값이 차면 앵커 32가 나타나 문장이 오른쪽으로 밀린다 |
| **다 — 한 줄만** | 문장 행(눌림, 화살표)만. 더보기를 걷는다 | 가장 짧다. **급여 블록과 모양이 갈린다.** 일정으로 가는 길은 하단 탭에 그대로 있다 |

**사용자가 나(앵커 없이)를 골랐다.** 라운드 30과 같은 모양이 되어 **빈 블록 어법이 하나로 섰다** —
아래 「빈 블록」 절을 확정 규칙에 신설했다.

### 라운드 32 — 홈의 빈 상태 ④ 이번 주 근무 없음 + 오늘 표시 (2026-08-18, 확정)

**라운드 29가 빚을 남겼다.** 라운드 20이 스트립에서 오늘 밑줄을 뺀 근거는 「위 「오늘」 카드가 이미
오늘을 말한다」였는데, 라운드 29에서 **그 카드가 없는 날이 생겼다.** 근무 없는 날 홈에는 오늘을
말하는 자리가 하나도 없다.

발치(문장 + `—`)는 라운드 21에서 확정된 것이라 다시 열지 않는다. 갈리는 것은 **빈 주에 스트립을
남기나**와 **오늘을 무엇이 말하나** 둘인데, 스트립을 걷으면 오늘을 얹을 자리도 같이 없어져
**하나의 결정**이다.

| 안 | 모양 | 대가 |
| --- | --- | --- |
| **가 — 스트립 + 오늘 표시** | 스트립 유지 + **오늘 칸을 두르는 실선 1px `#e5e8eb`**(빈 주만이 아니라 **늘 켜짐**) | 일정 화면 달력(라운드 28)과 같은 표시라 **새 색이 안 늘고 어법이 하나**가 된다. **라운드 20의 「밑줄 삭제」를 개정한다** |
| **나 — 스트립만** | 현행. 흐린 숫자 일곱 + 발치 | 이번 주 범위는 읽힌다. 대가 — **근무 없는 날엔 홈 어디에도 오늘이 없다** |
| **다 — 한 줄로 접기** | 스트립을 걷고 앵커 없는 한 줄 + `—` | 급여·다가오는 근무와 **같은 한 줄 어법**이 된다. 대가 — **오늘을 얹을 자리가 아예 없어지고** 규칙이 하나 는다 |

**사용자가 가를 골랐다.** 위 「이번 주 블록」 절에 반영했다 — **오늘 표시가 스트립에 돌아왔고
빈 주만이 아니라 늘 켜진다.** 라운드 20의 「오늘 밑줄 삭제」를 개정한 것이고, 표시는 밑줄이 아니라
**일정 화면 달력과 같은 칸 두르는 실선 1px `#e5e8eb`**다(라운드 28). 새 색이 안 늘었다.

### 라운드 33 — 홈의 빈 상태 ⑤ 알림 없음 (2026-08-18, 확정)

블록이 접히는 것은 이미 정해져 있다(라운드 6·8) — 마지막 장을 끄면 260ms로 접히고 아래 블록이
올라온다. 남은 물음은 **헤더의 종 점**이다. 지금 코드는 홈 카드를 다 끄면 점도 같이 없애는데,
**홈에서 끈 것과 알림 화면에서 읽은 것이 같은 일인가**는 정한 적이 없다. 라운드 6~8의 표를 보면
**알림 화면에는 오는데 홈 블록에는 안 오는 알림**이 있다(마감 임박 · 다음 근무).

**셋째 안이 없다** — 갈리는 것이 모양이 아니라 낱말의 뜻 하나이고, 화면에 보이는 차이는 점 하나다.

| 안 | 점의 뜻 | 「끄기」의 뜻 | 대가 |
| --- | --- | --- | --- |
| **가 — 같이 사라진다**(현행) | 홈에 안 끈 카드가 있다 | 읽음 | 점이 거짓말을 안 한다(점 있으면 아래 카드가 있다). 대가 — **안 읽은 알림이 알림 화면에 남아도 점이 없다** |
| **나 — 남는다** | 알림 화면에 안 읽은 것이 있다 | 홈에서 치우기(그날 하루) | `X`가 지우기가 아니라 **미루기**가 되어 라운드 6~8의 「그날 하루는 다시 뜨지 않는다」와 말이 맞는다. 대가 — 다 껐는데 점이 남아 「왜 아직 점이 있나」가 나올 수 있다 |

**사용자가 나를 골랐다** — "알림 페이지로 넘어가서 읽거나 모두 읽음 처리 해야 사라지는 거야,
저걸로 사라지지 않아". 위 「알림 블록」 절에 반영했다. **「모두 읽음」이 알림 화면의 것으로
확인됐다** — 그 화면은 P0-T51 몫이라 여기서 모양을 그리지 않고, 종 점을 끄는 길이 거기 있다는
사실만 못 박는다.

**빈 상태 다섯이 다 닫혔다.** 확정본 `confirmed/home.html`을 라운드 33까지로 다시 세웠다.

### 라운드 34 — 로딩 · 오프라인 · 에러 전역 규칙 (2026-08-18, 확정)

화면마다 다시 묻지 않을 **전역 규칙 한 판**이다. 셋 중 둘은 승인 문서가 이미 소유하고 있어
**모양만 준다** — `PATTERNS.md:75-82`가 「처음 읽는 화면은 구조를 유지하는 skeleton」·
「오프라인이면 상단 고정 배너 + 모든 변경 행동 비활성화」·「오프라인 queue 없음」·
「복구되면 정본에서 다시 조회」를 정해뒀고, `COMPONENTS.md:137-141`이 배너 문구와 복구 피드백을
정해뒀다. **에러만 규칙이 없다** — `PATTERNS.md`의 「로딩, 오류, 오프라인」 절에 로딩·오프라인
줄은 있는데 **「서버가 실패했을 때」 줄이 없다.**

**적용한 것(안이 갈리지 않는다).**

- **아는 것은 가리지 않는다.** 화면 제목 · 블록 이름표 · 요일과 날짜는 서버 없이 아는 값이라
  skeleton으로 덮지 않는다. 회색 막대는 **서버에서 와야 하는 것**에만 깐다.
- **막대 색은 `--color-border` `#e5e8eb`**, radius 6(앵커 자리는 11, 아바타·알약 자리는 pill).
  흰 카드 위에서 `surface-weak`은 거의 안 보였다. 새 색이 안 늘었다.
- **반짝임(shimmer)을 넣지 않는다.** 움직임은 눌림·리빌·시트처럼 뜻이 있는 순간에만 쓴다
  (라운드 5·18·22). `FOUNDATIONS.md:145`가 반복 애니메이션을 허용하지만 **필수는 아니다.**
- **오프라인 배너는 헤더 레이어에 붙는다.** 라운드 25가 상태 표시줄(24)+헤더(56)를 한 레이어
  80으로 묶었으므로 배너(40)는 그 아래 셋째 줄로 들어가 **한 레이어 120**이 되고 콘텐츠가 그 뒤로
  지나간다. 색은 `--color-warning-surface` `#fff7d6` + `--color-warning` `#765500`
  (`globals.css:20-21` 기존 토큰). 문구는 「인터넷 연결이 끊겼어요 · 지금은 보기만 할 수 있어요」.
- **오프라인이면 출근 인증 버튼이 죽고 이유를 말한다** — 라벨이 「연결되면 인증할 수 있어요」로
  바뀐다. `COMPONENTS.md:141`의 「이유와 함께 비활성화」를 라벨로 지킨 것이다.
- **에러에 빨강을 쓰지 않는다.** 되돌릴 수 있는 실패이고 바로 옆에 「다시 시도」가 있다.
  `COMPONENTS.md:61`이 danger를 허용하지만 강제하지 않는다. 라운드 7의 「빨강은 진짜 오류에만」과
  일관되게 남긴다.

**갈리는 것 — 에러를 어디에 그리나.**

| 안 | 모양 | 대가 |
| --- | --- | --- |
| **가 — 블록별만** | 실패한 카드 자리마다 「불러오지 못했어요 + 다시 시도」 | 「블록 자리는 항상 같다」(라운드 6)를 끝까지 지킨다. 대가 — **다 실패하면 같은 말·버튼이 다섯 번 쌓이고 다시 시도를 다섯 번 누른다** |
| **나 — 화면 한 장만** | 콘텐츠를 걷고 가운데 한 장(헤더·탭바는 남는다) | 한 번만 말하고 버튼도 하나다. 대가 — **하나만 실패해도 멀쩡한 블록 넷을 같이 걷는다.** 오늘 출근 인증을 못 하게 된다 |
| **다 — 섞는다** | 일부 실패는 그 블록만(문구에 블록 이름), 전부 실패면 한 장 | 무엇이 없는지 그 자리에서 말한다. 대가 — **규칙이 둘이라 경계를 정해야 한다.** 시안은 **전부 실패일 때만** 한 장이다 |

**사용자가 다(섞는다)를 골랐다.** 아래 「로딩 · 오프라인 · 에러」 절을 확정 규칙에 신설했다.
**전역 규칙이라 다음 화면 라운드에서 다시 묻지 않는다.**

확정본은 `confirmed/home.html`이 갖는다(라운드 34까지). **`confirmed/schedule.html`은 이 셋을
그리지 않았다** — 라운드 28 판이라 그렇고, 전역 규칙이라 화면 계약이 아니라 위 절이 정본이다.
퍼블리싱 때 두 화면 모두 이 규칙을 따른다.

**같은 판에서 시안 버그 하나를 잡았다.** 조작판을 이 판에 필요한 한 그룹으로 줄이면서 버튼
셋(`resetNotices` · `skipToStart` · `skipPast`)을 걷었는데 스크립트가 그 ID를 계속 붙잡고 있었다.
`getElementById`가 `null`을 주고 `addEventListener`에서 터져 **뒤 코드가 통째로 안 돌아**
초기 `apply()`까지 못 갔다 — 조작판 전체가 죽었다. 죽은 리스너를 걷었다.
**조작판을 줄일 때마다 `getElementById` 참조와 실제 `id` 목록을 전수 대조한다.**

### 출퇴근 인증은 페이지로 넘어간다 (2026-08-18 사용자, 홈 쪽만 확정)

「출근 인증하기」를 누르면 **별도 페이지로 넘어가** 「현재 위치를 확인하는 중」 애니메이션과 내
위치를 보여주고, **끝나면 홈으로 돌아온다**(2026-08-18 사용자).

- **홈 쪽 계약만 이 task가 갖는다** — 버튼은 카드 안에서 상태가 바뀌는 것이 아니라 **화면을
  이동시키고**, 인증이 끝나면 홈으로 복귀한다. 복귀한 홈은 인증 뒤 상태(퇴근 카운트다운)로 선다.
- **인증 화면 자체는 P0-T49**(인증·상태 8 화면 묶음)다(2026-08-18 사용자 선택). 방향만 넘긴다 —
  별도 페이지 · 위치 확인 애니메이션 · 끝나면 홈 복귀.
- **「내 위치」는 지도가 아니라 핀 + 잔잔한 원**이다(2026-08-18 사용자 선택). `WORKER-FLOWS.md:96`이
  이미 정해둔 것이고 외부 의존이 없다 — 실제 지도 타일은 외부 호스트가 필요해
  `FOUNDATIONS.md:51`(자체 호스팅)과 부딪히고 시안으로 확인도 안 된다. 좌표·거리·정확도를 숨기는
  `WORKER-FLOWS.md:97`도 그대로 산다.

## 미결

- ~~`display` 32/40 단계를 FOUNDATIONS 표에서 뺄지~~ — 2026-08-18 전수 대조에서 닫혔다. **뺀다.**
  그 자리에 `caption-strong` 13/18 500이 들어가 8단계가 유지된다(개정 목록 `globals.css:109-155` 행).
- **`action`·`action-pressed` 의미 토큰의 배경·테두리를 어쩔지** — `FOUNDATIONS.md:32-33`이 정한
  `action` 배경 `#eef4ff` · 테두리 `#b8ceff`, `action-pressed` 배경 `#e2ebff` · 테두리 `#0052ff`를
  **홈·일정 시안이 한 번도 안 쓴다.** 시안의 파란 테두리는 `.spinner`(로딩)와 `.dcell.chg`(변경 요청
  빈 점, `action-deep`) 둘뿐이고 파란 채움은 전부 틴트 사다리다. 값을 새 틴트로 갈면 근거 없이
  찍는 것이고, 두면 죽은 값이 남는다. **화면 둘로는 판단이 안 선다** — P0-T49~T54가 파란 테두리
  표면을 쓰는지 보고 정한다. 그때까지 행은 손대지 않는다.
- **`--color-warn-tint` `#fdf0dd` · `--color-warn-deep` `#8a5300`** — 시안의 `.callout`(기기 밖
  주석 상자)만 쓴다. 앱 토큰이 아니라 이관 대상에서 뺀다.
- **모션 대역** — `FOUNDATIONS.md:137`은 「버튼·날짜 선택 120~160ms」뿐이다. 탭 눌림 420ms,
  알림 교체 280ms, 블록 접힘 260ms가 대역 밖이다. 「자리 이동 200~300ms」 행을 더하고 탭 눌림은
  따로 적는 것으로 정리해야 한다.
- **인증을 닫는 시각** — PRD에 없다. `PRD.md:301`은 "뒤늦은 과거 날짜 직접 등록은 허용하지
  않는다"뿐이라 그날 안이면 언제든 인증되는 셈이다. 「마감까지 남은 시간」을 세려면 마감 시각을
  먼저 정해야 하고 그건 PRD 몫이라 기획으로 돌려보낼 물음이다.
- **긴 이름 말줄임** — 출근 카드 오른쪽 포지션 이름, 알림 카드 두 줄 넘침. 기준값과 계산 위치는
  `model`이다.
- task를 가를지 — handoff의 미결 항목이 그대로 유효하다.
- **홈 수정 대기열(2026-08-17 사용자)** — ① 금액 가림(라운드 18 확정) ② 이번 주(라운드 20·21
  확정) ⑤ Bold 감량(라운드 19 확정) ④ 다가오는 근무(라운드 22 확정) ③ 급여 블록(라운드 23
  확정). **대기열 전부 소진 — 홈 고정**(2026-08-17 사용자: "이렇게 홈은 고정").
- **주급 기획 물음 셋** — 주의 경계(월요일 시작인지 급여 주기 시작일인지) · 월별 합계를
  남길지 주급으로 갈지 · 달에 걸친 근무(8/31이 9월 첫 주에 드는 경우)의 귀속.
  `PRD.md:346` 개정이 걸려 있어 기획 몫이다.
- **D 배지 낱개 검수** — 색 층위는 라운드 27이 정했고, 32px·radius 11이라는 형태 자체는 라운드 7
  수준의 검수를 안 받았다. 행 앵커 그림 셋은 라운드 23이 검수·확정했다.
- **「확정」이 누구의 확정인가**(라운드 28, 기획 반환) — `schedule-cell-state.ts:20-29`가 스케줄
  status를 먼저 보고 내 신청을 덮는다. 결과 둘 — ① **내가 신청한 날이 마감되면 화면에서 내 신청이
  사라진다**(`closed`가 `requested`를 덮는다). ② **확정된 날은 내가 배정됐는지와 무관하게
  「확정」**이라, 홈의 「다가오는 근무」(내 확정 근무만)와 개수가 안 맞는다. 이 화면은 배정을 알
  수가 없다 — 받는 데이터가 `listRecruitmentSchedules`(홀의 모집)와 `listOwnApplications`(내
  신청) 둘뿐이다. 색으로 풀 문제가 아니라 **화면이 무엇을 받아야 하는가**의 문제라 기획 몫이다.
  시안은 내 신청을 잃지 않는 쪽으로 그렸다 — 마감된 날도 목록에서 알약을 그대로 단다.
- **홈 목 데이터의 요일**(라운드 28 발견) — 홈 시안은 「8월 18일 월요일」인데 실제 2026-08-18은
  화요일이다(8/1 토, 8/17 월). 홈은 확정본이라 안 건드리고 달력만 실제 요일을 따랐다 — 달력은
  요일이 틀리면 화면 자체가 깨져 보인다. 퍼블리싱 때 목 데이터를 맞춘다.

- **포지션 교대 승인 규칙**(라운드 28, 기획 반환) — ① 누가 수락해야 바뀌나(상대 + 관리자로
  그렸다) ② **「내가 맡을 수 있는 포지션」을 무엇이 정하나**(겸직 목록? 등급? 교육 이수?)
  ③ `WORKER-FLOWS.md:80`의 「특정 근무자와 교대」(근무 자체를 넘기는 것)를 대체하는지 둘 다
  남는지. 이 화면에는 자격도 배정도 안 온다 — 「확정」 물음과 같은 갈래의 공백이다.
- ~~**로딩 · 에러 · 오프라인**~~ — 라운드 34에서 닫혔다. 위 「로딩 · 오프라인 · 에러」 절이 정본이다.

## 이 task가 낳는 후속 task

- **금액 가림 설정을 계정에 저장** — `profiles` 컬럼, RLS, 서버 왕복. P0-T48이 만든 「화면 설정」
  화면과 스위치는 그대로 두고 저장 계층만 갈아끼운다. 2026-08-17 사용자 결정.
- **e2e 시딩 충돌**(P0-T47 F-03) — `schedules_work_date_active_unique`. `tests/e2e/**`가
  P0-T47 범위 밖이라 넘어온 것이며 P0-T48 범위도 아니다.

## 이 task가 바꿔야 하는 승인된 문서

**라운드가 승인 문서와 부딪히면 그 라운드를 닫기 전에 여기에 행을 넣는다.** 자동 검사는 없다 —
`gate:docs`는 의존 순환 · 제목 일치 · spec 참조 · 내부 링크 넷만 본다
(`harness/lib/docs-check.ts:315-320`). 이 표가 유일한 장부다. 나중 라운드가 **같은 문서 같은
줄**을 다시 건드리면 행을 늘리지 않고 덮어쓰되, 근거는 라운드 기록이 갖는다. 실제 문서 개정은
설계 단계에서 하지 않는다 — 이 표가 RADIO Architecture로 들어가 봉인되고 개발 단계가 한 번에
고친다.

| 문서 | 지금 적힌 것 | 새 틀 | 낳은 라운드 | 상태 |
| --- | --- | --- | --- | --- |
| `PATTERNS.md:7` | 하단 탭은 `홈 · 일정 · 알림 · 전체` 네 개, 선택 탭만 action 색 | `홈 · 일정 · 급여 · 전체`, 선택 탭은 `gray-800` 채움 | 4·5 | 이관함 |
| `PATTERNS.md:19` | 홈은 고정 위젯 모음이 아니라 현재 가장 중요한 행동을 상황에 따라 바꾼다 | 고정 네 블록. 블록 자리는 같고 안의 내용이 바뀐다 | 6 | 이관함 |
| `WORKER-FLOWS.md:30` | 같은 네 탭 | 같이 갱신 | 4 | 이관함 |
| `WORKER-FLOWS.md:30-35` | 홈은 5단계 우선순위에서 하나만 고른다 | 네 블록 구조와 블록별 상태표로 대체 | 6 | 이관함 |
| `WORKER-FLOWS.md:39·110` | 예상 급여 금액은 홈에 표시하지 않는다 / 금액 미리보기를 두지 않는다 | 홈에 가린 금액을 둔다. 눈으로 그 자리에서 본다 | 6·18 | 이관함 |
| `WORKER-FLOWS.md:90` | 홈에는 현재 가능한 행동 하나만 크게 표시한다 | 파랑 버튼 하나는 유지, 블록은 넷 | 6 | 이관함 |
| `FOUNDATIONS.md:126-128` | Lucide 한 세트, 선택은 action 색과 굵기 변화 | 탭 선택만 채움 예외, `LayoutGrid` 교체, 「그림 자산」 절 신설 | 3·5·7 | 이관함 |
| `FOUNDATIONS.md:137` | 버튼·날짜 선택 120~160ms | 「자리 이동 200~300ms」 신설, 탭 눌림 420ms 별도 | 5·6~8 | 이관함 |
| `FOUNDATIONS.md` 블러 | 개념이 없다 | 둘을 나눠 적는다 — 금액 숨김 7px(읽지 못하게) · 헤더 9px + 지면색 50%(뒤에 뭔가 있다고 알리게) | 18·25 | 이관함 |
| `FOUNDATIONS.md:56-57` | `display` = 금액, `headline-lg` = 화면 제목 | 금액 26/34, 제목 22/30, 카운트다운도 26/34 | 2 | 이관함 |
| `FOUNDATIONS.md:53` | variable 축으로 굵기 **400·600·700**을 표현한다 | **500이 는다**(행 값·이름표·모달 명단). 라운드 19 감량으로 700은 화면에서 사라졌다 | 19·24 | 이관함 |
| `FOUNDATIONS.md:56-63` 굵기 열 | `title` 600 · `body-strong` 600 · `headline-*` 700 | 무게를 자리마다 다시 정한다 — 라운드 24 계층 표가 정본 | 24 | 이관함 |
| `FOUNDATIONS.md:90` | 화면 좌우 여백 20px | 16px | 1 | 이관함 |
| `FOUNDATIONS.md:92` | 카드로 모든 구획을 감싸지 않는다 | 카드가 구조의 중심 | 1 | 이관함 |
| `DESIGN.md:24` | 카드, 색상, 팝업을 남용하지 않는다 | 카드 기준으로 재작성 | 1 | 이관함 |
| `FOUNDATIONS.md` 원시 팔레트 | `ink-950`과 `gray-700` 사이가 비었다 | `gray-800` `#3a3f4a` 행 추가 | 5 | 이관함 |
| `FOUNDATIONS.md:32-33` action 틴트 | 실제로 적힌 파랑 틴트는 `#eef4ff`·`#e2ebff`다(`globals.css:9,13`). **시안이 쓰는 `#f2f6fd`·`#e4edfd`는 문서에도 코드에도 없다** — 라운드 27 행이 「둘은 이미 있다」고 잘못 적었다(2026-08-18 전수 대조에서 발견) | 틴트 사다리 셋을 **원시 팔레트에** 올린다 — `blue-25` `#f2f6fd`(신청함) · `blue-50` `#e4edfd`(확정·D-2) · `blue-100` `#cfe0fc`(D-1·선택 칸). 의미 토큰 `--color-action-tint-weak`·`-tint`·`-tint-strong`이 이 원시색을 가리킨다 | 1·27 | 이관함 |
| `FOUNDATIONS.md` 원시 팔레트 · 지면색 | **지면 `#f1f3f6`이 없다.** 가장 가까운 `gray-200` `#eef0f3`은 「구분선, 강한 중립 표면」이라 뜻이 다르다 | `gray-150` `#f1f3f6` 행 추가 + 의미 토큰 `--color-canvas` 신설. 라운드 1의 「지면」이 이름을 갖는다 | 1 | 이관함 |
| `FOUNDATIONS.md:20` `gray-300` | 테두리는 `#dee1e6`(`--color-border`) | **hairline은 `#e5e8eb`다.** `gray-250` `#e5e8eb` 행을 넣고 `--color-border`가 그것을 가리킨다. `#dee1e6`은 시안 어디에도 안 쓰인다 | 1·13 | 이관함 |
| `FOUNDATIONS.md:22` `gray-100` | `--color-surface-weak`가 `#f7f7f7` | 시안은 `#f2f4f6`(비활성 버튼·회색 D 배지·알약 바닥). **지면 `#f1f3f6`과 채널당 1 차이라 하나로 합친다** — `--color-surface-weak`도 `gray-150`을 가리킨다. 합치지 않을 근거가 나오면 미결로 되돌린다 | 13·22 | 이관함 |
| `FOUNDATIONS.md` action 전경 | 확정 상태의 글자색 `#0c3f9c`가 토큰이 아니다. `blue-700` `#003ecc`는 「pressed」라 뜻이 다르고 값도 다르다 | `blue-800` `#0c3f9c` 행 추가 + 의미 토큰 `--color-action-deep` 신설. D 배지 글자 · 확정 날짜 숫자 · 변경 요청 빈 점이 쓴다 | 1·27·28 | 이관함 |
| `WORKER-FLOWS.md:45` 주 시작 요일 | 일정 달력은 **일요일 시작**이고 시안도 그렇다(`schedule.html:1777` `DOW = ["일","월",…]`) | **홈 주간 스트립은 월요일 시작이다**(`home.html:2340` 월→일). 한 앱에 주 시작이 둘인 셈인데, 스트립은 발치에 주급을 달아 **급여 주**를 그리고 달력은 **달력 관습**을 그린다. 급여 주의 경계는 `PRD.md:346`에 걸린 기획 물음이라 이 task가 정하지 않는다 — 기획이 답한 뒤 스트립을 맞춘다. 그때까지 두 화면이 다른 것을 **문서에 적어** 다음 화면이 아무거나 베끼지 않게 한다 | 16·21·28 | 이관함 |
| `globals.css:109-155` typo 유틸리티 | `typo-display`·`headline-lg`·`headline-md`가 **700**이다 | 라운드 24로 교체 — `headline-lg` 600 · `headline-md` 600 · `title` **500**. `display` 32/40은 자리가 없어 표에서 뺀다. 그 자리에 **`caption-strong` 13/18 500**(블록 이름표)을 넣어 8단계를 지킨다. 무게는 유틸리티의 **기본값**이고 자리마다 override한다(모달 제목 16/24 600 · 더보기 14/20 400 · D 배지 13/18 600) | 19·24 | 이관함 |
| `FOUNDATIONS.md` 「그림 자산」 | 없다 | 신설 — 토큰 예외, 다섯 그림, 28px 기본·24px 하한, 고정 색 목록 | 7·23 | 이관함 |
| `WORKER-FLOWS.md` | 「화면 설정」 화면이 없다 | 금액 가리기 스위치를 둔 화면 신설 | 5·18 | 이관함 |
| `PRD` | 금액 가림이라는 개념이 없다 | 기본 가림과 설정 소유를 적는다 | 3·18 | 1단계 반환 |
| `PRD.md:298-300` 주변 | 인증 창만 있고 닫는 시각이 없다 | 기획 물음으로 남긴다(이 task가 정하지 않는다) | 8 | 1단계 반환 |
| `PRD.md:346` | 근무자는 날짜별 금액과 월별 합계를 본다 | 주 단위 합계가 주가 된다 — 기획 결정 뒤 반영 | 14~16·21 | 1단계 반환 |
| `PATTERNS.md` | 행 어법이 없다 | 행 골격(앵커 32 · 라벨/값 · 오른쪽 하나 · hairline · 더보기) 신설 | 13 | 이관함 |
| `WORKER-FLOWS.md:117` 주변 | 누적 급여 개념이 없다 | 「누적 예상 급여」 명명을 적는다(실제 지급 아님) | 23 | 이관함 |
| `COMPONENTS.md:64-85` | Calendar 절 — 셀에 「날짜 숫자 + 상태 라벨 + 선택 상태」, 오늘은 action outline, 상태 여섯 표 | 셀은 **숫자만**, 오늘은 **밑줄 2px**, 상태 표를 라운드 28 표로 교체 | **1**(28에서 표로 옮김) | 이관함 |
| `PATTERNS.md:13` | 하단 고정 저장 버튼은 「깊은 작업」에만 | 조건부 등장(변경 0이면 안 뜬다)을 「깊은 작업」의 한 꼴로 적는다 | 28 | 이관함 |
| 라운드 1 **「셀에 배지·점·범례를 넣지 않는다」** | 달력 셀에는 숫자만 둔다 | **점 하나만 예외.** 뜻이 하나로 못 박힌다 — 점은 **「내가 근무하는(했던) 날」**이고 다른 상태는 점을 못 갖는다. 범례·`open` 표시용 점 금지는 그대로 | 28 | 이관함 |
| 라운드 1 틴트 배정 | 신청함 `#f2f6fd` · 확정 `#e4edfd` | **채움을 셀에서 걷는다.** 상태는 숫자 색과 점이 지고, 채움 `#cfe0fc`는 **「이번에 선택한 칸」 하나에만** 남는다 | 28 | 이관함 |
| `WORKER-FLOWS.md:47`·`:49-50` | 하단 영역에서 변경 개수를 확인하고 `신청하기`를 누른다. 저장 뒤 **`방금 변경한 N개 날짜 되돌리기` undo**를 주고 마지막 batch 전체를 복원한다 | 변경이 0이면 바가 없고 1개부터 240ms로 올라온다. 개수를 **추가·취소로 나눠 센다**(「신청 +2 · 취소 −1」). **undo를 없앤다** — 저장하면 바가 걷히고 「신청이 완료되었어요」 토스트 하나. 되돌릴 길이 이미 둘이다(마감 전 다시 눌러 취소 · 확정 뒤 근무 변경 요청) | 28 | 이관함 |
| `WORKER-FLOWS.md:71` | 본인 행은 **action tint**와 `나` | **파랑 500 글자**. 라운드 22 확정본이 이미 그렇게 정했고 시트는 앱에서 하나다 | 28 | 이관함 |
| `WORKER-FLOWS.md:86` | 당일 근무자 간 **포지션 교환 기능은 만들지 않고** 관리자 문의로 안내한다 | **만든다**(사용자 지시). 상대 + 관리자 수락, 자격 있는 포지션만. **기획 승인이 먼저다** | 28 | P0-T55 이월 |
| 라운드 22 시트 닫기 | 발치 파란 `닫기` 버튼 | **머리 오른쪽 ✕**, 겹겹 시트는 머리 왼쪽 ←. `confirmed/home.html`도 같이 고쳤다 | 28 | 이관함 |
| `deadline-batches.ts` | 마감 묶음이 **여럿**인 전제로 날짜를 묶는다 | **보통 하나**, 가끔 추가 모집. 화면은 한 회차를 기본 모양으로 잡고 여럿이면 **마감 임박순**으로 쌓는다 | 28 | 5라운드 코드 |
| 계층 표 18/26 | 행 값 18/26 500 | **여러 줄이 이어지는 목록 항목은 16/24 400**. 18/26은 카드에 한 줄씩만 설 때다 | 28 | 이관함 |
| `PATTERNS.md:19` · 라운드 6 「없다고 말한다」 | 데이터 없는 블록은 사라지지 않는다(알림만 예외) | **예외가 둘이다** — 알림과 **오늘**. 근무 없는 날엔 오늘 블록이 통째로 접히고 「이번 주」가 두 번째 자리로 올라온다 | 29 | 이관함 |
| `PATTERNS.md:22` 빈 상태 | 「이유와 다음 행동을 함께 제공한다」 한 줄뿐이라 모양이 없다 | **「빈 블록」 절 신설** — 앵커 없는 **안 눌리는** quiet 한 줄(16/24 **400**) + 더보기 유지. 배지에 낱말을 넣지 않는다. 값 자리가 있으면 `—`(0원 아님, 블러·탭 없음) | 30·31 | 이관함 |
| `WORKER-FLOWS.md:39·110` 주변 | 급여 금액 표시 규칙만 있다 | **지난 달 fallback을 걷는다.** 급여 행 셋의 자리는 **지난주 · 이번 달 · 연 누적**으로 고정이고 이름이 달을 넘나들지 않는다 | 30 | 이관함 |
| 라운드 20 「오늘 밑줄 삭제」 | 홈 스트립에 오늘 표시가 없다 | **오늘 칸을 두르는 실선 1px `#e5e8eb`**(일정 화면 달력과 같은 표시, 라운드 28). **늘 켜진다.** 뺐던 근거(오늘 카드가 말한다)가 라운드 29로 사라졌다 | 32 | 이관함 |
| `COMPONENTS.md:131-136` Notification row | 탭하면 이동하고 읽음 처리. 유형 필터·삭제·보관은 MVP에서 빼고 **「모두 읽음」은 없다** | **「모두 읽음」이 있다.** 홈 카드의 `X`는 **읽음이 아니라 그날 하루 미루기**이고, 종 점은 **알림 화면에서 읽거나 「모두 읽음」**을 눌러야 꺼진다. 홈 블록에 안 오는 알림(마감 임박 · 다음 근무)도 점을 만든다. 모양은 **P0-T51** | 33 | 이관함 |
| `PATTERNS.md:73-82` 「로딩, 오류, 오프라인」 | 로딩·오프라인 줄만 있고 **「서버가 실패했을 때」 줄이 없다** | **에러 규칙 신설** — 일부 실패는 그 블록 자리에 「〈블록 이름〉을 불러오지 못했어요」 + 「다시 시도」 알약, **전부 실패면 화면 한 장**(헤더·탭바는 남는다). **빨강을 쓰지 않는다** | 34 | 이관함 |
| `PATTERNS.md:75` skeleton | 「콘텐츠 구조를 유지하는 skeleton」뿐 | 모양을 적는다 — 막대 `--color-border` radius 6, **반짝임 없음**, **아는 것(제목·이름표·요일·날짜)은 가리지 않는다** | 34 | 이관함 |
| `WORKER-FLOWS.md:94-95` | 「출근 인증하기」를 누르면 **홈 카드 안의 영역이** 「현재 위치를 확인하고 있어요」로 바뀐다 | **별도 페이지로 넘어간다.** 위치 확인 애니메이션이 그 페이지에 서고 **끝나면 홈으로 돌아온다.** 화면 자체는 **P0-T49** | 34 후속 | 이관함 |
| `WORKER-FLOWS.md:90` | 홈에는 현재 가능한 행동 하나만 크게 표시한다 | 라운드 6 행과 같이 갱신 — 파랑 버튼 하나는 유지되나 그 버튼이 **화면을 이동시킨다** | 34 후속 | 이관함 |
| `COMPONENTS.md:137-141` Connectivity banner | 상단에서 알린다 | **헤더 레이어의 셋째 줄**(80 → 120)이라 콘텐츠가 그 뒤로 지나간다. 색 `warning-surface` `#fff7d6` + `warning` `#765500`, 문구에 읽기 전용을 붙인다. 비활성 이유는 **버튼 라벨**이 진다 | 34 | 이관함 |
| `FOUNDATIONS.md:20` `gray-300`(잔여) | 용도가 아직 「테두리」다 | 실제 소비처는 `--color-neutral-border`·`--color-disabled-border` 둘뿐이라 **중립·비활성 테두리**로 고친다 | 1라운드 토큰 재배치의 잔여 | 이관함 |
| `FOUNDATIONS.md:22` `gray-100`(잔여) | 용도가 아직 「입력과 약한 표면」이다 | `--color-surface-weak`는 `gray-150`으로 옮겨갔고 남은 소비처는 `--color-neutral-surface` 하나라 **중립 표면**으로 고친다 | 1라운드 토큰 재배치의 잔여 | 이관함 |

