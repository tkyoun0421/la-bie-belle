# 승인할 일

## 목적과 진입

관리자가 근무자에게서 온 요청에 답하는 화면이다. 값은 [tokens.md](../../design-system/tokens.md)에만 있고 여기는 토큰 이름으로만 말한다.

두 가지가 한 목록에 선다. 못 찍은 출근의 [사유](../../modules/attendance/README.md#att-010)와 [근무 취소](../../modules/schedule/README.md#sch-018) 요청이다. 성격은 다르지만 관리자가 하는 일이 같다 — 읽고 승인하거나 이유를 적어 거절한다.

들어오는 문은 하나다. 관리자 홈의 「승인할 일 · 3건」 줄이고([schedule-admin.md](../../modules/schedule/screens/schedule-admin.md#관리자-홈)) 그 줄이 이 화면을 연다. 사유가 도착해도 푸시는 안 간다 — 그 줄의 숫자가 유일한 신호다([notification/README.md](../../modules/notification/README.md#ntf-030)). 근무 취소 요청은 푸시로도 온다.

보내는 쪽 화면은 둘이다. 사유는 [excuse.md](../../modules/attendance/screens/excuse.md)의 사유 시트, 근무 취소는 [schedule-worker.md](../../modules/schedule/screens/schedule-worker.md#시트의-문)의 날 시트다.

경로와 역할 조건은 [navigation.md](../navigation.md#경로)가 든다.

## 참조 기준

- 업무 규칙 — [attendance/README.md](../../modules/attendance/README.md#att-010)의 ATT-010, [attendance/README.md](../../modules/attendance/README.md#att-015)의 ATT-015, [attendance/README.md](../../modules/attendance/README.md#용어)의 출근 인정, [schedule/README.md](../../modules/schedule/README.md#sch-018)의 SCH-018, [notification/README.md](../../modules/notification/README.md#ntf-014)의 NTF-014, [notification/README.md](../../modules/notification/README.md#ntf-030)의 NTF-030
- design의 행위 — [schedule/design.md](../../modules/schedule/design.md#근무-취소-요청과-판정)
- 탐색 — [navigation.md](../navigation.md#경로)
- 공통 시각·문안 — [tokens.md](../../design-system/tokens.md), [components.md](../../design-system/components.md), [motion.md](../../design-system/foundation/motion.md), [writing.md](../../design-system/writing.md)
- 다른 영역 — [excuse.md](../../modules/attendance/screens/excuse.md), [schedule-worker.md](../../modules/schedule/screens/schedule-worker.md#시트의-문), [schedule-admin.md](../../modules/schedule/screens/schedule-admin.md), [members-pending.md](../../modules/account/screens/members-pending.md#빈-상태), [prd.md](../../../1-plan/prd.md)

## 화면 상태와 흐름

근무 취소 승인의 흐름은 [schedule/design.md](../../modules/schedule/design.md#근무-취소-요청과-판정)에 있다.

### 목록

| 상태 | 진입 조건·계기 | 보이는 내용 | 가능한 행동 | 다음 상태·목적지 |
| --- | --- | --- | --- | --- |
| 정상 | 관리자 홈의 「승인할 일 · 3건」 줄 | 앱바, 줄 목록 | 줄을 누른다 | 상세 시트 |
| 빈 상태 | 답할 것이 하나도 없다 | 앱바, 빈 상태 제목과 아래 줄 | — | — |

### 상세 시트

| 상태 | 진입 조건·계기 | 보이는 내용 | 가능한 행동 | 다음 상태·목적지 |
| --- | --- | --- | --- | --- |
| 정상 | 목록의 줄을 누른다 | 제목, 부제, 보낸 시각, 보낸 글, 하단 버튼 둘 | 「거절」, 「승인」 | 거절 → 거절. 사유의 승인 → 사유 승인. 근무 취소의 승인 → 근무 취소 확인 |
| 근무 취소 확인 | 근무 취소의 「승인」을 누른다 | 화면 가운데 Dialog | 「뒤로」, 「취소 승인」 | 뒤로 → 정상. 취소 승인 → 그날 [날 상세](../../modules/schedule/screens/schedule-admin.md) |
| 사유 승인 | 사유의 「승인」을 누른다 | 시트가 닫히고 목록에서 줄이 빠진다. 토스트 | — | 목록 |

### 거절

| 상태 | 진입 조건·계기 | 보이는 내용 | 가능한 행동 | 다음 상태·목적지 |
| --- | --- | --- | --- | --- |
| 이유 고르기 | 상세 시트에서 「거절」을 누른다 | 제목, 이유 줄, 입력 칸, 아래 줄, 하단 버튼 둘 | 이유 줄을 고른다. 「직접 쓰기」면 입력 칸에 적는다. 「뒤로」, 「거절 보내기」 | 뒤로 → 상세 시트. 거절 보내기 → 보내는 중 |
| 보내는 중 | 「거절 보내기」를 누른다 | 하단 버튼 둘이 다 잠긴다 | — | 성공 → 목록. 실패 → 보내기 실패 |
| 보내기 실패 | 보내기가 실패한다 | 시트가 안 닫히고 오류가 시트 안에 선다. 쓴 내용이 그대로 남는다 | 「다시 보내기」 | 보내는 중 |

## 짜임과 토큰

### 목록 짜임

세로로 쌓는다.

1. 앱바 — 뒤로와 「승인할 일」
2. 목록

**섞어서 한 목록이다.** 종류로 탭을 나누지 않는다. 관리자가 여는 이유는 「오늘 답할 것이 있나」지 「사유가 몇 건인가」가 아니고, 탭으로 가르면 다른 탭에 밀린 것을 숫자로만 알게 된다.

**근무 취소가 위에 선다.** 그 안에서는 근무 날이 가까운 것부터고, 사유는 그 아래에 보낸 지 오래된 것부터 붙는다. 취소는 마감(근무 전날)이 있고 승인한 뒤 빈 자리를 채울 시간까지 필요한데, 사유는 [관리자에게 시한이 없다](../../modules/attendance/README.md#att-015).

#### 줄 하나

[components.md](../../design-system/components.md#listrow)의 ListRow다.

| 자리 | 담는 것 |
| --- | --- |
| 제목 앞 | Badge neutral — 「사유」 또는 「근무 취소」 |
| 제목 | 사람 이름과 근무 날. 취소는 포지션까지 |
| 보조 정보 | 보낸 글의 첫 줄. 한 줄에서 말줄임 |
| 오른쪽 | 화살표 |

배지가 제목 앞에 서서 두 종류의 이름 시작 위치가 어긋난다. 목록이 대개 서너 줄이라 스캔 부담보다 종류가 먼저 읽히는 쪽을 택했다.

줄을 누르면 [상세 시트](#상세-시트-짜임)가 올라온다. 목록에서 바로 답하는 길은 없다 — 승인도 거절도 되돌릴 수 없고 [기록도 안 남으니](#끝난-뒤), 글을 다 읽은 자리에서만 누르게 한다.

#### 빈 상태

[components.md](../../design-system/components.md#빈-상태)의 빈 상태다. 제목과 아래 줄만 있고 버튼이 없다 — 관리자가 여기서 할 일이 없다.

관리자 홈의 줄이 「승인할 일 · 0건」이어도 눌린다. 눌러 보고 비었다는 것을 확인하는 것이 안 눌리는 줄을 보고 왜 안 눌리나 하는 것보다 낫다([members-pending.md](../../modules/account/screens/members-pending.md#빈-상태)와 같다).

#### 목록 색

| 자리 | 토큰 |
| --- | --- |
| 화면 바탕 | `bg.neutral` |
| 앱바 | [components.md](../../design-system/components.md#앱바)의 앱바 |
| 줄 | [components.md](../../design-system/components.md#listrow)의 ListRow |
| 종류 배지 | [components.md](../../design-system/components.md#badge)의 Badge neutral |

배지를 neutral로 둔 것은 종류가 급함을 뜻하지 않기 때문이다. 무엇이 먼저인지는 정렬이 이미 말한다.

#### 목록 글자

| 자리 | 유틸 |
| --- | --- |
| 앱바 제목 | `text-lg font-semibold` |
| 줄 제목 | `text-base font-medium` |
| 줄 보조 정보 | `text-sm` |
| 배지 | `text-xs font-medium` |

날짜와 시각에 `tabular-nums`를 건다.

#### 목록 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 화면 좌우 여백 | `px-6` |
| 줄 | `py-4` |
| 배지와 제목 사이 | `gap-2` |

### 상세 시트 짜임

줄을 누르면 아래에서 올라온다. [components.md](../../design-system/components.md#dialog와-바텀시트)의 바텀시트고 `--duration-slow`다.

1. 제목 — 사람 이름, 근무 날과 요일, 포지션
2. 부제 — 그날 근무 시간
3. 보낸 시각
4. 보낸 글
5. 하단 버튼 둘 — 「거절」(Button secondary)과 「승인」(Button primary)

사유와 근무 취소가 같은 짜임을 쓴다. 다른 것은 「승인」을 눌렀을 때다 — 사유는 바로 끝나고, 취소는 [확인을 한 번 더 묻는다](#근무-취소-승인).

**보낸 시각을 같이 보여준다.** 사유는 근무가 끝나고 48시간이 마감이라 언제 보낸 것인지가 판단에 든다. 마감이 지난 뒤에 온 사유도 같은 자리에 그대로 서고 승인도 된다 — 마감은 근무자가 넣는 문만 닫는다.

**글은 접지 않는다.** 사유가 200자, 취소가 100자까지라 시트 안에 다 들어간다. 더보기를 두면 안 펼친 채로 누르는 자리가 생긴다.

#### 근무 취소 승인

근무 취소의 「승인」을 누르면 확인을 한 번 더 묻는다. 사유 승인은 안 묻는다.

가르는 것은 무게다. 사유 승인은 그날이 결근이 아니게 될 뿐이고 틀렸으면 급여에서 조정한다. 근무 취소 승인은 누르는 순간 자리가 비고 근무자에게 알림이 나가며 그 사람은 그날 안 나온다. [끝난 것을 보는 자리가 없어서](#끝난-뒤) 되돌릴 기록도 안 남는다.

[components.md](../../design-system/components.md#dialog와-바텀시트)의 Dialog다. 화면 가운데에 선다.

오른쪽 버튼을 「취소」로 안 쓴 것은 그 말이 이 대화상자를 무른다는 뜻으로도 읽히기 때문이다.

**그날 [날 상세](../../modules/schedule/screens/schedule-admin.md)로 넘어간다.** 시트와 목록이 닫히고 화면이 바뀐다. 토스트가 「근무를 취소했어요」로 뜬다.

승인한 뒤 관리자가 할 일이 빈 자리를 채우는 것 하나뿐이고, 그 도구가 날 상세에 있다([schedule/README.md](../../modules/schedule/README.md#sch-018)의 배정 추가). 목록으로 돌아오는 길은 앱바의 뒤로다.

토스트에 「자리 채우기」 버튼을 다는 길은 막혀 있다 — [components.md](../../design-system/components.md#토스트)가 사라지는 것에 액션을 걸지 말라고 한다. 그래서 버튼 대신 화면이 옮겨간다.

#### 사유 승인

「승인」을 누르면 바로 끝난다. 시트가 닫히고 목록에서 줄이 빠지며 토스트가 「사유를 승인했어요」로 뜬다.

승인된 날은 [출근 인정](../../modules/attendance/README.md#용어)이 된다. 근무표 날 명단에서 그 사람 줄이 「확인 중」에서 「출근 인정」으로 바뀌고([schedule-worker.md](../../modules/schedule/screens/schedule-worker.md#인증-상태)), 급여에서 빠졌던 그날이 돌아온다.

#### 끝난 뒤

**처리한 건은 목록에서 사라지고 다시 안 보인다.** 승인한 것도 거절한 것도 마찬가지다.

기록을 따로 안 남기는 것은 결과가 이미 다른 화면에 있어서다. 승인된 사유는 근무표 명단의 「출근 인정」으로, 취소된 근무는 빈 자리로 남는다. 거절은 근무자가 받은 알림과 다시 보내는 흐름이 이어받는다.

치르는 값은 잘못 누른 것을 여기서 찾을 수 없다는 것이다. 근무 취소에만 확인을 한 번 더 두는 것이 그 값을 낮추는 선이다.

#### 상세 시트 색

| 자리 | 토큰 |
| --- | --- |
| 시트 | [components.md](../../design-system/components.md#dialog와-바텀시트)의 바텀시트 |
| 제목 | `fg.neutral` |
| 부제 | `fg.neutral-muted` |
| 보낸 시각 | `fg.neutral-subtle` |
| 보낸 글 | `fg.neutral` |
| 글이 앉는 면 | `bg.neutral-weak` |

보낸 글에 면을 깔아 화면의 다른 문장과 가른다. 관리자가 읽고 판단하는 대상이 그 글 하나다.

#### 상세 시트 글자

| 자리 | 유틸 |
| --- | --- |
| 제목 | `text-lg font-semibold` |
| 부제 | `text-sm` |
| 보낸 시각 | `text-xs` |
| 보낸 글 | `text-base` |

#### 상세 시트 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 시트 안쪽 | `p-6` |
| 글이 앉는 면 | `rounded-md` `p-4` |
| 제목과 글 사이 | `mt-5` |
| 글과 버튼 사이 | `mt-6` |
| 버튼 둘 사이 | `gap-3` |

시트는 위쪽만 `rounded-lg`다.

### 거절 짜임

「거절」을 누르면 시트가 닫히지 않고 안쪽이 이유 고르기로 바뀐다. 새 시트를 겹치지 않는 것은 방금 읽은 글에서 이어지는 한 동작이기 때문이다.

1. 제목 — 거절하는 이유
2. 이유 줄 — 사유는 넷, 근무 취소는 셋. 마지막이 「직접 쓰기」다
3. 입력 칸 — 「직접 쓰기」를 골랐을 때만 열린다
4. 아래 줄 — 고른 문장이 근무자에게 그대로 간다는 안내
5. 하단 버튼 둘 — 「뒤로」(Button secondary)와 「거절 보내기」(Button primary)

**이유가 필수다.** 아무것도 안 고르면 「거절 보내기」가 안 눌린다. 거절은 「이 설명으로는 못 믿겠다」거나 「그날은 나와야 한다」는 무거운 판정이고, 이유가 없으면 근무자가 다시 쓸지 포기할지 정하지 못한다.

**자주 쓰는 문장을 미리 놓는다.** 매번 처음부터 쓰게 하면 결국 한 글자짜리 이유가 간다.

**고른 줄 오른쪽에 체크가 선다.** `fg.brand`다. 고르는 것으로 끝나지 않고 하단 버튼이 확정한다 — 되돌릴 길이 없는 동작이라 손이 한 번 더 간다.

#### 보낸 뒤

시트가 닫히고 목록에서 그 줄이 빠진다. 화면 아래에 [토스트](../../design-system/components.md#토스트)가 뜬다 — 「거절했어요」다.

거절 결과는 그 근무자에게 푸시로 간다. 관리자가 적은 이유가 같이 실린다([notification/README.md](../../modules/notification/README.md#ntf-014)).

**실패하면 시트가 안 닫힌다.** 오류가 시트 안에 서고 쓴 내용이 그대로 남는다. 오른쪽 버튼이 「다시 보내기」가 된다 — [사유 시트](../../modules/attendance/screens/excuse.md)가 같은 방식이다.

#### 거절 색과 글자

| 자리 | 토큰·유틸 |
| --- | --- |
| 제목 | `fg.neutral` · `text-lg font-semibold` |
| 이유 줄 | [components.md](../../design-system/components.md#listrow)의 ListRow |
| 고른 표시 | `fg.brand` |
| 입력 칸 | [components.md](../../design-system/components.md#input)의 Input |
| 아래 줄 | `fg.neutral-subtle` · `text-xs` |

이유 줄에는 화살표를 안 붙인다. 다음 화면이 없다.

#### 거절 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 시트 안쪽 | `p-6` |
| 제목과 첫 줄 사이 | `mt-4` |
| 이유 줄 | `py-3` |
| 입력 칸 | `rounded-md` `p-3` `min-h-20` |
| 아래 줄 | `mt-3` |
| 버튼과의 간격 | `mt-6` |
| 버튼 둘 사이 | `gap-3` |

입력 칸이 [사유 시트](../../modules/attendance/screens/excuse.md)의 `min-h-22`보다 얕다. 근무자가 무슨 일이 있었는지 적는 자리와 달리 여기는 한두 마디로 끝난다.

## 문안

### 목록 문안

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 앱바 제목 | 승인할 일 | — |
| 사유 배지 | 사유 | 사유 줄에만 |
| 취소 배지 | 근무 취소 | 근무 취소 줄에만 |
| 사유 줄 제목 | 김민지 · 10월 7일 | 사람 이름과 근무 날 |
| 취소 줄 제목 | 이준호 · 10월 12일 메인 | 사람 이름과 근무 날과 포지션 |
| 빈 상태 제목 | 승인할 일이 없어요 | 빈 상태 |
| 빈 상태 아래 줄 | 사유나 근무 취소가 오면 여기 서요 | 빈 상태 |

### 상세 시트 문안

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 사유 제목 | 김민지 · 10월 7일(화) 메인 | 사람 이름과 근무 날과 요일과 포지션 |
| 취소 제목 | 이준호 · 10월 12일(월) 메인 | 사람 이름과 근무 날과 요일과 포지션 |
| 부제 | 10:00–19:00 | 그날 근무 시간 |
| 보낸 시각 | 10월 7일 19:40에 보냈어요 | 보낸 날과 시각 |
| 왼쪽 버튼 | 거절 | — |
| 오른쪽 버튼 | 승인 | — |
| 근무 취소 확인 제목 | 근무를 취소할까요? | 근무 취소의 승인에만 |
| 근무 취소 확인 본문 | 10월 12일 메인 자리가 비고 이준호님에게 알림이 가요 | 근무 날과 포지션과 사람 이름 |
| 근무 취소 확인 왼쪽 버튼 | 뒤로 | — |
| 근무 취소 확인 오른쪽 버튼 | 취소 승인 | — |
| 사유 승인 뒤 토스트 | 사유를 승인했어요 | 사유의 승인에만 |
| 근무 취소 승인 뒤 토스트 | 근무를 취소했어요 | 근무 취소의 승인에만 |

시각은 [writing.md](../../design-system/writing.md#숫자와-단위)대로 24시간제다.

**관리자 화면은 판정 어휘를 쓴다.** 「승인」과 「거절」이다. [writing.md](../../design-system/writing.md)의 「사람이 누르는 버튼은 예외다」는 근무자가 상대에게 답하는 자리의 조항이고, 여기는 관리자가 기록에 남길 판정을 고르는 자리다.

### 거절 문안

사유 거절:

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 제목 | 거절하는 이유 | — |
| 첫째 | 설명만으로는 확인이 어려워요 | — |
| 둘째 | 그날 기록과 맞지 않아요 | — |
| 셋째 | 조금 더 자세히 적어 주세요 | — |
| 넷째 | 직접 쓰기 | — |

근무 취소 거절:

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 제목 | 거절하는 이유 | — |
| 첫째 | 그날 대신 나올 사람이 없어요 | — |
| 둘째 | 근무가 코앞이라 어려워요 | — |
| 셋째 | 직접 쓰기 | — |

공통:

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 입력 라벨 | 이유 | 「직접 쓰기」를 골랐을 때만 |
| 입력 자리표시 | 근무자에게 보낼 말을 적어 주세요 | 「직접 쓰기」를 골랐을 때만 |
| 아래 줄 | 고른 문장이 근무자에게 그대로 가요 | — |
| 왼쪽 버튼 | 뒤로 | — |
| 오른쪽 버튼 | 거절 보내기 | — |
| 보내는 중 | 보내는 중 | 보내는 동안 |
| 보낸 뒤 토스트 | 거절했어요 | — |
| 보내기 실패 오른쪽 버튼 | 다시 보내기 | 보내기가 실패했을 때 |

**보내는 동안 버튼 둘이 다 잠긴다.** 「뒤로」까지 막는 것은 요청이 이미 떠난 뒤에 시트를 닫으면 결과를 어디서도 못 보기 때문이다. [사유 시트](../../modules/attendance/screens/excuse.md)가 같은 방식이다.

**셋째를 양쪽에서 다르게 뒀다.** 사유는 거절 뒤에 다시 넣는 것이 정상 흐름이라 「조금 더 자세히 적어 주세요」가 실제로 다음 행동을 부른다. 근무 취소는 다시 요청해도 그날 사정이 그대로일 때가 많아서 같은 문장을 안 놓았다.

## 모션

### 목록 모션

**등장 모션이 없다.** 관리자가 자주 드나드는 화면이다([motion.md](../../design-system/foundation/motion.md#자주-일어나는-것은-움직이지-않는다)).

처리한 줄이 목록에서 빠질 때만 움직인다. 그 줄이 사라지고 아래가 올라온다.

### 상세 시트 모션

시트가 올라오고 내려간다. 그 밖에는 움직이지 않는다.

## 예외와 미정

### 안 담은 것

**끝난 것을 보는 화면.** 위의 이유로 안 만들었다.

**빈 자리를 채우는 화면.** 근무 취소를 승인한 뒤 넘어가는 곳이고 [schedule-admin.md](../../modules/schedule/screens/schedule-admin.md)에 있다.

**통계.** 사람별 출근과 지각 횟수는 [prd.md](../../../1-plan/prd.md)의 통계에 들고 별도 화면이다.

**시안.** 이 문서가 서고 나서 `approvals.sian.html`을 그린다.

### 규칙과 부딪힌 자리

**토스트에 액션을 못 건다.** 근무 취소를 승인한 뒤 「빈 자리 채우기」로 잇는 자리인데 [components.md](../../design-system/components.md#토스트)가 막는다. 버튼을 다는 대신 승인이 곧바로 날 상세로 데려가는 쪽으로 갔다.

**단일 선택 목록의 규격이 없다.** 거절 이유 넷 중 하나를 고르는 자리인데 [components.md](../../design-system/components.md)에 라디오도 선택 상태의 ListRow도 없다. 오른쪽 체크로 그렸고, 같은 모양이 다른 화면에서 한 번 더 나오면 그때 공용으로 올린다.

**가운데 Dialog의 치수가 정본에 올라갔다.** 좌우 여백과 안쪽 여백과 그림자는 [components.md](../../design-system/components.md#dialog와-바텀시트)의 「가운데 Dialog」 표가 정본이다. 이 화면이 첫 자리고 시안은 그 값대로 서 있다.

**배지가 제목 앞에 서서 정렬이 어긋난다.** 「사유」와 「근무 취소」의 폭이 달라 이름 시작이 두 위치로 갈린다. 배지를 오른쪽으로 옮기면 화살표와 겹치고, 줄여 쓰면 [domain 용어](../../modules/schedule/README.md#용어)와 어긋난다.

### 아직 안 정한 것

임의로 채우지 않는다. 사람이 정하고 나서 이 절에서 뺀다. 「제안」으로 그린 것은 결정되면 본문에서 표시를 뺀다.

#### Q-01

- 질문: 거절 이유를 직접 쓸 때의 글자 수 상한(제안: 100자)
- 필요한 근거와 대안: 근무 취소 사유가 100자라 같은 값으로 뒀는데 근거가 대칭뿐이다

#### Q-02

- 질문: 승인·거절이 통신에 실패했을 때의 문안
- 필요한 근거와 대안: 시트가 안 닫히고 오류가 선다는 것까지 정했고 문장은 안 정했다
