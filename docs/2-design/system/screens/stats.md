# 통계

## 목적과 진입

한 달을 숫자로 보는 화면이다. 값은 [tokens.md](../../design-system/tokens.md)에만 있고 여기는 토큰 이름으로만 말한다.

**역할마다 한 벌씩 있다.** 관리자는 `/admin/stats`에서 홀 전체를 보고 탭이 근무·근태다. 근무자는 `/stats`에서 자기 것만 보고 탭이 근태·포지션·급여다. 근거가 되는 규칙은 [payroll/README.md](../../modules/payroll/README.md)와 [attendance/README.md](../../modules/attendance/README.md#att-023)에 있다.

**관리자 화면에 금액이 없다.** 급여를 주는 자리가 앱 밖이라([prd.md](../../../1-plan/prd.md)) 홀 전체 인건비를 묻는 사람이 없다. 관리자가 시간과 출결을 보고, 금액은 시급을 정하는 [시급](../../modules/payroll/screens/wages.md)과 자기 것을 보는 [급여 조회](../../modules/payroll/screens/payroll.md)가 든다.

**조각을 전부 같이 쓴다.** 세그먼트도 달 줄도 [추이 그래프](../../design-system/components.md#추이-그래프)도 [줄 막대](../../design-system/components.md#줄-막대)도 [비율 띠](../../design-system/components.md#비율-띠)도 같은 것이고, 갈리는 것은 담는 값과 탭 이름과 칸 수뿐이다. 그래서 문서를 안 가른다 — 가르면 같은 짜임이 두 벌 선다.

들어오는 문은 둘이다. 관리자는 [관리자 홈](../../modules/schedule/screens/schedule-admin.md#관리자-홈)의 「통계」 줄, 근무자는 [「나」](../../modules/account/screens/profile.md)의 「통계」 줄이다. 관리자도 근무자라 두 화면을 다 본다 — 홈에서 들어가면 홀 전체고 「나」에서 들어가면 자기 것이다.

경로와 역할 조건은 [navigation.md](../navigation.md#경로)가 든다.

**세는 단위가 달이다.** 주도 연도 아니다 — 근태를 [attendance/README.md](../../modules/attendance/README.md#att-023)가 달로 센다고 정했고 근무 시간도 같은 달에 묶인다. [급여 조회](../../modules/payroll/screens/payroll.md)가 주·월·연 셋을 여는 것과 갈린다.

**근무자 급여 탭의 숫자는 예상치다.** 급여가 [예상치](../../modules/payroll/README.md)라서다. 화면이 그 사실을 한 줄로 밝힌다. 관리자 쪽에는 금액이 없어 그 줄도 없다.

## 참조 기준

- 업무 규칙 — [attendance/README.md](../../modules/attendance/README.md#att-020)의 ATT-020, [attendance/README.md](../../modules/attendance/README.md#att-023)의 ATT-023, [schedule/README.md](../../modules/schedule/README.md#sch-021)의 SCH-021, [payroll/README.md](../../modules/payroll/README.md#용어)의 조정, [schedule/README.md](../../modules/schedule/README.md)의 포지션과 겸임
- 탐색 — [navigation.md](../navigation.md#경로)
- 공통 시각·문안 — [tokens.md](../../design-system/tokens.md), [components.md](../../design-system/components.md), [writing.md](../../design-system/writing.md)
- 다른 영역 — [payroll.md](../../modules/payroll/screens/payroll.md), [wages.md](../../modules/payroll/screens/wages.md), [members.md](../../modules/account/screens/members.md), [profile.md](../../modules/account/screens/profile.md), [schedule-worker.md](../../modules/schedule/screens/schedule-worker.md), [attendance/design.md](../../modules/attendance/design.md), [dashboard.md](dashboard.md), [prd.md](../../../1-plan/prd.md), [metrics.md](../../../1-plan/metrics.md)

## 화면 상태와 흐름

### 관리자 — `/admin/stats`

| 상태 | 진입 조건·계기 | 보이는 내용 | 가능한 행동 | 다음 상태·목적지 |
| --- | --- | --- | --- | --- |
| 근무 | 세그먼트의 「근무」 | 앱바, 달 줄, 세그먼트, 추이 그래프, 합계, 보조 줄, 사람별 구획, 포지션 구획 | 달 줄의 좌우 화살표, 세그먼트, 사람별 줄을 누른다 | 근태, 근무 내역 시트 |
| 근태 | 세그먼트의 「근태」 | 앱바, 달 줄, 세그먼트, 추이 그래프, 현황 줄, 비율 띠, 사람별 목록 | 달 줄의 좌우 화살표, 세그먼트 | 근무 |
| 근무 내역 시트 | 근무 탭 사람별 구획의 줄을 누른다 | 손잡이, 이름, 날짜 목록, 합계 줄 | 닫는다 | 근무 |
| 빈 상태 | 그달에 확정된 근무표가 없다 | 그래프는 그 달만 점이 빠진 채 서고, 합계 자리는 `–`, 목록 자리에 빈 상태 제목과 아래 줄 | 달 줄의 좌우 화살표, 세그먼트 | 두 탭이 같은 모양으로 빈다 |
| 달이 하나뿐 | 앱을 쓰기 시작한 첫 달 | 그래프 자리에 점 하나. 선도 면도 없다 | 위와 같다 | — |

### 근무자 — `/stats`

| 상태 | 진입 조건·계기 | 보이는 내용 | 가능한 행동 | 다음 상태·목적지 |
| --- | --- | --- | --- | --- |
| 근태 | 세그먼트의 「근태」 | 앱바, 달 줄, 세그먼트, 추이 그래프, 현황 줄, 비율 띠, 날짜 목록 | 달 줄의 좌우 화살표, 세그먼트 | 포지션, 급여 |
| 포지션 | 세그먼트의 「포지션」 | 앱바, 달 줄, 세그먼트, 추이 그래프, 합계, 내 포지션 목록 | 달 줄의 좌우 화살표, 세그먼트 | 근태, 급여 |
| 급여 | 세그먼트의 「급여」 | 앱바, 달 줄, 세그먼트, 추이 그래프, 합계, 예상치 안내, 보조 줄, 「내역 보기」 줄 | 달 줄의 좌우 화살표, 세그먼트, 「내역 보기」 | 근태, 포지션, [급여 조회](../../modules/payroll/screens/payroll.md) |
| 빈 상태 | 그달에 내 근무가 하나도 없다 | 관리자 쪽과 같은 모양 | 위와 같다 | 세 탭이 같은 모양으로 빈다 |
| 달이 하나뿐 | 앱을 쓰기 시작한 첫 달 | 그래프 자리에 점 하나. 선도 면도 없다 | 위와 같다 | — |

**근무자 쪽에는 사람별 구획이 없다.** 볼 사람이 자기 하나라 줄이 한 줄뿐인 목록이 된다. 그 자리를 근태는 날짜 목록이, 포지션은 내가 들어간 포지션만 든 목록이 대신한다.

**근무자 쪽은 포지션이 탭 하나다.** 관리자 쪽에서 포지션이 근무 탭의 둘째 구획인 것과 갈린다 — 관리자 근무 탭은 같은 432시간을 사람으로 한 번, 포지션으로 한 번 가르는 자리고, 근무자에게는 가를 사람이 자기뿐이라 포지션만 남는다.

## 짜임과 토큰

### 통계 짜임

세로로 쌓는다.

1. 앱바 — 뒤로와 「통계」
2. 달 줄 — 좌우 화살표와 「2026년 10월」
3. 세그먼트 — 관리자는 근무·근태, 근무자는 근태·포지션·급여
4. 추이 그래프 — 고른 탭의 대표 숫자가 열두 달
5. 고른 것의 내용

**넷까지가 두 역할에서 같다.** 앱바 제목도 달 줄도 세그먼트 자리도 그래프 자리도 같고, 갈리는 것은 세그먼트 칸 이름과 칸 수와 다섯째다.

**달 줄이 세그먼트 위다.** 탭을 오가도 보는 달은 그대로다. 아래에 두면 탭마다 달이 따로 있는 것처럼 읽힌다.

**세그먼트는 [components.md](../../design-system/components.md#세그먼트)의 그것이다.** [급여 조회](../../modules/payroll/screens/payroll.md#기간-세그먼트)는 기간 단위를 고르고 여기는 볼 것을 고르는데, 모양과 값이 같다. 칸이 둘이면 칸 하나가 절반을 먹는다 — 조각이 칸 수를 안 고정한다.

**달 이동에 제한이 없다.** 앞으로는 이번 달에서 멈추고 뒤로는 첫 근무표가 있는 달까지 간다.

#### 추이 그래프

세그먼트 바로 아래다. 탭마다 하나씩 갖고 자리가 같다 — [components.md](../../design-system/components.md#추이-그래프)의 조각이다.

| 역할 | 탭 | 그래프가 달마다 찍는 것 |
| --- | --- | --- |
| 관리자 | 근무 | 그달 근무 시간 합 |
| 관리자 | 근태 | 그달 출근율 |
| 근무자 | 근태 | 그달 내 출근율 |
| 근무자 | 포지션 | 그달 내 근무 시간 합 |
| 근무자 | 급여 | 그달 내 급여 합 |

**탭마다 선이 하나다.** 근태에 출근·지각·결근을 각각 선으로 그리면 서른 명 규모에서 지각이 한 달 0~3건이라 두 선이 바닥에 깔리고 출근 선만 위에 뜬다. 갈리는 것은 아래 [비율 띠](#근태-현황-줄)와 목록이 맡는다.

**보는 달이 오른쪽 끝이다.** 열두 달은 보는 달에서 열한 달을 거슬러 올라간 구간이고 보는 달이 그 끝에 점으로 선다. 달 줄로 지난달을 고르면 구간 전체가 한 칸 왼쪽으로 밀린다.

**열두 달인 이유는 성수기다.** 예식장은 봄과 가을에 몰려서, 여섯 달만 보면 10월에 열었을 때 지난봄이 잘려 나간다 — 근무 시간이 늘어난 것이 사람을 더 쓴 탓인지 성수기 탓인지를 못 가른다.

**앱을 쓰기 전 달은 선이 끊긴다.** 0으로 잇지 않는다. 첫 배포 직후에는 점이 하나뿐이라 그래프가 아니라 점 하나로 선다 — 달이 쌓이면 저절로 선이 된다.

**그래프를 눌러도 달이 안 바뀐다.** 달을 고르는 자리는 달 줄 하나다. 점이 열둘이면 손가락으로 집기에 좁고, 무엇보다 문이 둘이면 지금 무엇을 보고 있는지가 두 군데서 말해진다.

#### 근무

1. 합계 — 그달 근무 시간 전체. 큰 글자
2. 보조 줄 — 근무 건수
3. 사람별 구획
4. 포지션 구획

**합계가 맨 위에 크게 선다.** 이 탭에 온 사람이 먼저 묻는 것이 그 하나다.

**한 숫자를 두 축으로 가른다.** 사람별 구획과 포지션 구획의 시간 합이 둘 다 그 합계다 — 같은 432시간을 「누가 얼마나 일했나」와 「어디에 사람이 몰리나」로 나눠 본다. 탭을 가르지 않는 것은 두 목록이 같은 합계를 설명하는 자리라서다.

**구획 머리글은 [직원](../../modules/account/screens/members.md#퇴사-구획)의 그것과 같은 꼴이다.** 가는 선 아래에 머리글이 서고 그 아래에 줄이 온다.

**리허설은 안 든다.** 근무표가 들고 있는 배정만 센다 — [SCH-021](../../modules/schedule/README.md#sch-021)이 리허설을 그날 명단과 현황 머릿수에서 뺀 것과 같은 자리다. 관리자가 언제인지 모르는 일정이 통계에서만 보이면 두 화면이 다른 홀을 말하게 된다. 급여는 리허설을 합산하므로([PAY-028](../../modules/payroll/README.md#pay-028)) 이 시간과 급여가 기대는 시간은 다를 수 있다.

#### 근무 사람별 구획

[components.md](../../design-system/components.md#listrow)의 ListRow고 시간 많은 순이다.

| 자리 | 담는 것 |
| --- | --- |
| 왼쪽 | 프로필 사진 |
| 제목 | 이름 |
| 보조 정보 | 근무 회수 |
| 오른쪽 값 | 시간 합 |
| 오른쪽 | 화살표 |
| 줄 아래 | [줄 막대](../../design-system/components.md#줄-막대) |

**줄 막대가 몫을 견주게 한다.** 시간순으로 서 있어도 첫째와 둘째가 두 배 차이인지 한 끗 차이인지는 숫자를 둘 다 읽어야 안다. 막대는 가장 오래 일한 사람을 100%로 잡는다.

**이름순이 아니라 시간순이다.** 이 구획을 훑는 이유가 「누가 얼마나 나오나」라서 큰 것이 위에 있어야 한다. 사람을 찾으려면 [직원](../../modules/account/screens/members.md)으로 간다.

**줄을 누르면 근무 내역 시트가 열린다.** 화면을 안 바꾸고 시트만 올라온다. 여기서 답하는 것은 「이 시간이 어느 날들에서 나왔나」 하나다 — 그날 무슨 일이 있었는지는 근무표 날 상세의 몫이다.

**퇴사한 사람도 그 달에 일했으면 구획에 있다.** 근무 시간은 그달의 사실이고 지금 재직 중인지와 무관하다.

#### 근무 포지션 구획

아홉 포지션이 시간 많은 순이다([schedule/README.md](../../modules/schedule/README.md)).

| 자리 | 담는 것 |
| --- | --- |
| 제목 | 포지션 이름 |
| 보조 정보 | 배정 건수 |
| 오른쪽 값 | 시간 합 |
| 줄 아래 | [줄 막대](../../design-system/components.md#줄-막대) |

**막대가 아홉 줄을 한눈에 견주게 한다.** 이 구획이 답하는 것은 「어디에 사람이 몰리나」고, 숫자만 있으면 첫째와 아홉째를 눈으로 잇기 어렵다. 가장 긴 포지션이 100%다.

**시간이 0인 포지션은 막대가 없다.** 1px 선도 안 남긴다 — 빈 줄이 그대로 「여기는 한 달 내내 아무도 안 들어갔다」를 말한다.

**시간이 0인 포지션도 구획에 있다.** 아홉이 다 선다. 빠지면 그 포지션이 이 앱에 없는 것처럼 읽히고, 한 달 내내 아무도 안 들어간 자리가 있다는 것 자체가 읽을 거리다.

**교육 배정도 든다.** 정규 자리를 안 먹을 뿐 그날 나와서 같은 시간을 있었다([attendance/README.md](../../modules/attendance/README.md#att-020)). 어느 포지션의 교육이었는지로 센다.

**겸임 자리는 시간을 나눠 세지 않는다.** 한 사람이 두 포지션을 맡아도 근무 시간은 날짜마다 하나고 급여도 한 번만 센다([schedule/README.md](../../modules/schedule/README.md)). 포지션별 시간에서도 같은 시간을 양쪽에 다 얹지 않는다 — 합이 전체보다 커진다. 겸임 자리는 앞 포지션으로 센다.

**포지션 줄은 안 눌린다.** 한 화면에서 위 구획은 눌리고 아래 구획은 안 눌리는데, 화살표가 그것을 미리 말한다 — 사람별 줄에만 있고 포지션 줄에는 없다. 포지션 하나를 파고드는 화면이 따로 없기도 하다.

#### 근무 내역 시트

사람별 구획의 줄을 누르면 올라온다. [components.md](../../design-system/components.md#dialog와-바텀시트)의 바텀시트다.

1. 손잡이
2. 이름
3. 날짜 목록 — 날마다 날짜·요일·포지션·시간
4. 합계 줄

**모든 줄이 열린다.** 그달에 한 번 나온 사람은 한 줄짜리 시트다. 여러 날 나온 줄만 열리게 하면 같은 구획에서 어떤 줄은 눌리고 어떤 줄은 안 눌리게 되고, 안 눌리는 줄을 눌렀을 때 아무 일도 안 일어나는 것이 고장으로 읽힌다.

**날짜순이다.** 시간 긴 날이 위로 오지 않는다 — 이 시트는 한 달을 따라 읽는 자리라 달력 순서가 그대로 순서다.

**교육 배정도 한 줄로 선다.** 어느 포지션의 교육이었는지를 적는다 — 「안내 교육」 꼴이다.

**겸임인 날은 앞 포지션만 적는다.** 포지션 구획이 세는 방식과 같다. 두 포지션을 다 적으면 시간이 두 번 든 것처럼 읽힌다.

**합계 줄이 구획 줄의 값과 같다.** 다르면 둘 중 하나가 틀린 것이고, 그것을 눈으로 확인하는 것이 이 시트의 쓸모다. 회수도 같이 적어 보조 정보와 맞춘다.

#### 근태

1. 현황 한 줄 — 그달 전체
2. 사람별 목록 — 근무자 쪽에서는 날짜 목록

#### 근태 현황 줄

그달의 출근·지각·출근 인정·결근 넷을 한 줄에 센다. [attendance/README.md](../../modules/attendance/README.md#att-023)가 집계에 든다고 정한 넷이다.

**출근과 출근 인정을 합치지 않는다.** 둘을 합치면 인증이 실제로 얼마나 도는지 안 보이고, 그것이 [지표](../../../1-plan/metrics.md)의 출근 인증률이다.

**현황 줄 아래에 [비율 띠](../../design-system/components.md#비율-띠)가 선다.** 넷이 한 띠 안에서 몫으로 갈린다.

| 몫 | 담는 것 | 토큰 |
| --- | --- | --- |
| 첫째 | 출근 | `bg.chart-a` |
| 둘째 | 출근 인정 | `bg.chart-b` |
| 셋째 | 지각 | `bg.chart-c` |
| 넷째 | 결근 | `bg.neutral-weak` |

**결근만 중립이다.** 차트 색이 셋뿐이라 넷째가 없고, 넷째를 만드는 대신 「아무것도 안 일어난 몫」을 빈 면으로 둔다 — 결근은 나온 사람이 없는 자리라 색을 줄 일이 아니다.

**여기도 색이 좋고 나쁨을 안 말한다.** 지각이 셋째 색인 것은 순서일 뿐이고 경고색이 아니다 — 아래 목록이 [색으로 안 가르는 것](#근태-사람별-목록)과 같은 이유다.

**띠 아래에 범례가 선다.** 몫이 0인 것은 범례에서도 빠진다.

#### 근태 사람별 목록

ListRow고 이름 가나다순이다.

| 자리 | 담는 것 |
| --- | --- |
| 왼쪽 | 프로필 사진 |
| 제목 | 이름 |
| 오른쪽 값 | 출근 n · 지각 n |

**지각이 0이면 그 자리가 비어 있다.** 「지각 0」을 안 적는다 — 세로로 쌓이면 0이 줄마다 서서 실제로 지각한 사람이 안 보인다.

결근과 출근 인정은 있는 사람 줄에만 뒤에 붙는다.

**색으로 안 가른다.** 지각과 결근에 경고색을 쓰면 화면이 사람을 나무라는 자리가 된다 — [날 명단](../../modules/schedule/screens/schedule-worker.md)과 [대시보드](dashboard.md)가 같은 결이다. 이 화면은 여러 달을 겹쳐 보는 자리라 더 그렇다.

**정렬을 지각 순으로 안 바꾼다.** 그렇게 세우면 이 화면의 첫 줄이 늘 누군가를 지목하게 된다. 찾는 눈은 오른쪽 값에서 이미 멈춘다.

**근태 줄은 안 눌린다.** 근무 탭의 사람별 줄과 다른 자리다 — 여기서 열 시트가 있다면 그 사람의 지각한 날 목록인데, 한 사람의 잘못을 모아 보여주는 화면은 이 앱이 만들 것이 아니다.

#### 내 근태 날짜 목록

근무자 쪽에서 사람별 목록 자리에 선다. ListRow고 날짜순이다.

| 자리 | 담는 것 |
| --- | --- |
| 제목 | 날짜와 요일 |
| 보조 정보 | 포지션 |
| 오른쪽 값 | 인증 상태와 찍은 시각 |

**사람이 아니라 날이 줄이다.** 근무자가 이 탭에서 묻는 것은 「언제 늦었나」고 그 답은 날짜에 붙어 있다.

**상태 글자도 색으로 안 가른다.** 「지각」이 붉지 않다 — 관리자 쪽 목록과 같은 이유고, 자기 화면이라고 더 세게 말할 일이 아니다. 상태 여섯의 이름은 [attendance/design.md](../../modules/attendance/design.md)가 정본이다.

**못 찍은 날도 줄로 선다.** 사유를 냈으면 결과까지 오른쪽 값에 붙는다 — 그 자리를 눌러 사유 시트를 열지는 않는다. 사유는 [대시보드](dashboard.md)와 [날 시트](../../modules/schedule/screens/schedule-worker.md)의 문으로 들어간다.

#### 내 포지션

근무자 쪽 둘째 탭이다.

1. 합계 — 그달 내 시간 합
2. 내가 들어간 포지션만 든 목록

짜임과 줄의 자리는 관리자 [포지션 구획](#근무-포지션-구획)과 같다.

**안 들어간 포지션은 안 선다.** 관리자 쪽이 아홉을 다 세우는 것과 반대다. 관리자는 「한 달 내내 아무도 안 들어간 자리」가 읽을 거리지만, 근무자에게 그 여덟 줄은 자기가 안 한 일의 목록이라 읽을 것이 없다.

#### 내 급여

근무자 쪽 셋째 탭이다.

1. 합계 — 그달 내 급여
2. 예상치 안내 한 줄
3. 보조 줄 — 근무 건수와 시간
4. 「내역 보기」 줄

**여기 있는 것은 달 하나의 합과 열두 달의 모양이다.** 날짜별 내역과 주·연 조회는 [급여 조회](../../modules/payroll/screens/payroll.md)가 다 들고 있고, 그쪽에 없는 것이 여러 달을 한 화면에서 보는 그림 하나다 — 겹치는 것은 이 달의 합계 숫자 한 줄이다.

**「내역 보기」가 급여 화면을 연다.** [components.md](../../design-system/components.md#listrow)의 ListRow고 오른쪽에 화살표가 있다. 이 탭에서 숫자를 보다 「그래서 어느 날 얼마였나」로 넘어가는 문이다.

#### 빈 상태

관리자 쪽은 그달에 확정된 근무표가 없을 때, 근무자 쪽은 그달에 내 근무가 하나도 없을 때다. 탭이 전부 같은 모양으로 빈다.

합계 자리는 `–`로 서고 목록 자리에만 빈 상태가 온다([writing.md](../../design-system/writing.md#급여는-예상치라고-밝힌다)). [급여 조회](../../modules/payroll/screens/payroll.md#빈-상태)와 같은 방식이다.

**보조 줄과 예상치 안내는 같이 사라진다.** 「근무 0건」은 빈 상태 제목이 이미 하는 말을 숫자로 한 번 더 하는 것이고, 예상치 안내는 설명할 숫자가 있을 때 쓰는 단서라 `–` 옆에서 할 일이 없다. `–` 하나만 남고 바로 빈 상태로 내려간다.

**근무 탭은 구획 머리글도 사라진다.** 빈 상태 하나가 목록 자리를 통째로 받는다 — 머리글 둘만 남으면 사람과 포지션이 각각 비었다고 두 번 말하게 된다.

**추이 그래프는 안 빈다.** 그 달에 근무표가 없을 뿐 지난 달들은 값이 있다 — 그래프가 통째로 사라지면 빈 달을 고를 때마다 화면이 접혔다 펴진다. 빈 달은 선이 그 자리에서 끊기고 점이 안 선다.

**비율 띠와 줄 막대는 목록과 함께 사라진다.** 둘 다 목록 줄에 붙은 것이라 줄이 없으면 붙을 자리가 없다.

**근무자 급여 탭의 「내역 보기」도 사라진다.** 그 줄이 이 탭의 목록 자리를 쓰고 있어서 빈 상태가 그 자리에 들어온다. 볼 내역이 없는 달에 내역으로 가는 문만 남겨두지도 않는다 — 다른 달을 보려면 달 줄이 이미 있다.

#### 통계 색

| 자리 | 토큰 |
| --- | --- |
| 화면 바탕 | `bg.neutral` |
| 앱바 | [components.md](../../design-system/components.md#앱바)의 앱바 |
| 달 줄 글자 | `fg.neutral` |
| 달 화살표 | `fg.neutral-subtle` |
| 못 가는 화살표 | 안 그린다. 자리만 남긴다 |
| 세그먼트 | [components.md](../../design-system/components.md#세그먼트)의 세그먼트 |
| 합계 | `fg.neutral` |
| 예상치 안내 | `fg.neutral-subtle` |
| 보조 줄 | `fg.neutral-muted` |
| 현황 줄 | `fg.neutral-muted` |
| 구획 머리글 | `fg.neutral-subtle` |
| 구획 사이 가는 선 | `stroke.neutral` |
| 줄 | [components.md](../../design-system/components.md#listrow)의 ListRow |
| 값이 0인 자리 | `fg.neutral-subtle` |
| 사람별 줄 화살표 | `fg.neutral-subtle` |
| 추이 그래프 | [components.md](../../design-system/components.md#추이-그래프)의 값 |
| 줄 막대 | `bg.chart-a` |
| 비율 띠 — 출근 | `bg.chart-a` |
| 비율 띠 — 출근 인정 | `bg.chart-b` |
| 비율 띠 — 지각 | `bg.chart-c` |
| 비율 띠 — 결근 | `bg.neutral-weak` |
| 범례 글자 | `fg.neutral-muted` |
| 근무 내역 시트 | [components.md](../../design-system/components.md#dialog와-바텀시트)의 바텀시트 |
| 시트 날짜와 포지션 | `fg.neutral-muted` |
| 시트 시간 | `fg.neutral` |
| 시트 합계 줄 | `fg.neutral` |
| 「내역 보기」 화살표 | `fg.neutral-subtle` |

**이 화면에 브랜드 색이 없다.** 버튼이 없고 표식도 없다. 그림이 여럿인데도 브랜드를 안 쓰는 것은 [foundation/color.md](../../design-system/foundation/color.md#그림이-색을-쓰는-법)가 정한 것이다 — 아홉 포지션 막대 중 어느 것도 누르는 자리가 아니다.

**positive·warning·critical도 안 쓴다.** 지각과 결근에 색을 붙이지 않는 것이 이 화면의 규칙이라 차트에도 그대로 간다. 그림이 색을 갈라도 그 색들은 뜻이 없는 계열이다.

#### 통계 글자

| 자리 | 유틸 |
| --- | --- |
| 앱바 제목 | `text-lg font-semibold` |
| 달 줄 | `text-base font-medium` |
| 세그먼트 | `text-sm font-medium` |
| 합계 | `text-3xl font-bold` |
| 예상치 안내 | `text-sm` |
| 보조 줄 | `text-sm` |
| 현황 줄 | `text-sm` |
| 구획 머리글 | `text-xs font-medium` |
| 줄 제목 | `text-base font-medium` |
| 줄 보조 정보 | `text-sm` |
| 줄 값 | `text-base font-medium` |
| 그래프 가로축 | `text-xs` |
| 그래프 값 — 보는 달 | `text-xs font-medium` |
| 범례 | `text-xs` |
| 시트 이름 | `text-lg font-semibold` |
| 시트 날짜 줄 | `text-sm` |
| 시트 합계 줄 | `text-base font-medium` |
| 「내역 보기」 | `text-base font-medium` |

숫자가 세로로 쌓이는 자리 전부에 `tabular-nums`를 건다 — 합계, 줄 값, 줄 보조 정보의 회수와 건수, 시트의 시간과 합계다.

#### 통계 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 화면 좌우 여백 | `px-6` |
| 앱바와 달 줄 사이 | `mt-2` |
| 달 줄 | `py-2` |
| 달 줄과 세그먼트 사이 | `mt-3` |
| 세그먼트 | 트랙 `h-11` `rounded-lg` `p-1`, 칸은 `h-9` `rounded-[10px]` |
| 세그먼트와 그래프 사이 | `mt-6` |
| 그래프 | 높이 96px, 가로는 화면 폭 |
| 그래프와 가로축 글자 사이 | `mt-2` |
| 그래프와 합계 사이 | `mt-6` |
| 줄 막대 | 줄 아래 `mt-2`, 높이 4px |
| 비율 띠 | 현황 줄 아래 `mt-3`, 높이 8px |
| 비율 띠와 범례 사이 | `mt-2` |
| 범례 항목 사이 | `gap-4` |
| 범례와 목록 사이 | `mt-8` |
| 합계와 안내 사이 | `mt-1` |
| 안내와 보조 줄 사이 | `mt-3` |
| 보조 줄과 목록 사이 | `mt-8` |
| 구획 사이 가는 선 | 위 `mt-8` |
| 구획 머리글 | `py-2` |
| 줄 | `py-4` |
| 사진 | 36px 원 |
| 사진과 이름 사이 | `gap-3` |
| 보조 줄과 「내역 보기」 사이 | `mt-8` |
| 목록 끝 | `pb-8` |
| 시트 | [components.md](../../design-system/components.md#dialog와-바텀시트)의 바텀시트. 위쪽만 `rounded-lg` |
| 시트 이름과 날짜 목록 사이 | `mt-4` |
| 시트 날짜 줄 | `py-3` |
| 날짜 목록과 합계 줄 사이 | 가는 선 위 `mt-2`, 선 아래 `mt-3` |

## 문안

### 통계 문안

| 상태·위치 | 최종 문구 | 가변값·표기 조건 |
| --- | --- | --- |
| 앱바 제목 | 통계 | — |
| 달 줄 | 2026년 10월 | 보고 있는 달 |
| 세그먼트 — 관리자 | 근무 · 근태 | — |
| 세그먼트 — 근무자 | 근태 · 포지션 · 급여 | — |
| 근무 합계 | 432시간 | 그달 근무 시간 전체 |
| 근무 보조 줄 | 근무 48건 | 그달 근무 건수 |
| 사람별 구획 머리글 | 사람별 | — |
| 사람별 줄 보조 정보 | 12회 | 그 사람의 근무 회수 |
| 사람별 줄 값 | 108시간 | 그 사람의 시간 합 |
| 포지션 구획 머리글 | 포지션 | — |
| 포지션 줄 보조 정보 | 12건 | 그 포지션의 배정 건수 |
| 포지션 줄 값 | 108시간 | 그 포지션의 시간 합 |
| 근태 현황 줄 | 출근 41 · 지각 4 · 출근 인정 2 · 결근 1 | 그달 넷의 수 |
| 근태 줄 값 | 출근 12 · 지각 1 | 지각이 0이면 그 자리가 비고, 결근과 출근 인정은 있는 사람 줄에만 붙는다 |
| 빈 상태 제목 — 관리자 | 이 달은 아직 근무표가 없어요 | 빈 상태 |
| 빈 상태 아래 줄 — 관리자 | 근무표를 확정하면 여기 숫자가 서요 | 빈 상태 |
| 빈 상태 제목 — 근무자 | 이 달은 근무가 없어요 | 빈 상태 |
| 빈 상태 아래 줄 — 근무자 | 근무가 잡히면 여기 숫자가 서요 | 빈 상태 |
| 그래프 가로축 | 1월 · 4월 · 7월 · 10월 | 넷만 적고 나머지 여덟은 눈금만 |
| 그래프 값 — 시간 | 432시간 | 보는 달의 값 |
| 그래프 값 — 출근율 | 89% | 보는 달의 값 |
| 그래프 값 — 급여 | 130만 | 보는 달의 값. 돈은 만 단위로 줄여 적는다 |
| 근태 범례 | 출근 41 · 인정 2 · 지각 4 · 결근 1 | 몫이 0인 것은 빠진다 |
| 시트 이름 | 최윤아 | 누른 줄의 사람 |
| 시트 날짜 줄 | 10월 9일(금) · 안내 교육 | 날짜와 요일과 포지션 |
| 시트 날짜 줄 값 | 6시간 | 그날 근무 시간 |
| 시트 합계 줄 | 합계 · 8회 · 72시간 | 구획 줄의 값과 같다 |
| 내 근태 줄 제목 | 10월 3일(토) | 날짜와 요일 |
| 내 근태 줄 보조 정보 | 메인 | 그날 포지션 |
| 내 근태 줄 값 | 출근 · 09:52 | 인증 상태와 찍은 시각. 못 찍었으면 상태만 |
| 내 포지션 합계 | 108시간 | 그달 내 시간 합 |
| 내 급여 합계 | 1,296,000원 | 그달 내 급여 |
| 예상치 안내 | 예상치예요. 실제 지급액과 다를 수 있어요 | 근무자 급여 탭 |
| 내 급여 보조 줄 | 근무 12건 · 108시간 | 그달 내 근무 건수와 시간 |
| 「내역 보기」 | 내역 보기 | — |

달 줄에 연도가 붙는다. 달을 넘기다 해가 바뀌는 화면이다([writing.md](../../design-system/writing.md#숫자와-단위)).

목록에 이름이 잔뜩 서므로 「님」을 뺀다([writing.md](../../design-system/writing.md#사람-이름)).

## 모션

### 통계 모션

**등장 모션이 없다.**

세그먼트를 바꾸거나 화살표를 누르면 내용이 갈린다. 움직이는 것은 세그먼트의 고른 칸 면 하나고 `--duration-fast`다. 숫자는 안 굴린다 — 화살표를 연타해 여러 달을 훑는 자리라 모션이 붙으면 앞 것이 끝나기 전에 다음이 들어온다.

**그림도 안 자란다.** 막대가 0에서 늘어나거나 선이 왼쪽부터 그려지는 연출이 없다([components.md](../../design-system/components.md#차트-넷)). 달을 넘기면 점과 면이 `--duration-base`로 새 값 자리로 옮겨간다 — 그래프가 통째로 다시 그려지지 않는다.

**시트는 공용 모션이다.** 근무 내역 시트가 올라오고 내려가는 것은 [motion.md](../../design-system/foundation/motion.md)의 바텀시트고 이 화면이 따로 정하는 것이 없다.

## 예외와 미정

### 안 담은 것

**관리자가 보는 금액.** 홀 전체 인건비도 사람별 금액도 안 그린다. 급여를 주는 계산이 앱 밖에 있어([prd.md](../../../1-plan/prd.md)) 앱 안에서 합계를 묻는 사람이 없다. 필요해지면 근무 탭이 그 값을 얹을 자리를 이미 갖고 있다 — 사람별 구획의 오른쪽 값이다.

**그날 현황 한 줄.** 하루짜리고 [날 명단](../../modules/schedule/screens/schedule-worker.md)에 산다. 이 화면의 근태 현황 줄은 그것의 달 판이다.

**근무자의 날짜별 급여 내역.** [급여 조회](../../modules/payroll/screens/payroll.md)가 든다. 근무자 급여 탭은 달 합계와 열두 달 모양까지고 「내역 보기」로 그쪽에 넘긴다.

**리허설.** 관리자 통계 어디에도 안 든다. 본인은 [「나」](../../modules/schedule/screens/rehearsal.md)에서 자기 것을 본다.

**내보내기.** 엑셀이나 CSV로 뽑는 길을 안 만들었다. 실제 지급 계산이 앱 밖에서 일어나는데([payroll/README.md](../../modules/payroll/README.md)) 그 손 계산을 앱이 없애지 않는다는 것이 [prd.md](../../../1-plan/prd.md)의 전제다. 필요해지면 그때 연다.

**지난달 대비 증감률.** 「+18%」 같은 숫자를 안 적는다. 추이 그래프가 열두 달의 모양을 보여주는 것으로 충분하고, 한 달치 증감은 성수기가 오르내리는 폭에 묻혀 뜻이 없다 — 10월이 9월보다 오른 것은 사람을 더 쓴 것이 아니라 가을이라서다.

**사람별·포지션별 추이.** 그래프가 찍는 것은 탭마다 대표 숫자 하나뿐이다. 「김지우의 지난 열두 달」은 그리지 않는다 — 한 사람을 달 단위로 따라가는 화면은 감시에 가깝고, 이 앱이 답해야 할 질문이 아니다.

### 규칙과 부딪힌 자리

**세그먼트가 공용으로 올라갔다.** 토큰과 크기는 [components.md](../../design-system/components.md#세그먼트)가 정본이다. [급여 조회](../../modules/payroll/screens/payroll.md#기간-세그먼트)가 첫 자리고 이 화면이 둘째다. 칸이 둘인 것도 여기가 처음이다.

**겸임 자리를 앞 포지션으로 센다.** 나눠 세면 합이 안 맞고 양쪽에 다 얹으면 전체보다 커진다. 어느 쪽도 사실을 그대로 옮기지 못하는 자리고, 셋 중 합이 맞는 것을 골랐다. [schedule/README.md](../../modules/schedule/README.md)가 겸임을 자리 둘을 합친 하나로 정의해서 「앞 포지션」이 무엇인지는 그 자리가 들고 있다.

**한 화면에서 눌리는 줄과 안 눌리는 줄이 같이 선다.** 근무 탭의 사람별 줄은 눌리고 포지션 줄은 안 눌린다. 같은 목록 안이면 안 될 일이지만 구획이 갈려 있고 화살표가 미리 말한다.

**근무 시간과 급여가 기대는 시간이 다르다.** 리허설을 통계는 빼고 급여는 더한다. 두 숫자가 다른 것을 화면이 설명하지 않는다 — 통계에 리허설이 없다는 것을 관리자가 알 길이 없는 자리고, 그래도 [SCH-021](../../modules/schedule/README.md#sch-021)을 깨는 쪽보다 낫다고 봤다.

### 아직 안 정한 것

임의로 채우지 않는다. 사람이 정하고 나서 이 절에서 뺀다.

비어 있다.
