# 시급

관리자가 사람마다의 시간당 단가를 정하는 화면이다. 값은 [tokens.md](../tokens.md)에만 있고 여기는 토큰 이름으로만 말한다.

여기서 정한 값이 [급여 조회](payroll.md)와 [통계](stats.md)의 월별 인건비를 만든다. 규칙은 [domain/payroll.md](../../domain/payroll.md)에 있다.

들어오는 문은 [관리자 홈](schedule-admin.md#관리자-홈)의 「시급」 줄이다.

**정해야 할 것이 둘이다.** 전원이 기본으로 쓰는 값 하나와, 거기서 벗어난 사람들의 값이다. 따로 안 정한 사람은 [기본 시급](../../domain/payroll.md#기본-시급)을 쓰고, 기본이 바뀌면 그 사람들도 같이 바뀐다.

---

## 목록

### 짜임

세로로 쌓는다.

1. 앱바 — 뒤로와 「시급」
2. 기본 시급 줄
3. 가는 선
4. 사람 목록

**기본 시급이 맨 위에 따로 선다.** 사람 목록에 섞지 않는다. 한 줄이 여러 사람의 값을 한꺼번에 움직이는 자리라 같은 모양으로 늘어놓으면 무게가 안 보인다.

### 기본 시급 줄

[components.md](../components.md#listrow)의 ListRow고 오른쪽에 값과 화살표다. 아래에 몇 명이 이 값을 쓰는지 한 줄로 붙는다.

누르면 [기본 시급 시트](#기본-시급-시트)가 올라온다.

### 사람 줄

ListRow다. 이름 가나다순이고 개별로 정한 사람을 위로 올리지 않는다 — 관리자가 찾는 방식이 이름이지 「따로 정했는지」가 아니다.

| 자리 | 담는 것 |
| --- | --- |
| 왼쪽 | 프로필 사진 |
| 제목 | 이름 |
| 오른쪽 값 | 시급 |
| 오른쪽 | 화살표 |

**기본을 쓰는 사람을 따로 표시 안 한다.** 값만 선다. 개별로 11,000원을 정한 사람과 기본이 11,000원이라 그 값인 사람이 목록에서 같아 보이는데, 그 둘을 가르는 것이 이 목록을 훑는 이유가 아니다 — 누가 얼마를 받는지가 답이다.

누가 기본을 따라 움직이는지는 **맨 위 줄이 수로 말하고**([기본 시급 줄](#기본-시급-줄)), 정확히 누구인지는 [기본 시급 시트](#기본-시급-시트)를 열 때 알면 된다. 배지를 줄마다 붙이면 목록의 오른쪽이 값과 표식 둘로 갈려 금액을 세로로 훑기 어려워진다.

**목록에 이름이 잔뜩 서므로 「님」을 뺀다**([writing.md](../writing.md#사람-이름)).

**퇴사한 사람은 목록에 없다.** 앞으로 정할 시급이 없다. 지난 급여는 그때 적용되던 시급으로 이미 계산돼 있다([domain/payroll.md](../../domain/payroll.md#확정하지-않는다)).

줄을 누르면 [사람 시트](#사람-시트)가 올라온다.

### 빈 상태

승인된 사람이 하나도 없을 때다. [components.md](../components.md#빈-상태)의 빈 상태고 버튼이 없다.

기본 시급 줄은 그때도 그대로 선다. 사람이 없어도 값은 정해둘 수 있고, 첫 사람이 승인되는 순간 그 값이 붙는다.

---

## 기본 시급 시트

아래에서 올라온다. [components.md](../components.md#dialog와-바텀시트)의 바텀시트고 `--duration-slow`다.

1. 제목 — 기본 시급
2. 입력 칸 — 숫자와 「원」이 붙어 오른쪽에 선다
3. 아래 줄 — 몇 명이 같이 바뀌는지
4. 하단 버튼 둘 — 「닫기」(Button secondary)와 「저장」(Button primary)

**몇 명이 움직이는지를 저장 전에 말한다.** 한 번 누르면 여러 사람의 급여가 같이 달라지는 자리라 그 수가 손 앞에 있어야 한다. 0명이면 그 줄이 「아직 이 값을 쓰는 사람이 없어요」로 바뀐다.

**적용은 오늘부터다.** 날짜 고르개가 없다. 미리 넣어두는 길도 지난 날로 되돌리는 길도 없고, 근거는 [domain/payroll.md](../../domain/payroll.md#시급-변경)에 있다. 입력 칸 아래에 그 사실을 한 줄로 적는다.

**확인을 한 번 더 안 묻는다.** 여러 사람이 움직이지만 되돌릴 수 있는 자리다 — 같은 날 다시 저장하면 그날 줄을 덮어쓰니 잘못 넣은 값이 이력에 쌓이지 않는다.

값이 지금과 같으면 「저장」이 안 눌린다. 빈 칸이거나 0이어도 안 눌린다.

## 사람 시트

아래에서 올라온다. 바텀시트다.

1. 제목 — 사진과 이름
2. 입력 칸 — 숫자와 「원」이 붙어 오른쪽에 선다
3. 아래 줄 — 오늘부터 적용된다는 것
4. 기본으로 되돌리기 (개별로 정한 사람에게만)
5. 이력
6. 하단 버튼 둘 — 「닫기」와 「저장」

열 때 입력 칸에 지금 시급이 채워져 있다. 기본을 쓰는 사람도 그 값이 채워져 있고, 고쳐 저장하면 그때부터 개별이 된다.

### 기본으로 되돌리기

입력 칸 아래 한 줄이다. [Button](../components.md#button)의 ghost고 왼쪽 정렬이다.

누르면 그 사람이 다시 기본 끈에 붙는다. 되돌린 날부터 적용되고 이력에 그 줄이 선다.

**기본을 쓰는 사람에게는 이 줄이 없다.** 이미 붙어 있다.

**확인을 한 번 묻는다.** 지금 값이 기본보다 높으면 누르는 순간 내려가는 자리라서다. Dialog 하나고 바뀔 값을 그 자리에서 말한다.

### 이력

시트 안 목록이다. 최근이 위고 눌리지 않는다.

| 자리 | 담는 것 |
| --- | --- |
| 왼쪽 | 적용 시작일 |
| 오른쪽 | 그때의 시급 |

**지난 줄은 못 고치고 못 지운다.** 화살표도 더보기도 없다. 지난 급여가 흔들리지 않는다는 것이 [domain/payroll.md](../../domain/payroll.md#시급-변경)의 규칙이고, 화면에 고치는 문을 두면 그 규칙이 깨진다.

줄이 하나뿐이면 — 승인되고 아직 한 번도 안 바꾼 사람 — 이력을 안 그린다. 지금 값이 위에 이미 있다.

이력이 길어지면 다섯 줄까지만 보이고 「더 보기」가 붙는다.

---

## 색

| 자리 | 토큰 |
| --- | --- |
| 화면 바탕 | `bg.neutral` |
| 앱바 | [components.md](../components.md#앱바)의 앱바 |
| 기본 시급 줄 | [components.md](../components.md#listrow)의 ListRow |
| 기본 시급 아래 줄 | `fg.neutral-subtle` |
| 가는 선 | `stroke.neutral` |
| 사람 줄 | ListRow |
| 시트 | [components.md](../components.md#dialog와-바텀시트)의 바텀시트 |
| 입력 칸 | [components.md](../components.md#input)의 Input |
| 입력 칸 「원」 | `fg.neutral` |
| 적용 안내 | `fg.neutral-subtle` |
| 같이 바뀌는 인원 | `fg.neutral` |
| 기본으로 되돌리기 | [Button](../components.md#button) ghost |
| 이력 날짜 | `fg.neutral-muted` |
| 이력 값 | `fg.neutral` |

**브랜드 색이 「저장」 하나다.** 목록에도 이력에도 안 쓴다. 시급 액수의 높고 낮음에 색을 붙이지 않는다 — 사람마다 다른 것이 이 화면의 전제지 이상한 일이 아니다.

## 글자

| 자리 | 유틸 |
| --- | --- |
| 앱바 제목 | `text-lg font-semibold` |
| 기본 시급 이름표 | `text-base font-medium` |
| 기본 시급 값 | `text-base font-medium` |
| 기본 시급 아래 줄 | `text-xs` |
| 사람 이름 | `text-base font-medium` |
| 사람 시급 | `text-base` |
| 시트 제목 | `text-lg font-semibold` |
| 입력 글자 | `text-2xl font-semibold` |
| 적용 안내 | `text-xs` |
| 이력 줄 | `text-sm` |

금액과 날짜에 `tabular-nums`를 건다. 목록에서 금액이 세로로 쌓이는 자리다.

입력 글자가 큰 것은 이 시트에서 고치는 것이 그 하나라서다.

## 여백과 모양

| 자리 | 유틸 |
| --- | --- |
| 화면 좌우 여백 | `px-6` |
| 기본 시급 줄 | `py-4` |
| 기본 시급 아래 줄 | `mt-1` |
| 가는 선 위아래 | `my-2` |
| 사람 줄 | `py-4` |
| 사진 | 36px 원 |
| 사진과 이름 사이 | `gap-3` |
| 시트 안쪽 | `p-6` |
| 시트 제목과 입력 사이 | `mt-5` |
| 입력 칸 | `rounded-md` `p-4` |
| 입력과 안내 사이 | `mt-2` |
| 되돌리기 줄 | `mt-4` `py-2` |
| 이력 위 | `mt-6` |
| 이력 줄 | `py-3` |
| 버튼과의 간격 | `mt-6` |
| 버튼 둘 사이 | `gap-3` |

시트는 위쪽만 `rounded-lg`다.

## 문안

| 자리 | 문장 |
| --- | --- |
| 앱바 제목 | 시급 |
| 기본 시급 이름표 | 기본 시급 |
| 기본 시급 아래 줄 | 3명이 이 값을 써요 |
| 기본 시급 아래 줄 — 0명 | 아직 이 값을 쓰는 사람이 없어요 |
| 빈 상태 제목 | 아직 승인된 사람이 없어요 |
| 빈 상태 아래 줄 | 가입을 승인하면 여기 서요 |
| 기본 시트 제목 | 기본 시급 |
| 기본 시트 안내 | 오늘부터 적용돼요 |
| 기본 시트 인원 줄 | 3명의 시급이 같이 바뀌어요 |
| 사람 시트 안내 | 오늘부터 적용돼요 |
| 입력 라벨 | 시급 |
| 되돌리기 | 기본 시급으로 되돌리기 |
| 되돌리기 확인 제목 | 기본 시급으로 되돌릴까요? |
| 되돌리기 확인 본문 | 오늘부터 11,000원이 적용돼요 |
| 되돌리기 확인 왼쪽 | 닫기 |
| 되돌리기 확인 오른쪽 | 되돌리기 |
| 이력 제목 | 이력 |
| 이력 더 보기 | 더 보기 |
| 저장 뒤 토스트 | 시급을 바꿨어요 |
| 기본 저장 뒤 토스트 | 기본 시급을 바꿨어요 |
| 되돌린 뒤 토스트 | 기본 시급으로 되돌렸어요 |
| 왼쪽 버튼 | 닫기 |
| 오른쪽 버튼 | 저장 |

시급은 세 자리마다 쉼표를 찍고 「원」을 붙인다([writing.md](../writing.md#숫자와-단위)). 입력 칸 안에서도 치는 동안 쉼표가 따라 들어간다.

**「원」이 숫자 바로 뒤에 붙고, 둘이 한 덩이로 칸 오른쪽에 선다.** 숫자를 왼쪽에 두고 「원」만 오른쪽 끝으로 미는 식이 아니다 — 떨어뜨리면 `12,000`과 `원`이 다른 것 둘로 읽히고, 목록과 이력에서는 `12,000원`으로 붙어 있어 한 화면에서 같은 값이 두 모양으로 선다.

덩이를 오른쪽에 두는 것은 금액이 오른쪽으로 자라기 때문이다. 왼쪽에 붙이면 자릿수가 늘 때마다 「원」이 오른쪽으로 밀려 글자가 자리를 옮긴다. 오른쪽에 걸어두면 숫자만 왼쪽으로 자라고 단위는 그 자리에 있다.

「오늘부터 적용돼요」에 날짜를 안 적는다. 관리자가 오늘이 며칠인지 알고 있고, 날짜를 적으면 그것이 고를 수 있는 값처럼 읽힌다.

## 모션

**등장 모션이 없다.**

시트가 올라오고 내려간다. 저장하면 목록의 그 줄 값이 바뀐다 — 숫자를 안 굴리고 그냥 바뀐다. 기본 시급을 바꾸면 여러 줄이 한꺼번에 바뀌는데 순차로 흐르게 하지 않는다. 한 동작의 결과라 한 번에 바뀌는 쪽이 무슨 일이 일어났는지를 더 정확히 말한다.

---

## 안 담은 것

**조정.** 배정과 실제가 어긋난 날에 그 건의 근무 시간을 손보는 것은 [domain/payroll.md](../../domain/payroll.md#용어)의 다른 행위고, 자리는 근무표 날 상세다([schedule-admin.md](schedule-admin.md)).

**인건비 합계.** 이 화면은 단가를 정하는 자리고 합계는 [통계](stats.md)에 있다.

**최저임금 하한 검사.** 아래 [아직 안 정한 것](#아직-안-정한-것)에 있다.

**시안.** 이 문서가 서고 나서 `wages.sian.html`을 그린다.

---

## 규칙과 부딪힌 자리

**한 화면에 사람 목록이 둘이다.** 이 화면과 [직원 관리](members.md)가 같은 사람들을 늘어놓는다. 갈라둔 것은 하는 일이 달라서다 — 여기는 숫자를 훑으며 인건비 감각을 잡는 자리고, 거기는 한 사람을 놓고 역할과 이름과 퇴사를 다루는 자리다. 같은 시트에 다 넣으면 시급을 고치러 들어간 손이 퇴사 버튼 옆에 선다.

**「더 보기」가 공용으로 올라갔다.** 이력이 길 때 접는 자리고, 모양과 눌렀을 때의 동작은 [Button](../components.md#button)의 「목록을 접는 더 보기」 문단이 정본이다. 이 화면이 첫 자리고 몇 줄부터 접을지만 여기서 정한다.

**되돌리기 확인 Dialog 시안이 정본과 어긋난다.** [components.md](../components.md#dialog와-바텀시트)의 가운데 Dialog는 화면 양옆 32px, 안쪽 24px, `shadow-pop`에 `stroke.surface` 테두리인데 시안은 양옆 24px, 안쪽 20px, 시트 그림자로 그렸다. 시안을 다시 그린다.

---

## 아직 안 정한 것

임의로 채우지 않는다. 사람이 정하고 나서 이 절에서 뺀다.

- **최저임금보다 낮은 값을 막을지.** 지금은 0보다 크면 저장된다. 막으려면 해마다 바뀌는 값을 앱이 알아야 하고, 경고만 띄우는 길도 있다
- **시급의 상한.** 0 하나를 더 찍은 오타를 잡는 자리인데 상한을 정하면 정당한 값을 막을 수 있다
- **이력을 몇 줄까지 보여줄지(제안: 다섯).** 「더 보기」가 붙는 기준이고 근거가 아직 감각뿐이다
- **저장이 통신에 실패했을 때의 문안.** 시트가 안 닫히고 오류가 선다는 것까지 정했고 문장은 안 정했다
