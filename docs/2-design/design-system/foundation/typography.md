# 타이포그래피

규칙과 근거를 담는다. 크기와 행간과 굵기 값은 [tokens.md](../tokens.md#3-타이포그래피)에만 있다.

## 스케일

7단계다. 토스 TDS의 스케일을 그대로 가져왔다. 크기만이 아니라 행간까지 짝으로 정해져 있어서, 개발자가 `leading-`을 따로 고를 일이 없다.

| 유틸 | 용도 |
| --- | --- |
| `text-3xl` | 아주 큰 제목 |
| `text-2xl` | 큰 제목 |
| `text-xl` | 일반 제목 |
| `text-lg` | 작은 제목 |
| `text-base` | 일반 본문 |
| `text-sm` | 작은 본문 |
| `text-xs` | 부가 텍스트 |

이름은 Tailwind 기본 이름을 그대로 두고 값만 갈아끼웠다. `text-title-1` 같은 새 이름을 만들 수도 있었지만 그러면 shadcn이 뱉는 `text-sm`이 우리 스케일 밖에 남는다. 컴포넌트 하나 설치할 때마다 클래스를 손으로 바꿔야 하고, 안 바꾼 자리가 조용히 남는다. 이름을 빼앗는 쪽이 싸다.

`text-4xl`부터 위는 지웠다. 가장 큰 단계보다 큰 글자를 쓸 자리가 없다.

행간을 클래스로 따로 건드리지 않는다. `text-base leading-6` 같은 조합은 스케일 밖으로 나가는 것이고, 한글은 라틴 문자보다 세로로 꽉 차서 행간을 줄이면 금방 답답해진다. 토큰의 행간이 이미 그것을 감안한 값이다.

## 굵기

넷이다.

| 유틸 | 쓰는 자리 |
| --- | --- |
| `font-normal` | 본문 |
| `font-medium` | 라벨, 버튼 |
| `font-semibold` | 제목 |
| `font-bold` | 강조 |

Tailwind가 들고 오는 `font-thin`, `font-extralight`, `font-light`, `font-extrabold`, `font-black`은 지웠다.

`font-light`가 저 목록에 든 것은 서체 때문이다. Wanted Sans의 가변 축이 400에서 시작해서 300을 걸어도 400으로 눌린다. 남겨두면 눌러도 아무 일이 없는 유틸이 되고, 그걸 모르는 사람이 큰 숫자를 가늘게 만들려고 계속 집는다.

한글에서 굵기는 라틴 문자보다 티가 덜 난다. 획이 많아서 인접한 두 단계의 차이가 작다. 강조가 필요하면 굵기 한 단계보다 크기 한 단계가 잘 먹는다.

## 서체

Wanted Sans를 쓴다. Wanted Lab이 만들었고 [SIL Open Font License 1.1](https://scripts.sil.org/OFL)로 배포된다. 상업적 사용과 수정과 재배포가 전부 열려 있다.

고른 이유는 셋이다. 한글과 라틴 문자를 같이 그린 UI용 서체라 "9월 12일 10:00" 같은 문자열이 한 줄에서 어긋나지 않는다. 가변 폰트 한 장이 400부터 1000까지 덮어서 위 굵기 넷이 파일 하나에 다 든다. 그리고 만든 쪽이 `unicode-range`로 갈라둔 웹폰트 빌드를 같이 배포한다 — 서브셋을 우리가 만들 필요가 없다.

**저장소에 폰트 파일을 넣지 않는다.** jsdelivr에 올라간 조각 나눔 스타일시트를 `<link>`로 건다. 거는 코드는 [tokens.md](../tokens.md#서체-연결)에 있다.

한글 폰트는 글자 수 때문에 라틴 폰트보다 훨씬 무겁다. 통짜 한 장이 1,259KB고, self-host하면 첫 화면에서 그걸 다 받는다. 조각 나눔은 화면에 실제로 찍힌 글자가 든 조각만 받게 해서 같은 화면을 100~300KB로 끝낸다. 매일 십 초 쓰는 앱이라 첫 화면의 무게가 전부다.

바깥 호스트를 거치니 연결이 한 번 더 생긴다. 그 값은 `preconnect`로 미리 치른다. 폰트가 늦게 와도 글자가 먼저 보이게 하는 `font-display: swap`은 그 스타일시트가 이미 걸어두었다.

굵기를 `font-light`에서 뺀 것도 이 서체 때문이다. 가변 축이 400에서 시작해 300을 받아주지 않는다.

## 숫자 정렬

시각과 금액에는 `tabular-nums`를 건다.

```
<span className="tabular-nums">126,000원</span>
```

`tabular-nums`가 없으면 숫자마다 폭이 달라서 세로로 쌓인 목록에서 자릿수가 어긋난다. `1`은 좁고 `0`은 넓어서 급여 내역을 훑을 때 금액이 들쭉날쭉해 보인다. 근무 시각도 마찬가지다. `11:00`과 `10:00`이 다른 폭으로 찍힌다.

거는 자리는 이렇다.

- 근무 시각과 인증 시각
- 급여 금액과 시급
- 날짜와 근무 시간 합계

문장 안에 섞인 숫자에는 안 건다. "근무자 30명"처럼 흐르는 글에서는 오히려 어색하다.

## 크기를 하드코딩하지 않는다

`font-size`를 직접 쓰거나 대괄호 임의값으로 크기를 박지 않는다. 토큰만 쓴다.

토큰 값은 rem이다. iOS와 안드로이드의 접근성 글자 크기 설정, 그리고 브라우저의 기본 글자 크기 설정이 rem에는 걸리고 px에는 안 걸린다. px로 박으면 근무자가 시스템에서 글자를 키워도 앱만 그대로 남는다.

TDS가 이 원칙을 명시해뒀고 우리도 그대로 가져온다. 웨딩홀 근무자 서른 명 중에 글자를 키워 쓰는 사람이 반드시 있다.

스케일 밖 크기가 필요해 보이면 대개 스케일이 아니라 레이아웃이 잘못된 것이다. 그래도 필요하면 토큰을 하나 더하고 [tokens.md](../tokens.md#3-타이포그래피)에 줄을 더한다.

## 화면에서 자주 쓰는 자리

출발점이다. 화면별 디자인이 나오면 그쪽이 정본이 된다.

| 자리 | 단계 | 굵기 |
| --- | --- | --- |
| 급여 금액 | `text-3xl` | `font-normal` |
| 화면 제목 | `text-xl` | `font-semibold` |
| 카드 제목 | `text-lg` | `font-semibold` |
| 본문 | `text-base` | `font-normal` |
| 목록 한 줄의 이름 | `text-base` | `font-medium` |
| 목록 한 줄의 보조 정보 | `text-sm` | `font-normal` |
| 버튼 라벨 | `text-base` | `font-medium` |
| 배지, 시각, 날짜 | `text-xs` | `font-medium` |

## 출처

스케일과 접근성 원칙은 토스 TDS의 [Consumer UX Guide](https://developers-apps-in-toss.toss.im/design/consumer-ux-guide)에서 가져왔다.

Wanted Sans는 Wanted Lab이 만들었고 [SIL Open Font License 1.1](https://scripts.sil.org/OFL)로 배포된다. 원본은 [wanteddev/wanted-sans](https://github.com/wanteddev/wanted-sans)에 있다.
