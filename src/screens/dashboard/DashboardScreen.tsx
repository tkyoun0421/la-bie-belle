import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function DashboardScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="App Dashboard"
      title="Operational dashboard bootstrap"
      description="This route now resolves through the flow layer and is ready for the first real operational slice."
    />
  );
}
