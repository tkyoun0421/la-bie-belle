import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function AdminRequestsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Admin Requests"
      title="Approval queue bootstrap"
      description="Membership request review will be implemented in a later auth and onboarding slice."
    />
  );
}
