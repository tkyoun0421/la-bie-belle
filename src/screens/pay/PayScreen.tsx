import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function PayScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Payroll Preview"
      title="Expected pay view bootstrap"
      description="Batch-read payroll preview and override handling will be wired into this flow."
    />
  );
}
