import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function AdminPayrollRulesScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Admin Payroll Rules"
      title="Payroll rule bootstrap"
      description="Expected-pay rules and override history will be wired into this admin flow."
    />
  );
}
