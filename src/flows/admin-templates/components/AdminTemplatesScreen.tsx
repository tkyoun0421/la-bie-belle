import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function AdminTemplatesScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Admin Templates"
      title="Event template bootstrap"
      description="Template CRUD and slot defaults will be implemented in the event slices."
    />
  );
}
