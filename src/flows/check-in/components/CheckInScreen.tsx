import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function CheckInScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Check-in"
      title="Attendance flow bootstrap"
      description="Location-aware check-in, exception request, and manager review will land here."
    />
  );
}
