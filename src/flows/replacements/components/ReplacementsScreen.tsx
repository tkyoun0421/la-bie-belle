import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function ReplacementsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Replacements"
      title="Replacement review queue"
      description="This screen is reserved for the replacement request and candidate approval slices."
    />
  );
}
