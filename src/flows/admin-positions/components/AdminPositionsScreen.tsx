import { ScreenPlaceholder } from "#/shared/components/ScreenPlaceholder";

export function AdminPositionsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Admin Positions"
      title="Position matrix bootstrap"
      description="Qualified and training position management will be added here."
    />
  );
}
