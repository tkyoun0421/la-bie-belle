# 리뷰

## 결정

PASS, 94/100.

## 잘된 점

- 기존 9개 카테고리를 유지하면서 31개 하위 배점으로 실제 채점 단위를 세분화했다.
- 각 하위 항목에 `quality_levels.full/partial/minimal/zero`를 추가해 내부 품질 판단 기준을 고정했다.
- `validate-score.mjs`가 하위 점수 합계, 하위 배점 합계, 객체형 감점, 품질 기준 누락을 검증한다.
- 기존 #47 점수 파일은 하위 항목 없이도 통과해 하위 호환성을 유지했다.

## 부족한 점

- 대시보드 UI에는 아직 하위 항목 표시가 없다.
- PowerShell 기본 인코딩으로 UTF-8 JSON을 읽으면 깨질 수 있어 Node 기반 검증을 기준으로 삼았다.
- JSON Schema 자체를 별도 엔진으로 검증하는 테스트는 추가하지 않았다.

## 권장 다음 작업

PR 생성 가능. 후속으로 대시보드 상세 영역에 `subcriteria` 점수 표시를 추가하면 이번 세부 점수화의 가시성이 좋아진다.
